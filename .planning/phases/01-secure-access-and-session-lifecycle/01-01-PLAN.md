---
phase: 01-secure-access-and-session-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gradlew
  - gradlew.bat
  - gradle/wrapper/gradle-wrapper.properties
  - build.gradle.kts
  - settings.gradle.kts
  - src/main/java/com/collabeditor/CollaborativeCodeEditorApplication.java
  - src/main/resources/application.yml
  - src/main/resources/db/migration/V1__phase1_baseline.sql
  - src/test/java/com/collabeditor/migration/FlywayMigrationTest.java
  - src/test/resources/application-test.yml
autonomous: true
requirements:
  - QUAL-01
must_haves:
  truths:
    - "The repository has a runnable Gradle wrapper and Spring Boot application skeleton for Phase 1."
    - "Flyway applies versioned schema migrations automatically during application startup."
    - "The baseline schema supports users, refresh sessions, coding sessions, and session participants."
    - "Schema integrity is covered by an automated migration smoke test."
  artifacts:
    - path: "build.gradle.kts"
      provides: "Gradle Kotlin DSL build with Spring Boot, Flyway, JWT, and test dependencies."
    - path: "src/main/java/com/collabeditor/CollaborativeCodeEditorApplication.java"
      provides: "Spring Boot entry point for the backend service."
    - path: "src/main/resources/db/migration/V1__phase1_baseline.sql"
      provides: "Baseline Phase 1 schema for auth and session lifecycle persistence."
    - path: "src/test/java/com/collabeditor/migration/FlywayMigrationTest.java"
      provides: "Automated migration verification against PostgreSQL."
  key_links:
    - from: "src/main/resources/application.yml"
      to: "src/main/resources/db/migration/V1__phase1_baseline.sql"
      via: "spring.flyway.locations"
      pattern: "spring\\.flyway"
    - from: "src/test/java/com/collabeditor/migration/FlywayMigrationTest.java"
      to: "src/main/resources/db/migration/V1__phase1_baseline.sql"
      via: "test-boot migration execution"
      pattern: "V1__phase1_baseline"
---

<objective>
Bootstrap the Spring Boot backend foundation and baseline Flyway schema for Phase 1.

Purpose: Every later auth and session plan depends on a runnable build, shared configuration, and a versioned persistence baseline.
Output: Gradle wrapper and build files, Spring Boot app/config, baseline SQL migration, and a PostgreSQL-backed migration smoke test.
</objective>

<execution_context>
@/Users/abdullah/.codex/get-shit-done/workflows/execute-plan.md
@/Users/abdullah/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-secure-access-and-session-lifecycle/01-CONTEXT.md
@.planning/phases/01-secure-access-and-session-lifecycle/01-RESEARCH.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap the Gradle wrapper, Spring Boot build, and base configuration</name>
  <read_first>
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/phases/01-secure-access-and-session-lifecycle/01-CONTEXT.md
    .planning/phases/01-secure-access-and-session-lifecycle/01-RESEARCH.md
    AGENTS.md
    build.gradle.kts
    settings.gradle.kts
    src/main/resources/application.yml
  </read_first>
  <files>gradlew, gradlew.bat, gradle/wrapper/gradle-wrapper.properties, build.gradle.kts, settings.gradle.kts, src/main/java/com/collabeditor/CollaborativeCodeEditorApplication.java, src/main/resources/application.yml</files>
  <action>
    Create the Gradle wrapper with `gradle wrapper --gradle-version 8.10` so later plans can use `./gradlew` only. Set `rootProject.name = "collaborative-code-editor"` in `settings.gradle.kts`. In `build.gradle.kts`, use Spring Boot `3.3.4`, Java toolchain `17`, and exact dependencies `spring-boot-starter-web`, `spring-boot-starter-security`, `spring-boot-starter-data-jpa`, `spring-boot-starter-validation`, `org.flywaydb:flyway-core`, `org.flywaydb:flyway-database-postgresql`, `org.postgresql:postgresql`, `io.jsonwebtoken:jjwt-api:0.12.5`, `io.jsonwebtoken:jjwt-impl:0.12.5`, `io.jsonwebtoken:jjwt-jackson:0.12.5`, `spring-boot-starter-test`, `spring-security-test`, `org.testcontainers:junit-jupiter`, and `org.testcontainers:postgresql`; enable `useJUnitPlatform()`. Create `src/main/java/com/collabeditor/CollaborativeCodeEditorApplication.java` as the application entry point. Create `src/main/resources/application.yml` with datasource env vars `APP_DB_URL`, `APP_DB_USERNAME`, `APP_DB_PASSWORD`, `spring.jpa.hibernate.ddl-auto: validate`, and `spring.flyway.locations: classpath:db/migration`. Keep all generated Java sources under the `com.collabeditor` package root.
  </action>
  <acceptance_criteria>
    - `rg -n "org\\.springframework\\.boot\" version \"3\\.3\\.4\"|JavaLanguageVersion\\.of\\(17\\)|jjwt-api:0\\.12\\.5|testcontainers:postgresql" build.gradle.kts` returns all four configuration markers.
    - `rg -n "rootProject\\.name = \"collaborative-code-editor\"" settings.gradle.kts` returns the exact root project name.
    - `rg -n "APP_DB_URL|ddl-auto: validate|flyway:" src/main/resources/application.yml` returns datasource, JPA, and Flyway configuration lines.
    - `./gradlew help` exits with status 0.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew help</automated>
  </verify>
  <done>The repository can build through the Gradle wrapper and exposes the baseline Spring Boot configuration needed for auth and session work.</done>
</task>

<task type="auto">
  <name>Task 2: Create the Phase 1 baseline Flyway schema</name>
  <read_first>
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/phases/01-secure-access-and-session-lifecycle/01-CONTEXT.md
    src/main/resources/db/migration/V1__phase1_baseline.sql
  </read_first>
  <files>src/main/resources/db/migration/V1__phase1_baseline.sql</files>
  <action>
    Create `src/main/resources/db/migration/V1__phase1_baseline.sql` defining exactly four tables: `users`, `refresh_sessions`, `coding_sessions`, and `session_participants`. In `users`, include `id uuid primary key`, unique lowercase `email`, `password_hash`, `created_at`, and `updated_at`. In `refresh_sessions`, include `id uuid primary key`, `user_id`, unique `token_hash`, `device_id uuid`, `user_agent text`, `expires_at`, `revoked_at`, `replaced_by_session_id uuid`, `created_at`, and `last_used_at` to support rotation and multi-device tracking. In `coding_sessions`, include `id uuid primary key`, unique `invite_code varchar(8)`, `language varchar(16)` constrained to `JAVA` or `PYTHON`, `owner_user_id uuid`, `participant_cap smallint default 12`, `empty_since`, `cleanup_after`, `created_at`, and `updated_at`. In `session_participants`, include `session_id`, `user_id`, `role varchar(16)`, `status varchar(16)`, `joined_at`, `left_at`, and a uniqueness constraint on `(session_id, user_id)` so a user keeps one membership row per room. Add supporting indexes for `users(email)`, `refresh_sessions(token_hash)`, `coding_sessions(invite_code)`, and active participant lookups by `session_id` and `status`.
  </action>
  <acceptance_criteria>
    - `rg -n "CREATE TABLE users|CREATE TABLE refresh_sessions|CREATE TABLE coding_sessions|CREATE TABLE session_participants" src/main/resources/db/migration/V1__phase1_baseline.sql` returns all four table definitions.
    - `rg -n "token_hash|device_id|invite_code|owner_user_id|participant_cap|empty_since|cleanup_after" src/main/resources/db/migration/V1__phase1_baseline.sql` returns all required lifecycle-support columns.
    - `rg -n "CHECK \\(language IN \\('JAVA', 'PYTHON'\\)\\)|UNIQUE \\(session_id, user_id\\)" src/main/resources/db/migration/V1__phase1_baseline.sql` returns the language and membership constraints.
    - `./gradlew compileJava` exits with status 0.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew compileJava</automated>
  </verify>
  <done>The schema baseline captures every persistence primitive that the Phase 1 auth and session APIs rely on.</done>
</task>

<task type="auto">
  <name>Task 3: Add a PostgreSQL-backed Flyway migration smoke test</name>
  <read_first>
    src/main/resources/db/migration/V1__phase1_baseline.sql
    src/test/java/com/collabeditor/migration/FlywayMigrationTest.java
    src/test/resources/application-test.yml
    build.gradle.kts
  </read_first>
  <files>src/test/java/com/collabeditor/migration/FlywayMigrationTest.java, src/test/resources/application-test.yml</files>
  <action>
    Create `src/test/java/com/collabeditor/migration/FlywayMigrationTest.java` with `@Testcontainers` and `PostgreSQLContainer<>("postgres:16-alpine")`. Use `@DynamicPropertySource` to wire the container into Spring Boot and assert through `JdbcTemplate` or JDBC metadata that the migrated schema contains `users.email`, `refresh_sessions.device_id`, `coding_sessions.invite_code`, `coding_sessions.cleanup_after`, and `session_participants.role`. Create `src/test/resources/application-test.yml` with `spring.flyway.locations: classpath:db/migration`, `spring.jpa.hibernate.ddl-auto: validate`, and a dedicated test profile so the migration test is stable in local and CI runs.
  </action>
  <acceptance_criteria>
    - `rg -n "@Testcontainers|PostgreSQLContainer|DynamicPropertySource" src/test/java/com/collabeditor/migration/FlywayMigrationTest.java` returns all three markers.
    - `rg -n "invite_code|device_id|cleanup_after|session_participants|role" src/test/java/com/collabeditor/migration/FlywayMigrationTest.java` returns the expected schema assertion references.
    - `rg -n "ddl-auto: validate|flyway:" src/test/resources/application-test.yml` returns the exact test configuration markers.
    - `./gradlew test --tests "*FlywayMigrationTest"` exits with status 0.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "*FlywayMigrationTest"</automated>
  </verify>
  <done>Flyway migration correctness is exercised against PostgreSQL before the auth and session plans build on top of the schema.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `./gradlew help`
- [ ] `./gradlew compileJava`
- [ ] `./gradlew test --tests "*FlywayMigrationTest"`
</verification>

<success_criteria>
- The Gradle wrapper and Spring Boot application build locally.
- Flyway V1 baseline migration defines the auth and session lifecycle schema.
- PostgreSQL-backed migration smoke tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/01-secure-access-and-session-lifecycle/01-01-SUMMARY.md`
</output>
