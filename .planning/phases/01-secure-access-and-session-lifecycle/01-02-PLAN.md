---
phase: 01-secure-access-and-session-lifecycle
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/main/java/com/collabeditor/auth/api/AuthController.java
  - src/main/java/com/collabeditor/auth/api/dto/RegisterRequest.java
  - src/main/java/com/collabeditor/auth/api/dto/LoginRequest.java
  - src/main/java/com/collabeditor/auth/api/dto/AuthTokenResponse.java
  - src/main/java/com/collabeditor/auth/service/AuthService.java
  - src/main/java/com/collabeditor/auth/service/JwtTokenService.java
  - src/main/java/com/collabeditor/auth/service/RefreshSessionService.java
  - src/main/java/com/collabeditor/auth/security/SecurityConfig.java
  - src/main/java/com/collabeditor/auth/security/JwtAuthenticationFilter.java
  - src/main/java/com/collabeditor/auth/persistence/UserRepository.java
  - src/main/java/com/collabeditor/auth/persistence/RefreshSessionRepository.java
  - src/main/java/com/collabeditor/auth/persistence/entity/UserEntity.java
  - src/main/java/com/collabeditor/auth/persistence/entity/RefreshSessionEntity.java
  - src/main/java/com/collabeditor/common/api/ApiExceptionHandler.java
  - src/main/resources/application.yml
  - src/test/java/com/collabeditor/auth/JwtTokenServiceTest.java
  - src/test/java/com/collabeditor/auth/RefreshSessionServiceTest.java
  - src/test/java/com/collabeditor/auth/AuthServiceTest.java
  - src/test/java/com/collabeditor/auth/AuthControllerTest.java
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
must_haves:
  truths:
    - "A user can register with email and password and then log in to receive a bearer access token."
    - "Refresh tokens are delivered only through an httpOnly cookie, never through the JSON response body."
    - "Refreshing rotates the refresh token, preserves the device session, and invalidates reuse."
    - "Protected routes accept only valid bearer access tokens issued by the app."
  artifacts:
    - path: "src/main/java/com/collabeditor/auth/api/AuthController.java"
      provides: "Register, login, and refresh REST endpoints."
    - path: "src/main/java/com/collabeditor/auth/service/AuthService.java"
      provides: "Auth workflow orchestration for registration, login, and refresh rotation."
    - path: "src/main/java/com/collabeditor/auth/security/SecurityConfig.java"
      provides: "Spring Security filter chain for bearer token authentication."
    - path: "src/test/java/com/collabeditor/auth/AuthControllerTest.java"
      provides: "Automated verification of auth API contracts and cookie behavior."
  key_links:
    - from: "src/main/java/com/collabeditor/auth/security/SecurityConfig.java"
      to: "src/main/java/com/collabeditor/auth/security/JwtAuthenticationFilter.java"
      via: "filter chain registration for bearer token parsing"
      pattern: "addFilterBefore"
    - from: "src/main/java/com/collabeditor/auth/api/AuthController.java"
      to: "src/main/java/com/collabeditor/auth/service/AuthService.java"
      via: "controller delegates register, login, and refresh flows"
      pattern: "authService\\.(register|login|refresh)"
    - from: "src/main/java/com/collabeditor/auth/service/RefreshSessionService.java"
      to: "src/main/java/com/collabeditor/auth/persistence/RefreshSessionRepository.java"
      via: "refresh token lookup, rotation, and revocation persistence"
      pattern: "refreshSessionRepository\\."
---

<objective>
Implement JWT-based authentication with short-lived bearer access tokens and rotating refresh cookies.

Purpose: Phase 1 session APIs and every later protected capability depend on a stable auth contract now.
Output: Auth persistence model, JWT/security infrastructure, register/login/refresh endpoints, and automated auth tests.
</objective>

<execution_context>
@/Users/abdullah/.codex/get-shit-done/workflows/execute-plan.md
@/Users/abdullah/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-secure-access-and-session-lifecycle/01-CONTEXT.md
@.planning/phases/01-secure-access-and-session-lifecycle/01-RESEARCH.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement auth persistence, token services, and security configuration</name>
  <read_first>
    .planning/phases/01-secure-access-and-session-lifecycle/01-CONTEXT.md
    .planning/phases/01-secure-access-and-session-lifecycle/01-RESEARCH.md
    src/main/resources/db/migration/V1__phase1_baseline.sql
    src/main/resources/application.yml
    src/main/java/com/collabeditor/auth/service/JwtTokenService.java
    src/main/java/com/collabeditor/auth/service/RefreshSessionService.java
    src/main/java/com/collabeditor/auth/security/SecurityConfig.java
    src/main/java/com/collabeditor/auth/security/JwtAuthenticationFilter.java
  </read_first>
  <files>src/main/java/com/collabeditor/auth/service/JwtTokenService.java, src/main/java/com/collabeditor/auth/service/RefreshSessionService.java, src/main/java/com/collabeditor/auth/security/SecurityConfig.java, src/main/java/com/collabeditor/auth/security/JwtAuthenticationFilter.java, src/main/java/com/collabeditor/auth/persistence/UserRepository.java, src/main/java/com/collabeditor/auth/persistence/RefreshSessionRepository.java, src/main/java/com/collabeditor/auth/persistence/entity/UserEntity.java, src/main/java/com/collabeditor/auth/persistence/entity/RefreshSessionEntity.java, src/main/resources/application.yml, src/test/java/com/collabeditor/auth/JwtTokenServiceTest.java, src/test/java/com/collabeditor/auth/RefreshSessionServiceTest.java</files>
  <action>
    Map `UserEntity` to the `users` table and `RefreshSessionEntity` to `refresh_sessions` from `V1__phase1_baseline.sql`. Implement `JwtTokenService` to mint access JWTs signed with HS256 using `APP_JWT_SECRET`, issuer `collaborative-code-editor`, subject equal to the user UUID, an `email` claim, and an access-token TTL of exactly `PT15M`. Implement `RefreshSessionService` to mint opaque 32-byte Base64URL refresh tokens, store only the SHA-256 hex digest in `refresh_sessions.token_hash`, and preserve a stable `device_id` across rotations by creating a replacement row and writing `replaced_by_session_id` on the old row. Configure `SecurityConfig` for stateless auth with public routes `/api/auth/register`, `/api/auth/login`, `/api/auth/refresh`, and `/actuator/health`, and require bearer auth everywhere else. Add application properties `app.security.access-token-ttl: PT15M`, `app.security.refresh-token-ttl: P30D`, `app.security.refresh-cookie-name: ccd_refresh_token`, `app.security.refresh-cookie-path: /api/auth`, and `app.security.refresh-cookie-same-site: Strict`. Implement `JwtAuthenticationFilter` to parse `Authorization: Bearer <token>` and populate the authenticated principal only when the JWT is valid.
  </action>
  <acceptance_criteria>
    - `rg -n "PT15M|P30D|ccd_refresh_token|refresh-cookie-path: /api/auth" src/main/resources/application.yml` returns all configured auth properties.
    - `rg -n "issuer\\(\"collaborative-code-editor\"\\)|setSubject|claim\\(\"email\"" src/main/java/com/collabeditor/auth/service/JwtTokenService.java` returns the issuer, subject, and email claim wiring.
    - `rg -n "MessageDigest|getInstance\\(\"SHA-256\"\\)|replacedBySessionId|deviceId" src/main/java/com/collabeditor/auth/service/RefreshSessionService.java` returns hashed refresh token persistence and rotation markers.
    - `rg -n "STATELESS|/api/auth/register|/api/auth/login|/api/auth/refresh|addFilterBefore" src/main/java/com/collabeditor/auth/security/SecurityConfig.java` returns the public-route and filter-chain configuration.
    - `./gradlew test --tests "*JwtTokenServiceTest" --tests "*RefreshSessionServiceTest"` exits with status 0.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "*JwtTokenServiceTest" --tests "*RefreshSessionServiceTest"</automated>
  </verify>
  <done>Auth primitives are in place: JWT issuance, hashed refresh-session persistence, and a security filter chain that can protect later APIs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement register, login, and refresh HTTP contracts</name>
  <read_first>
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/phases/01-secure-access-and-session-lifecycle/01-CONTEXT.md
    src/main/java/com/collabeditor/auth/service/AuthService.java
    src/main/java/com/collabeditor/auth/api/AuthController.java
    src/main/java/com/collabeditor/common/api/ApiExceptionHandler.java
    src/test/java/com/collabeditor/auth/AuthControllerTest.java
  </read_first>
  <files>src/main/java/com/collabeditor/auth/api/AuthController.java, src/main/java/com/collabeditor/auth/api/dto/RegisterRequest.java, src/main/java/com/collabeditor/auth/api/dto/LoginRequest.java, src/main/java/com/collabeditor/auth/api/dto/AuthTokenResponse.java, src/main/java/com/collabeditor/auth/service/AuthService.java, src/main/java/com/collabeditor/common/api/ApiExceptionHandler.java, src/test/java/com/collabeditor/auth/AuthControllerTest.java</files>
  <action>
    Create `POST /api/auth/register`, `POST /api/auth/login`, and `POST /api/auth/refresh` in `AuthController`. `register` must accept `email` and `password`, lowercase and trim the email, reject duplicates with HTTP `409`, hash the password with `BCryptPasswordEncoder`, and return `201` without issuing a refresh token. `login` must validate credentials, create a new refresh session row, return JSON `{ accessToken, expiresInSeconds, userId, email }`, and set the raw refresh token only in an httpOnly cookie named `ccd_refresh_token` with `Path=/api/auth`, `SameSite=Strict`, and `Max-Age=2592000`. `refresh` must read the cookie, rotate the refresh session on every success, return a new access token JSON body, and overwrite the refresh cookie with the new raw token; it must never expose the refresh token in JSON. Add a shared `ApiExceptionHandler` so duplicate email, bad credentials, expired refresh token, and reused refresh token map to deterministic `409` or `401` responses with a small JSON error payload.
  </action>
  <acceptance_criteria>
    - `rg -n "@PostMapping\\(\"/api/auth/register\"\\)|@PostMapping\\(\"/api/auth/login\"\\)|@PostMapping\\(\"/api/auth/refresh\"\\)" src/main/java/com/collabeditor/auth/api/AuthController.java` returns all three endpoint mappings.
    - `rg -n "ccd_refresh_token|httpOnly|SameSite=Strict|2592000" src/main/java/com/collabeditor/auth/service/AuthService.java src/main/java/com/collabeditor/auth/api/AuthController.java` returns cookie construction markers.
    - `rg -n "accessToken|expiresInSeconds|userId|email" src/main/java/com/collabeditor/auth/api/dto/AuthTokenResponse.java` returns the exact access-token response fields.
    - `rg -n "409|401|duplicate|bad credentials|refresh token" src/main/java/com/collabeditor/common/api/ApiExceptionHandler.java` returns explicit exception-to-status mapping.
    - `./gradlew test --tests "*AuthControllerTest"` exits with status 0.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "*AuthControllerTest"</automated>
  </verify>
  <done>The auth REST contract is concrete and matches the chosen bearer-access plus httpOnly-refresh-token model.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add auth service and controller coverage for rotation and failure paths</name>
  <read_first>
    src/main/java/com/collabeditor/auth/service/AuthService.java
    src/main/java/com/collabeditor/auth/service/RefreshSessionService.java
    src/test/java/com/collabeditor/auth/AuthServiceTest.java
    src/test/java/com/collabeditor/auth/AuthControllerTest.java
  </read_first>
  <files>src/test/java/com/collabeditor/auth/AuthServiceTest.java, src/test/java/com/collabeditor/auth/AuthControllerTest.java, src/test/java/com/collabeditor/auth/RefreshSessionServiceTest.java</files>
  <action>
    Add focused tests that cover the behaviors the roadmap depends on: duplicate-email rejection during registration, successful login issuing an access token plus refresh cookie, invalid-password login returning `401`, successful refresh rotating the refresh session and reusing the same `device_id`, and refresh-token reuse revoking the session chain and returning `401`. Use `MockMvc` for controller behavior and direct service tests for rotation internals. Keep test names explicit, such as `shouldRotateRefreshTokenOnRefresh` and `shouldRejectRefreshTokenReuse`, so execution logs are easy to scan.
  </action>
  <acceptance_criteria>
    - `rg -n "shouldRejectDuplicateEmail|shouldIssueAccessTokenAndRefreshCookie|shouldRejectInvalidPassword|shouldRotateRefreshTokenOnRefresh|shouldRejectRefreshTokenReuse" src/test/java/com/collabeditor/auth/AuthServiceTest.java src/test/java/com/collabeditor/auth/AuthControllerTest.java src/test/java/com/collabeditor/auth/RefreshSessionServiceTest.java` returns all five named scenarios.
    - `rg -n "Set-Cookie|ccd_refresh_token|Authorization" src/test/java/com/collabeditor/auth/AuthControllerTest.java` returns auth contract assertions for cookies and bearer usage.
    - `./gradlew test --tests "*AuthServiceTest" --tests "*AuthControllerTest" --tests "*RefreshSessionServiceTest"` exits with status 0.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "*AuthServiceTest" --tests "*AuthControllerTest" --tests "*RefreshSessionServiceTest"</automated>
  </verify>
  <done>Phase 1 auth requirements are backed by automated coverage for the success path and the important failure path regressions.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `./gradlew test --tests "*JwtTokenServiceTest" --tests "*RefreshSessionServiceTest"`
- [ ] `./gradlew test --tests "*AuthControllerTest"`
- [ ] `./gradlew test --tests "*AuthServiceTest" --tests "*AuthControllerTest" --tests "*RefreshSessionServiceTest"`
</verification>

<success_criteria>
- Users can register and log in through the Phase 1 auth API.
- Access tokens are short-lived bearer JWTs and refresh tokens are cookie-only.
- Refresh tokens rotate on every successful refresh and reuse is rejected.
</success_criteria>

<output>
After completion, create `.planning/phases/01-secure-access-and-session-lifecycle/01-02-SUMMARY.md`
</output>
