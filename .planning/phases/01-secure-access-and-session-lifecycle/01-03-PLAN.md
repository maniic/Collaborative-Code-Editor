---
phase: 01-secure-access-and-session-lifecycle
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/main/java/com/collabeditor/CollaborativeCodeEditorApplication.java
  - src/main/java/com/collabeditor/session/api/SessionController.java
  - src/main/java/com/collabeditor/session/api/dto/CreateSessionRequest.java
  - src/main/java/com/collabeditor/session/api/dto/JoinSessionRequest.java
  - src/main/java/com/collabeditor/session/api/dto/SessionResponse.java
  - src/main/java/com/collabeditor/session/api/dto/SessionSummaryResponse.java
  - src/main/java/com/collabeditor/session/service/SessionService.java
  - src/main/java/com/collabeditor/session/service/SessionCleanupScheduler.java
  - src/main/java/com/collabeditor/session/persistence/CodingSessionRepository.java
  - src/main/java/com/collabeditor/session/persistence/SessionParticipantRepository.java
  - src/main/java/com/collabeditor/session/persistence/entity/CodingSessionEntity.java
  - src/main/java/com/collabeditor/session/persistence/entity/SessionParticipantEntity.java
  - src/main/resources/application.yml
  - src/test/java/com/collabeditor/session/SessionServiceTest.java
  - src/test/java/com/collabeditor/session/SessionControllerTest.java
  - src/test/java/com/collabeditor/session/SessionCleanupSchedulerTest.java
autonomous: true
requirements:
  - SESS-01
  - SESS-02
  - SESS-03
  - SESS-04
must_haves:
  truths:
    - "An authenticated user can create a session with a fixed language and generated invite code."
    - "An authenticated user can list only sessions they own or have joined."
    - "Joining happens through the invite code, not the canonical UUID, and leaving applies deterministic ownership transfer."
    - "Empty sessions remain recoverable for one hour, then a scheduled cleanup removes expired empty rooms."
  artifacts:
    - path: "src/main/java/com/collabeditor/session/api/SessionController.java"
      provides: "Create/list/join/leave REST endpoints."
    - path: "src/main/java/com/collabeditor/session/service/SessionService.java"
      provides: "Session lifecycle policy enforcement (cap, owner transfer, visibility, TTL)."
    - path: "src/main/java/com/collabeditor/session/service/SessionCleanupScheduler.java"
      provides: "Automated cleanup of expired empty sessions."
    - path: "src/test/java/com/collabeditor/session/SessionServiceTest.java"
      provides: "Automated lifecycle policy validation."
  key_links:
    - from: "src/main/java/com/collabeditor/session/api/SessionController.java"
      to: "src/main/java/com/collabeditor/session/service/SessionService.java"
      via: "controller delegates all lifecycle commands"
      pattern: "sessionService\\.(createSession|listSessions|joinSession|leaveSession)"
    - from: "src/main/java/com/collabeditor/session/service/SessionService.java"
      to: "src/main/java/com/collabeditor/session/persistence/CodingSessionRepository.java"
      via: "session lookup and state updates"
      pattern: "codingSessionRepository\\."
    - from: "src/main/java/com/collabeditor/session/service/SessionCleanupScheduler.java"
      to: "src/main/java/com/collabeditor/session/persistence/CodingSessionRepository.java"
      via: "scheduled expiration cleanup"
      pattern: "cleanupAfter|delete"
---

<objective>
Implement session lifecycle APIs for create, list, join-by-invite-code, and leave-with-owner-transfer flows.

Purpose: Complete the private-room session contract that Phase 2 collaboration and later persistence work will build on.
Output: Session entities, repositories, controllers, lifecycle services, cleanup scheduler, and automated session tests.
</objective>

<execution_context>
@/Users/abdullah/.codex/get-shit-done/workflows/execute-plan.md
@/Users/abdullah/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-secure-access-and-session-lifecycle/01-CONTEXT.md
@.planning/phases/01-secure-access-and-session-lifecycle/01-RESEARCH.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement session create/list contracts with invite-code model</name>
  <read_first>
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/phases/01-secure-access-and-session-lifecycle/01-CONTEXT.md
    src/main/resources/db/migration/V1__phase1_baseline.sql
    src/main/java/com/collabeditor/auth/security/SecurityConfig.java
    src/main/java/com/collabeditor/session/api/SessionController.java
    src/main/java/com/collabeditor/session/service/SessionService.java
  </read_first>
  <files>src/main/java/com/collabeditor/session/api/SessionController.java, src/main/java/com/collabeditor/session/api/dto/CreateSessionRequest.java, src/main/java/com/collabeditor/session/api/dto/SessionResponse.java, src/main/java/com/collabeditor/session/api/dto/SessionSummaryResponse.java, src/main/java/com/collabeditor/session/service/SessionService.java, src/main/java/com/collabeditor/session/persistence/CodingSessionRepository.java, src/main/java/com/collabeditor/session/persistence/SessionParticipantRepository.java, src/main/java/com/collabeditor/session/persistence/entity/CodingSessionEntity.java, src/main/java/com/collabeditor/session/persistence/entity/SessionParticipantEntity.java, src/main/resources/application.yml</files>
  <behavior>
    - Test 1: `POST /api/sessions` with `language=JAVA` returns `201` with both `sessionId` (UUID) and `inviteCode` (SESS-01, D-06).
    - Test 2: Created session stores owner as creator and creates owner participant membership (D-08).
    - Test 3: `GET /api/sessions` returns only sessions owned or joined by the current user (SESS-02, D-05).
    - Test 4: Session language is immutable after create (D-07).
  </behavior>
  <action>
    Implement `POST /api/sessions` and `GET /api/sessions` behind the Phase 1 bearer auth filter. For creation, accept only enum values `JAVA` or `PYTHON`, generate invite codes with exact regex `[A-Z2-9]{8}` using `SecureRandom`, persist the canonical session UUID plus the invite code, and immediately create an owner membership row for the creator. Add `app.session.participant-cap: 12` to `application.yml` and use that exact value in service logic and response payloads. `GET /api/sessions` must return only sessions where the caller is `owner_user_id` or has an `ACTIVE` membership row; never expose a public global directory. Keep language immutable by not implementing any update-language API or repository mutation path after creation.
  </action>
  <acceptance_criteria>
    - `rg -n "SecureRandom|\\[A-Z2-9\\]\\{8\\}|inviteCode" src/main/java/com/collabeditor/session/service/SessionService.java` returns invite-code generation logic.
    - `rg -n "participant-cap: 12|participantCap|OWNER|ownerUserId" src/main/resources/application.yml src/main/java/com/collabeditor/session/service/SessionService.java src/main/java/com/collabeditor/session/persistence/entity/SessionParticipantEntity.java` returns cap and owner assignment markers.
    - `rg -n "find.*owner|ACTIVE|listSessions" src/main/java/com/collabeditor/session/service/SessionService.java src/main/java/com/collabeditor/session/persistence/CodingSessionRepository.java` returns restricted listing logic.
    - `./gradlew test --tests "*SessionServiceTest.shouldCreateSessionAndListOnlyOwnedOrJoined*"` exits with status 0.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "*SessionServiceTest.shouldCreateSessionAndListOnlyOwnedOrJoined*"</automated>
  </verify>
  <done>Create and list behaviors satisfy the private-session visibility model, immutable language choice, and invite-code generation rules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement join and leave lifecycle policies</name>
  <read_first>
    .planning/phases/01-secure-access-and-session-lifecycle/01-CONTEXT.md
    src/main/java/com/collabeditor/session/service/SessionService.java
    src/main/java/com/collabeditor/session/persistence/CodingSessionRepository.java
    src/main/java/com/collabeditor/session/persistence/SessionParticipantRepository.java
    src/test/java/com/collabeditor/session/SessionServiceTest.java
  </read_first>
  <files>src/main/java/com/collabeditor/session/api/dto/JoinSessionRequest.java, src/main/java/com/collabeditor/session/service/SessionService.java, src/main/java/com/collabeditor/session/persistence/CodingSessionRepository.java, src/main/java/com/collabeditor/session/persistence/SessionParticipantRepository.java, src/test/java/com/collabeditor/session/SessionServiceTest.java, src/test/java/com/collabeditor/session/SessionControllerTest.java</files>
  <behavior>
    - Test 1: `POST /api/sessions/join` with body `{ "inviteCode": "ABC234JK" }` joins by invite code (not UUID) and returns `200` (SESS-03, D-06).
    - Test 2: Join fails with `409` when active participant count reaches cap `12` (D-11).
    - Test 3: Leaving as owner with remaining participants transfers ownership to earliest active joiner (SESS-04, D-09).
    - Test 4: Leaving as last participant marks session empty and sets cleanup-after to one hour later instead of immediate delete (SESS-04, D-10).
  </behavior>
  <action>
    Implement `POST /api/sessions/join` with a request body `{ inviteCode }` and `POST /api/sessions/{sessionId}/leave` for exit. Enforce the participant cap at exactly `12` active members. Make join idempotent for an already-active participant by returning the existing session response instead of inserting duplicate active rows. When the owner leaves and active participants remain, transfer ownership to the participant with the earliest `joined_at`; if two rows share the same timestamp, use the lexicographically smallest `user_id` as the tie-breaker. When the last active participant leaves, keep the session row, set `empty_since = now`, set `cleanup_after = now + 1 hour`, and mark the membership row `LEFT`. If anyone rejoins before cleanup, clear `empty_since` and `cleanup_after`.
  </action>
  <acceptance_criteria>
    - `rg -n "joinSession|inviteCode" src/main/java/com/collabeditor/session/service/SessionService.java src/main/java/com/collabeditor/session/api/dto/JoinSessionRequest.java` returns invite-code join logic.
    - `rg -n "12|participantCap|ACTIVE" src/main/java/com/collabeditor/session/service/SessionService.java` returns cap enforcement.
    - `rg -n "joinedAt|ownerUserId|lexicographically|transfer" src/main/java/com/collabeditor/session/service/SessionService.java` returns deterministic owner-transfer code or comments.
    - `rg -n "emptySince|cleanupAfter|plusHours\\(1\\)" src/main/java/com/collabeditor/session/service/SessionService.java` returns one-hour retention logic.
    - `./gradlew test --tests "*SessionServiceTest.shouldJoinAndLeaveWithOwnerTransferAndOneHourRetention*"` exits with status 0.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "*SessionServiceTest.shouldJoinAndLeaveWithOwnerTransferAndOneHourRetention*"</automated>
  </verify>
  <done>Join and leave flows satisfy invite-code access, participant limits, ownership transfer, and one-hour empty-room retention.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add cleanup scheduler and full session lifecycle test coverage</name>
  <read_first>
    src/main/java/com/collabeditor/CollaborativeCodeEditorApplication.java
    src/main/java/com/collabeditor/session/service/SessionService.java
    src/main/java/com/collabeditor/session/service/SessionCleanupScheduler.java
    src/test/java/com/collabeditor/session/SessionServiceTest.java
    src/test/java/com/collabeditor/session/SessionControllerTest.java
    src/test/java/com/collabeditor/session/SessionCleanupSchedulerTest.java
  </read_first>
  <files>src/main/java/com/collabeditor/CollaborativeCodeEditorApplication.java, src/main/java/com/collabeditor/session/service/SessionCleanupScheduler.java, src/test/java/com/collabeditor/session/SessionCleanupSchedulerTest.java, src/test/java/com/collabeditor/session/SessionServiceTest.java, src/test/java/com/collabeditor/session/SessionControllerTest.java</files>
  <behavior>
    - Test 1: Scheduled cleanup removes sessions with `cleanup_after <= now` and zero active participants.
    - Test 2: Scheduled cleanup does not remove sessions that still have active participants.
    - Test 3: End-to-end controller tests cover create, list, join, leave, cap rejection, and owner transfer.
  </behavior>
  <action>
    Enable scheduling on `CollaborativeCodeEditorApplication` and implement `SessionCleanupScheduler` with `@Scheduled(fixedDelayString = "${app.session.cleanup-fixed-delay:PT5M}")`. The scheduler must query only sessions with `cleanup_after <= now`, confirm their active participant count is zero, and then delete them. Expand `SessionServiceTest` and `SessionControllerTest` so the test suite covers create, list, join, leave, cap rejection, owner transfer, rejoin-before-cleanup, and leave-as-last-participant. Add `SessionCleanupSchedulerTest` for expired-empty deletion and non-deletion of rooms that are still active.
  </action>
  <acceptance_criteria>
    - `rg -n "@EnableScheduling" src/main/java/com/collabeditor/CollaborativeCodeEditorApplication.java` returns the scheduling enablement marker.
    - `rg -n "cleanup-fixed-delay: PT5M|@Scheduled\\(fixedDelayString = \"\\$\\{app\\.session\\.cleanup-fixed-delay:PT5M\\}\"\\)|cleanupAfter" src/main/resources/application.yml src/main/java/com/collabeditor/session/service/SessionCleanupScheduler.java` returns scheduler cadence and cleanup condition.
    - `rg -n "shouldDeleteExpiredEmptySession|shouldNotDeleteSessionWithActiveParticipants" src/test/java/com/collabeditor/session/SessionCleanupSchedulerTest.java` returns cleanup test cases.
    - `rg -n "shouldCreateSessionAndListOnlyOwnedOrJoined|shouldJoinAndLeaveWithOwnerTransferAndOneHourRetention|shouldRejectJoinWhenRoomIsFull|shouldClearCleanupWindowWhenParticipantRejoins" src/test/java/com/collabeditor/session/SessionServiceTest.java src/test/java/com/collabeditor/session/SessionControllerTest.java` returns lifecycle coverage markers.
    - `./gradlew test --tests "*SessionServiceTest" --tests "*SessionControllerTest" --tests "*SessionCleanupSchedulerTest"` exits with status 0.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "*SessionServiceTest" --tests "*SessionControllerTest" --tests "*SessionCleanupSchedulerTest"</automated>
  </verify>
  <done>Session lifecycle and cleanup behavior are fully automated and verifiably satisfy the Phase 1 session requirements.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `./gradlew test --tests "*SessionServiceTest.shouldCreateSessionAndListOnlyOwnedOrJoined*"`
- [ ] `./gradlew test --tests "*SessionServiceTest.shouldJoinAndLeaveWithOwnerTransferAndOneHourRetention*"`
- [ ] `./gradlew test --tests "*SessionServiceTest" --tests "*SessionControllerTest" --tests "*SessionCleanupSchedulerTest"`
</verification>

<success_criteria>
- Authenticated users can create, list, join, and leave sessions through REST APIs.
- Invite-code joins, participant-cap enforcement, and owner transfer are deterministic.
- Empty sessions remain recoverable for one hour and are cleaned on the configured schedule afterward.
</success_criteria>

<output>
After completion, create `.planning/phases/01-secure-access-and-session-lifecycle/01-03-SUMMARY.md`
</output>
