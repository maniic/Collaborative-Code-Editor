---
phase: 01.1-fix-phase-1-auth-session-and-verification-gaps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - build.gradle.kts
  - src/main/java/com/collabeditor/session/persistence/entity/CodingSessionEntity.java
  - src/main/resources/application.yml
  - src/test/java/com/collabeditor/migration/FlywayMigrationTest.java
  - src/test/resources/application-test.yml
autonomous: true
requirements:
  - QUAL-01
must_haves:
  truths:
    - "Hibernate validation and Flyway agree on the Phase 1 schema, including the `coding_sessions.participant_cap` column type."
    - "The repository exposes one repeatable verification path for auth and session work on a supported JDK and Docker-backed Testcontainers setup."
    - "Phase 1 integration verification fails only on real regressions, not on mismatched local assumptions about schema or test configuration."
  artifacts:
    - path: "src/main/java/com/collabeditor/session/persistence/entity/CodingSessionEntity.java"
      provides: "JPA mapping whose `participant_cap` type matches the migrated PostgreSQL schema."
    - path: "src/test/java/com/collabeditor/migration/FlywayMigrationTest.java"
      provides: "PostgreSQL-backed verification that the baseline migration and Hibernate validation boot cleanly together."
    - path: "src/test/resources/application-test.yml"
      provides: "Stable test-profile settings for real migration and Phase 1 verification runs."
  key_links:
    - from: "src/main/resources/application.yml"
      to: "src/test/resources/application-test.yml"
      via: "shared JPA/Flyway validation settings across app and test profiles"
      pattern: "ddl-auto: validate|flyway:"
    - from: "src/main/java/com/collabeditor/session/persistence/entity/CodingSessionEntity.java"
      to: "src/main/resources/db/migration/V1__phase1_baseline.sql"
      via: "matched `participant_cap` persistence type"
      pattern: "participant_cap"
    - from: "src/test/java/com/collabeditor/migration/FlywayMigrationTest.java"
      to: "src/main/resources/db/migration/V1__phase1_baseline.sql"
      via: "Spring Boot + PostgreSQL migration verification"
      pattern: "coding_sessions|participant_cap"
---

<objective>
Repair the Phase 1 verification baseline so schema validation, migration tests, and full-suite runs are trustworthy again.

Purpose: Every auth and session fix in this remediation phase needs a real verification path that reflects PostgreSQL, Hibernate validation, and the supported local runtime assumptions.
Output: Schema/entity parity for `participant_cap`, stable test-profile configuration, and a PostgreSQL-backed migration test that proves the application boots cleanly against the migrated schema.
</objective>

<execution_context>
@/Users/abdullah/.codex/get-shit-done/workflows/execute-plan.md
@/Users/abdullah/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01.1-fix-phase-1-auth-session-and-verification-gaps/01.1-RESEARCH.md
@.planning/phases/01.1-fix-phase-1-auth-session-and-verification-gaps/01.1-VALIDATION.md
@.planning/phases/01-secure-access-and-session-lifecycle/01-VERIFICATION.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Restore Flyway and JPA schema parity for coding sessions</name>
  <read_first>
    .planning/ROADMAP.md
    .planning/phases/01.1-fix-phase-1-auth-session-and-verification-gaps/01.1-RESEARCH.md
    src/main/resources/db/migration/V1__phase1_baseline.sql
    src/main/java/com/collabeditor/session/persistence/entity/CodingSessionEntity.java
    src/test/java/com/collabeditor/migration/FlywayMigrationTest.java
  </read_first>
  <files>src/main/java/com/collabeditor/session/persistence/entity/CodingSessionEntity.java, src/test/java/com/collabeditor/migration/FlywayMigrationTest.java</files>
  <action>
    Update `CodingSessionEntity` so the `participantCap` field maps to PostgreSQL `SMALLINT` instead of `INTEGER`. Use a Java `short` or `Short` field and preserve the existing getter contract by converting to `int` only at the API boundary if needed; do not widen the SQL migration. Expand `FlywayMigrationTest` so it proves the Spring Boot context starts with `spring.jpa.hibernate.ddl-auto=validate` against the migrated PostgreSQL container and explicitly asserts that `coding_sessions.participant_cap` reports the `int2` / `smallint` database type. Keep the baseline migration file as the source of truth and make the entity conform to it.
  </action>
  <acceptance_criteria>
    - `rg -n "participantCap" src/main/java/com/collabeditor/session/persistence/entity/CodingSessionEntity.java` shows the field now uses a smallint-compatible Java type rather than `int`.
    - `rg -n "participant_cap|int2|smallint|TYPE_NAME" src/test/java/com/collabeditor/migration/FlywayMigrationTest.java` returns explicit column-type assertions for `participant_cap`.
    - `JAVA_HOME=<supported-jdk> DOCKER_HOST=<docker-socket> ./gradlew test --tests "*FlywayMigrationTest"` exits with status 0.
  </acceptance_criteria>
  <verify>
    <automated>JAVA_HOME=&lt;supported-jdk&gt; DOCKER_HOST=&lt;docker-socket&gt; ./gradlew test --tests "*FlywayMigrationTest"</automated>
  </verify>
  <done>PostgreSQL migrations and Hibernate validation agree on the Phase 1 schema, so later fixes are checked against the real persistence contract instead of a false-positive verification report.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stabilize the supported verification profile for local and CI-like runs</name>
  <read_first>
    build.gradle.kts
    src/main/resources/application.yml
    src/test/resources/application-test.yml
    src/test/java/com/collabeditor/migration/FlywayMigrationTest.java
    .planning/phases/01.1-fix-phase-1-auth-session-and-verification-gaps/01.1-VALIDATION.md
  </read_first>
  <files>build.gradle.kts, src/main/resources/application.yml, src/test/resources/application-test.yml, src/test/java/com/collabeditor/migration/FlywayMigrationTest.java</files>
  <action>
    Keep Java 17 as the supported Gradle toolchain runtime for verification and make the test profile explicit instead of implicit. In `application-test.yml`, keep `spring.jpa.hibernate.ddl-auto: validate`, `spring.flyway.enabled: true`, and `spring.flyway.locations: classpath:db/migration` so every integration run exercises the real migration path. In `build.gradle.kts`, preserve the existing Docker host forwarding and add any missing test task wiring needed for Testcontainers-backed runs to behave consistently when `DOCKER_HOST` is exported. In `FlywayMigrationTest`, fail fast with clear assertions when the container-backed datasource or validation path is not active, so future verification cannot overstate green status without actually booting the application against PostgreSQL.
  </action>
  <acceptance_criteria>
    - `rg -n "JavaLanguageVersion\\.of\\(17\\)" build.gradle.kts` returns the supported toolchain marker.
    - `rg -n "ddl-auto: validate|flyway:|locations: classpath:db/migration|enabled: true" src/test/resources/application-test.yml` returns the explicit integration-test profile settings.
    - `rg -n "DOCKER_HOST|TESTCONTAINERS_DOCKER_SOCKET_OVERRIDE|DOCKER_API_VERSION" build.gradle.kts` returns the Testcontainers environment wiring.
    - `JAVA_HOME=<supported-jdk> ./gradlew test --tests "com.collabeditor.auth.*" --tests "com.collabeditor.session.*"` exits with status 0.
  </acceptance_criteria>
  <verify>
    <automated>JAVA_HOME=&lt;supported-jdk&gt; ./gradlew test --tests "com.collabeditor.auth.*" --tests "com.collabeditor.session.*"</automated>
  </verify>
  <done>The remediation phase has an honest, repeatable verification profile for both focused auth/session tests and the migration-backed integration check.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `JAVA_HOME=<supported-jdk> DOCKER_HOST=<docker-socket> ./gradlew test --tests "*FlywayMigrationTest"`
- [ ] `JAVA_HOME=<supported-jdk> ./gradlew test --tests "com.collabeditor.auth.*" --tests "com.collabeditor.session.*"`
</verification>

<success_criteria>
- The `participant_cap` entity mapping matches the migrated PostgreSQL schema.
- PostgreSQL-backed migration verification boots cleanly with Hibernate validation enabled.
- The supported local verification commands for auth and session work are explicit and green.
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-fix-phase-1-auth-session-and-verification-gaps/01.1-01-SUMMARY.md`
</output>
