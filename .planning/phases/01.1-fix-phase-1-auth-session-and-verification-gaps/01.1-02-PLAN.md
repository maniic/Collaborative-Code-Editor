---
phase: 01.1-fix-phase-1-auth-session-and-verification-gaps
plan: 02
type: execute
wave: 2
depends_on:
  - 01.1-01
files_modified:
  - src/main/java/com/collabeditor/auth/persistence/RefreshSessionRepository.java
  - src/main/java/com/collabeditor/auth/security/JwtAuthenticationFilter.java
  - src/main/java/com/collabeditor/auth/security/SecurityConfig.java
  - src/main/java/com/collabeditor/auth/security/SecurityProperties.java
  - src/main/java/com/collabeditor/auth/service/JwtTokenService.java
  - src/main/java/com/collabeditor/auth/service/RefreshSessionService.java
  - src/test/java/com/collabeditor/auth/AuthControllerTest.java
  - src/test/java/com/collabeditor/auth/JwtTokenServiceTest.java
  - src/test/java/com/collabeditor/auth/RefreshSessionServiceTest.java
  - src/test/java/com/collabeditor/auth/common/TestSecurityConfig.java
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
must_haves:
  truths:
    - "JWT validation fails closed when the signing secret is insecure, the issuer is wrong, or the JWT subject is not a UUID."
    - "A refresh token can be redeemed successfully at most once, even under concurrent requests from the same device."
    - "Automated tests exercise the real security filter behavior instead of bypassing auth with a blanket `permitAll()` test chain."
  artifacts:
    - path: "src/main/java/com/collabeditor/auth/service/JwtTokenService.java"
      provides: "Fail-fast JWT issuance and parsing with strict issuer and subject validation."
    - path: "src/main/java/com/collabeditor/auth/service/RefreshSessionService.java"
      provides: "Single-use refresh-token rotation with explicit reuse detection under concurrent access."
    - path: "src/test/java/com/collabeditor/auth/AuthControllerTest.java"
      provides: "Auth contract coverage that proves cookie behavior and protected-route enforcement with the real filter chain."
  key_links:
    - from: "src/main/java/com/collabeditor/auth/security/JwtAuthenticationFilter.java"
      to: "src/main/java/com/collabeditor/auth/service/JwtTokenService.java"
      via: "strict token parsing before security-context population"
      pattern: "parseToken|subject|issuer"
    - from: "src/main/java/com/collabeditor/auth/service/RefreshSessionService.java"
      to: "src/main/java/com/collabeditor/auth/persistence/RefreshSessionRepository.java"
      via: "locked or compare-and-swap refresh rotation"
      pattern: "findByTokenHash|revokedAt|replacedBySessionId"
    - from: "src/test/java/com/collabeditor/auth/AuthControllerTest.java"
      to: "src/test/java/com/collabeditor/auth/common/TestSecurityConfig.java"
      via: "controller tests must no longer globally bypass security"
      pattern: "permitAll|SecurityFilterChain"
---

<objective>
Harden the Phase 1 auth implementation so JWTs fail closed, refresh rotation is truly single-use, and security tests verify the real filter chain.

Purpose: Phase 2 cannot build on an auth layer that accepts a fallback signing secret, can mint multiple successor refresh tokens from one credential, or claims security behavior that is never actually tested.
Output: Strict JWT parsing, atomic refresh rotation, and auth/security tests that validate the production filter behavior.
</objective>

<execution_context>
@/Users/abdullah/.codex/get-shit-done/workflows/execute-plan.md
@/Users/abdullah/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01.1-fix-phase-1-auth-session-and-verification-gaps/01.1-RESEARCH.md
@.planning/phases/01.1-fix-phase-1-auth-session-and-verification-gaps/01.1-VALIDATION.md
@.planning/phases/01-secure-access-and-session-lifecycle/01-VERIFICATION.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove insecure JWT fallbacks and parse tokens strictly</name>
  <read_first>
    src/main/java/com/collabeditor/auth/security/SecurityProperties.java
    src/main/java/com/collabeditor/auth/service/JwtTokenService.java
    src/main/java/com/collabeditor/auth/security/JwtAuthenticationFilter.java
    src/main/java/com/collabeditor/auth/security/SecurityConfig.java
    src/test/java/com/collabeditor/auth/JwtTokenServiceTest.java
  </read_first>
  <files>src/main/java/com/collabeditor/auth/security/SecurityProperties.java, src/main/java/com/collabeditor/auth/service/JwtTokenService.java, src/main/java/com/collabeditor/auth/security/JwtAuthenticationFilter.java, src/main/java/com/collabeditor/auth/security/SecurityConfig.java, src/test/java/com/collabeditor/auth/JwtTokenServiceTest.java</files>
  <action>
    Remove the `@Value("${APP_JWT_SECRET:default-dev-secret-that-is-at-least-32-bytes-long}")` fallback from `JwtTokenService`. Move the JWT secret and issuer into `SecurityProperties` as explicit configuration inputs, require the issuer value `collaborative-code-editor`, and fail application startup if the secret is blank or shorter than 32 bytes. In `parseToken`, verify the expected issuer and return a safe empty result when parsing fails; do not let malformed claims bubble into `JwtAuthenticationFilter`. In `JwtAuthenticationFilter`, treat a missing or invalid UUID `sub` claim as an authentication failure instead of calling `UUID.fromString(...)` blindly. Add `JwtTokenServiceTest` coverage for wrong issuer, malformed subject, expired token, and missing/short secret configuration.
  </action>
  <acceptance_criteria>
    - `rg -n "default-dev-secret-that-is-at-least-32-bytes-long" src/main/java/com/collabeditor/auth/service/JwtTokenService.java` returns no matches.
    - `rg -n "issuer|jwt|secret" src/main/java/com/collabeditor/auth/security/SecurityProperties.java src/main/java/com/collabeditor/auth/service/JwtTokenService.java` returns explicit issuer and secret-property handling.
    - `rg -n "UUID\\.fromString|Optional|Claims" src/main/java/com/collabeditor/auth/security/JwtAuthenticationFilter.java` shows invalid subjects are guarded before authentication is created.
    - `rg -n "wrong issuer|malformed subject|expired token|short secret" src/test/java/com/collabeditor/auth/JwtTokenServiceTest.java` returns named strict-validation scenarios.
    - `JAVA_HOME=<supported-jdk> ./gradlew test --tests "*JwtTokenServiceTest"` exits with status 0.
  </acceptance_criteria>
  <verify>
    <automated>JAVA_HOME=&lt;supported-jdk&gt; ./gradlew test --tests "*JwtTokenServiceTest"</automated>
  </verify>
  <done>Access JWTs are accepted only when they were minted by this application and carry a valid UUID subject, closing the forgery and malformed-claim gaps.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make refresh-token rotation single-use under concurrent requests</name>
  <read_first>
    src/main/java/com/collabeditor/auth/persistence/RefreshSessionRepository.java
    src/main/java/com/collabeditor/auth/service/RefreshSessionService.java
    src/test/java/com/collabeditor/auth/RefreshSessionServiceTest.java
    src/main/resources/db/migration/V1__phase1_baseline.sql
  </read_first>
  <files>src/main/java/com/collabeditor/auth/persistence/RefreshSessionRepository.java, src/main/java/com/collabeditor/auth/service/RefreshSessionService.java, src/test/java/com/collabeditor/auth/RefreshSessionServiceTest.java</files>
  <action>
    Replace the current read-check-write refresh flow with an atomic rotation path. Use either a pessimistic lock on `findByTokenHash` or an update-first compare-and-swap repository method so only one transaction can mark a non-revoked session as consumed. Preserve the existing behavior that the replacement row keeps the same `deviceId` and that reuse detection revokes the active chain for that device. Update `RefreshSessionServiceTest` so it includes a concurrency scenario where two refresh attempts race on the same raw token and exactly one succeeds while the other is rejected as invalid or reused.
  </action>
  <acceptance_criteria>
    - `rg -n "Lock|PESSIMISTIC|@Modifying|revokedAt IS NULL" src/main/java/com/collabeditor/auth/persistence/RefreshSessionRepository.java` returns the chosen atomic-rotation guard.
    - `rg -n "deviceId|replacedBySessionId|revokedAt" src/main/java/com/collabeditor/auth/service/RefreshSessionService.java` returns the single-use rotation chain updates.
    - `rg -n "concurrent|race|single-use|reuse" src/test/java/com/collabeditor/auth/RefreshSessionServiceTest.java` returns a named concurrent-rotation regression test.
    - `JAVA_HOME=<supported-jdk> ./gradlew test --tests "*RefreshSessionServiceTest"` exits with status 0.
  </acceptance_criteria>
  <verify>
    <automated>JAVA_HOME=&lt;supported-jdk&gt; ./gradlew test --tests "*RefreshSessionServiceTest"</automated>
  </verify>
  <done>A refresh token cannot mint multiple successor sessions under load, so the rotation guarantee is real rather than best-effort.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Replace security-test bypasses with real filter-chain assertions</name>
  <read_first>
    src/test/java/com/collabeditor/auth/common/TestSecurityConfig.java
    src/test/java/com/collabeditor/auth/AuthControllerTest.java
    src/main/java/com/collabeditor/auth/security/SecurityConfig.java
    src/main/java/com/collabeditor/auth/security/JwtAuthenticationFilter.java
  </read_first>
  <files>src/test/java/com/collabeditor/auth/common/TestSecurityConfig.java, src/test/java/com/collabeditor/auth/AuthControllerTest.java, src/main/java/com/collabeditor/auth/security/SecurityConfig.java, src/main/java/com/collabeditor/auth/security/JwtAuthenticationFilter.java</files>
  <action>
    Stop using a blanket `permitAll()` test filter chain for auth controller coverage. Either delete `TestSecurityConfig` entirely or narrow it so it does not override the production `SecurityConfig` for protected-route assertions. Update `AuthControllerTest` to prove three concrete behaviors: unauthenticated access to a protected endpoint returns `401`, a request with an invalid bearer token is rejected, and a request with a valid app-issued bearer token is authenticated successfully. Keep the existing login/refresh cookie assertions and run them through the production filter chain instead of a test-only bypass.
  </action>
  <acceptance_criteria>
    - `rg -n "permitAll\\(\\)" src/test/java/com/collabeditor/auth/common/TestSecurityConfig.java` returns no blanket `anyRequest().permitAll()` override.
    - `rg -n "401|invalid bearer|valid bearer|Authorization" src/test/java/com/collabeditor/auth/AuthControllerTest.java` returns explicit protected-route security assertions.
    - `rg -n "anyRequest\\(\\)\\.authenticated\\(\\)|addFilterBefore" src/main/java/com/collabeditor/auth/security/SecurityConfig.java` confirms the production filter chain remains active in tests.
    - `JAVA_HOME=<supported-jdk> ./gradlew test --tests "*AuthControllerTest"` exits with status 0.
  </acceptance_criteria>
  <verify>
    <automated>JAVA_HOME=&lt;supported-jdk&gt; ./gradlew test --tests "*AuthControllerTest"</automated>
  </verify>
  <done>The auth layer is now verified through the same security behavior the application ships, closing the false-confidence testing gap.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `JAVA_HOME=<supported-jdk> ./gradlew test --tests "*JwtTokenServiceTest"`
- [ ] `JAVA_HOME=<supported-jdk> ./gradlew test --tests "*RefreshSessionServiceTest"`
- [ ] `JAVA_HOME=<supported-jdk> ./gradlew test --tests "*AuthControllerTest"`
</verification>

<success_criteria>
- JWTs fail closed on insecure configuration, wrong issuer, and malformed `sub` claims.
- Refresh rotation is single-use even when two requests race on the same token.
- Auth tests exercise the real production filter behavior instead of a `permitAll()` shortcut.
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-fix-phase-1-auth-session-and-verification-gaps/01.1-02-SUMMARY.md`
</output>
