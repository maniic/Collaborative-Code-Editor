---
phase: 01.1-fix-phase-1-auth-session-and-verification-gaps
plan: 03
type: execute
wave: 3
depends_on:
  - 01.1-01
  - 01.1-02
files_modified:
  - src/main/java/com/collabeditor/session/api/dto/JoinSessionRequest.java
  - src/main/java/com/collabeditor/session/persistence/CodingSessionRepository.java
  - src/main/java/com/collabeditor/session/persistence/SessionParticipantRepository.java
  - src/main/java/com/collabeditor/session/persistence/entity/SessionParticipantEntity.java
  - src/main/java/com/collabeditor/session/service/SessionCleanupScheduler.java
  - src/main/java/com/collabeditor/session/service/SessionService.java
  - src/test/java/com/collabeditor/session/SessionCleanupSchedulerTest.java
  - src/test/java/com/collabeditor/session/SessionControllerTest.java
  - src/test/java/com/collabeditor/session/SessionServiceTest.java
autonomous: true
requirements:
  - SESS-01
  - SESS-03
  - SESS-04
must_haves:
  truths:
    - "A session has exactly one coherent owner after leave, transfer, and rejoin flows."
    - "Join, leave, and cleanup behavior preserve participant-cap and one-hour retention guarantees even when requests race."
    - "Session create and join reject malformed invite codes and tolerate invite-code collisions deterministically."
  artifacts:
    - path: "src/main/java/com/collabeditor/session/service/SessionService.java"
      provides: "Serialized or compare-and-swap session lifecycle rules for create, join, rejoin, leave, and owner transfer."
    - path: "src/main/java/com/collabeditor/session/service/SessionCleanupScheduler.java"
      provides: "Cleanup logic that deletes only sessions still empty at delete time."
    - path: "src/test/java/com/collabeditor/session/SessionServiceTest.java"
      provides: "Lifecycle and concurrency regression coverage for invite collisions, owner transfer, cap enforcement, and rejoin behavior."
  key_links:
    - from: "src/main/java/com/collabeditor/session/service/SessionService.java"
      to: "src/main/java/com/collabeditor/session/persistence/CodingSessionRepository.java"
      via: "locked or guarded session mutations during create/join/leave"
      pattern: "findByInviteCode|findById|save"
    - from: "src/main/java/com/collabeditor/session/service/SessionService.java"
      to: "src/main/java/com/collabeditor/session/persistence/SessionParticipantRepository.java"
      via: "active participant counts, ownership transfer ordering, and membership reactivation"
      pattern: "countBySessionIdAndStatus|findActiveBySessionIdOrdered|findBySessionIdAndUserId"
    - from: "src/main/java/com/collabeditor/session/service/SessionCleanupScheduler.java"
      to: "src/main/java/com/collabeditor/session/persistence/CodingSessionRepository.java"
      via: "expired-empty lookup plus delete guard"
      pattern: "findExpiredEmptySessions|delete"
---

<objective>
Repair the Phase 1 session lifecycle so invite codes, owner transfer, participant caps, and cleanup windows remain deterministic under concurrent mutations.

Purpose: Real-time collaboration in Phase 2 depends on session membership invariants that do not collapse when owners leave, participants rejoin, or cleanup races with activity.
Output: Concurrency-safe session lifecycle code and regression tests for malformed invites, invite collisions, ownership transfer, capacity enforcement, and cleanup races.
</objective>

<execution_context>
@/Users/abdullah/.codex/get-shit-done/workflows/execute-plan.md
@/Users/abdullah/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01.1-fix-phase-1-auth-session-and-verification-gaps/01.1-RESEARCH.md
@.planning/phases/01.1-fix-phase-1-auth-session-and-verification-gaps/01.1-VALIDATION.md
@.planning/phases/01-secure-access-and-session-lifecycle/01-CONTEXT.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make session creation and join deterministic around invite codes</name>
  <read_first>
    src/main/java/com/collabeditor/session/service/SessionService.java
    src/main/java/com/collabeditor/session/api/dto/JoinSessionRequest.java
    src/main/java/com/collabeditor/session/persistence/CodingSessionRepository.java
    src/test/java/com/collabeditor/session/SessionServiceTest.java
    src/test/java/com/collabeditor/session/SessionControllerTest.java
  </read_first>
  <files>src/main/java/com/collabeditor/session/service/SessionService.java, src/main/java/com/collabeditor/session/api/dto/JoinSessionRequest.java, src/main/java/com/collabeditor/session/persistence/CodingSessionRepository.java, src/test/java/com/collabeditor/session/SessionServiceTest.java, src/test/java/com/collabeditor/session/SessionControllerTest.java</files>
  <action>
    Harden invite-code handling in `SessionService`. Validate join requests against the exact format `[A-Z2-9]{8}` before hitting the repository, normalize invite codes to uppercase, and return a deterministic validation error for malformed codes instead of looking them up blindly. In session creation, generate a new invite code and retry when the candidate collides with an existing `coding_sessions.invite_code`; keep retry logic bounded and explicit. Add repository support if needed to check invite-code existence without leaking full session data. Add regression tests for malformed invite codes, uppercase normalization, and an invite-code collision path that retries successfully instead of failing the create request.
  </action>
  <acceptance_criteria>
    - `rg -n "\\[A-Z2-9\\]\\{8\\}|toUpperCase|invite code" src/main/java/com/collabeditor/session/service/SessionService.java src/main/java/com/collabeditor/session/api/dto/JoinSessionRequest.java` returns explicit format validation and normalization logic.
    - `rg -n "existsByInviteCode|findByInviteCode" src/main/java/com/collabeditor/session/persistence/CodingSessionRepository.java` returns repository support for deterministic collision handling.
    - `rg -n "malformed invite|collision|retry|uppercase" src/test/java/com/collabeditor/session/SessionServiceTest.java src/test/java/com/collabeditor/session/SessionControllerTest.java` returns named invite-code regression tests.
    - `JAVA_HOME=<supported-jdk> ./gradlew test --tests "*SessionServiceTest" --tests "*SessionControllerTest"` exits with status 0.
  </acceptance_criteria>
  <verify>
    <automated>JAVA_HOME=&lt;supported-jdk&gt; ./gradlew test --tests "*SessionServiceTest" --tests "*SessionControllerTest"</automated>
  </verify>
  <done>Session creation and join behave predictably even when invite codes are malformed or collide, closing the brittle invite-path gaps.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Serialize join, leave, and owner-transfer mutations per session</name>
  <read_first>
    src/main/java/com/collabeditor/session/service/SessionService.java
    src/main/java/com/collabeditor/session/persistence/CodingSessionRepository.java
    src/main/java/com/collabeditor/session/persistence/SessionParticipantRepository.java
    src/main/java/com/collabeditor/session/persistence/entity/SessionParticipantEntity.java
    src/test/java/com/collabeditor/session/SessionServiceTest.java
  </read_first>
  <files>src/main/java/com/collabeditor/session/service/SessionService.java, src/main/java/com/collabeditor/session/persistence/CodingSessionRepository.java, src/main/java/com/collabeditor/session/persistence/SessionParticipantRepository.java, src/main/java/com/collabeditor/session/persistence/entity/SessionParticipantEntity.java, src/test/java/com/collabeditor/session/SessionServiceTest.java</files>
  <action>
    Make session membership mutations atomic at the session level. Use locking or guarded update queries so `joinSession` cannot observe a stale active-count and overfill the room, and so `leaveSession` cannot produce multiple active `OWNER` rows after ownership transfer and rejoin. When an owner leaves, downgrade the leaving membership from `OWNER` to `PARTICIPANT` or another non-owner role before transfer. When a previously-left owner rejoins, restore the row as `ACTIVE` but do not automatically resurrect owner privileges unless `owner_user_id` still points to that user. Prevent repeated `leaveSession` calls from extending the cleanup window; once a participant row is already `LEFT`, a second leave should be idempotent. Add tests that race two joins against a nearly-full room, verify only one owner remains after owner leave plus rejoin, and prove repeated leave calls do not push `cleanup_after` further into the future.
  </action>
  <acceptance_criteria>
    - `rg -n "Lock|PESSIMISTIC|@Version|for update|countBySessionIdAndStatus" src/main/java/com/collabeditor/session/persistence/CodingSessionRepository.java src/main/java/com/collabeditor/session/persistence/SessionParticipantRepository.java src/main/java/com/collabeditor/session/service/SessionService.java` returns the chosen per-session mutation guard.
    - `rg -n "setRole|OWNER|PARTICIPANT|LEFT|ACTIVE" src/main/java/com/collabeditor/session/service/SessionService.java src/main/java/com/collabeditor/session/persistence/entity/SessionParticipantEntity.java` returns explicit owner-downgrade and rejoin-role handling.
    - `rg -n "nearly full|rejoin|single owner|repeated leave|cleanup_after" src/test/java/com/collabeditor/session/SessionServiceTest.java` returns named lifecycle regression tests for the reviewed bugs.
    - `JAVA_HOME=<supported-jdk> ./gradlew test --tests "*SessionServiceTest"` exits with status 0.
  </acceptance_criteria>
  <verify>
    <automated>JAVA_HOME=&lt;supported-jdk&gt; ./gradlew test --tests "*SessionServiceTest"</automated>
  </verify>
  <done>Session membership invariants now hold under concurrent joins and owner exits, so the room state cannot drift into multiple owners or cap violations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Guard cleanup against rejoin races and close the session verification gaps</name>
  <read_first>
    src/main/java/com/collabeditor/session/service/SessionCleanupScheduler.java
    src/main/java/com/collabeditor/session/service/SessionService.java
    src/test/java/com/collabeditor/session/SessionCleanupSchedulerTest.java
    src/test/java/com/collabeditor/session/SessionControllerTest.java
    .planning/phases/01.1-fix-phase-1-auth-session-and-verification-gaps/01.1-VALIDATION.md
  </read_first>
  <files>src/main/java/com/collabeditor/session/service/SessionCleanupScheduler.java, src/main/java/com/collabeditor/session/service/SessionService.java, src/test/java/com/collabeditor/session/SessionCleanupSchedulerTest.java, src/test/java/com/collabeditor/session/SessionControllerTest.java</files>
  <action>
    Make cleanup deletion conditional on the session still being empty at delete time. Use either a locked session fetch, a guarded delete query, or an equivalent compare-and-swap path so a participant who rejoins after `findExpiredEmptySessions(now)` cannot lose the room to a stale cleanup worker. Keep the one-hour empty-room retention rule intact and ensure rejoin clears `empty_since` and `cleanup_after` exactly once. Expand `SessionCleanupSchedulerTest` to cover the reviewed time-of-check/time-of-delete race. Expand `SessionControllerTest` so the HTTP layer covers malformed invite rejection, full-room rejection, owner transfer, and rejoin-before-cleanup behavior.
  </action>
  <acceptance_criteria>
    - `rg -n "delete.*where|cleanupAfter|emptySince|activeCount" src/main/java/com/collabeditor/session/service/SessionCleanupScheduler.java src/main/java/com/collabeditor/session/service/SessionService.java` returns guarded cleanup logic rather than an unconditional stale delete.
    - `rg -n "rejoin|race|stale cleanup|full room|malformed invite" src/test/java/com/collabeditor/session/SessionCleanupSchedulerTest.java src/test/java/com/collabeditor/session/SessionControllerTest.java` returns the added cleanup and HTTP regression scenarios.
    - `JAVA_HOME=<supported-jdk> ./gradlew test --tests "*SessionCleanupSchedulerTest" --tests "*SessionControllerTest"` exits with status 0.
    - `JAVA_HOME=<supported-jdk> DOCKER_HOST=<docker-socket> ./gradlew test` exits with status 0.
  </acceptance_criteria>
  <verify>
    <automated>JAVA_HOME=&lt;supported-jdk&gt; ./gradlew test --tests "*SessionCleanupSchedulerTest" --tests "*SessionControllerTest"</automated>
    <automated>JAVA_HOME=&lt;supported-jdk&gt; DOCKER_HOST=&lt;docker-socket&gt; ./gradlew test</automated>
  </verify>
  <done>The cleanup worker and the HTTP layer both reflect the intended one-hour retention and concurrency guarantees, closing the remaining Phase 1 verification gaps.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `JAVA_HOME=<supported-jdk> ./gradlew test --tests "*SessionServiceTest"`
- [ ] `JAVA_HOME=<supported-jdk> ./gradlew test --tests "*SessionCleanupSchedulerTest" --tests "*SessionControllerTest"`
- [ ] `JAVA_HOME=<supported-jdk> DOCKER_HOST=<docker-socket> ./gradlew test`
</verification>

<success_criteria>
- Session create/join reject malformed or colliding invite-code paths deterministically.
- Join, leave, transfer, and rejoin preserve one coherent owner and the configured participant cap.
- Cleanup deletes only rooms that are still empty at delete time, and the full suite is green on the supported verification setup.
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-fix-phase-1-auth-session-and-verification-gaps/01.1-03-SUMMARY.md`
</output>
