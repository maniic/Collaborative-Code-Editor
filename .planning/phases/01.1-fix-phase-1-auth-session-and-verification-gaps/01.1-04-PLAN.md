---
phase: 01.1-fix-phase-1-auth-session-and-verification-gaps
plan: 04
type: execute
wave: 4
gap_closure: true
depends_on:
  - 01.1-02
  - 01.1-03
files_modified:
  - src/main/java/com/collabeditor/auth/security/SecurityConfig.java
  - src/test/java/com/collabeditor/auth/AuthControllerTest.java
  - src/test/java/com/collabeditor/session/SessionControllerTest.java
autonomous: true
requirements:
  - SESS-03
  - QUAL-01
must_haves:
  truths:
    - "Authenticated validation failures on protected session routes return HTTP 400, not HTTP 401."
    - "The real bearer filter path is covered by regression tests for malformed invite and invalid session-create payloads."
  artifacts:
    - path: "src/main/java/com/collabeditor/auth/security/SecurityConfig.java"
      provides: "Security rules that preserve framework validation responses on protected routes."
    - path: "src/test/java/com/collabeditor/session/SessionControllerTest.java"
      provides: "Regression coverage for malformed invite handling through the protected session API."
    - path: "src/test/java/com/collabeditor/auth/AuthControllerTest.java"
      provides: "Real bearer-filter assertions that prove protected route validation failures do not regress into unauthorized responses."
  key_links:
    - from: "src/main/java/com/collabeditor/auth/security/SecurityConfig.java"
      to: "src/main/java/com/collabeditor/session/api/SessionController.java"
      via: "error dispatch handling for protected MVC validation failures"
      pattern: "/error|DispatcherType|requestMatchers"
    - from: "src/test/java/com/collabeditor/auth/AuthControllerTest.java"
      to: "src/main/java/com/collabeditor/auth/security/JwtAuthenticationFilter.java"
      via: "real bearer-token filter-chain assertions"
      pattern: "Authorization|Bearer|JwtTokenService"
---

<objective>
Close the UAT gap where malformed protected POST requests return `401 Unauthorized` instead of the intended validation `400 Bad Request`.

Purpose: Session join/create validation must fail deterministically at the HTTP boundary. Phase 01.1 cannot claim trustworthy API hardening while protected validation errors are masked as auth failures in the live app.
Output: Security configuration and regression tests that preserve `400` validation behavior on authenticated session routes.
</objective>

<execution_context>
@/Users/abdullah/.codex/get-shit-done/workflows/execute-plan.md
@/Users/abdullah/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01.1-fix-phase-1-auth-session-and-verification-gaps/01.1-UAT.md
@.planning/debug/protected-validation-errors-return-401.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preserve validation status on protected MVC error paths</name>
  <read_first>
    src/main/java/com/collabeditor/auth/security/SecurityConfig.java
    src/main/java/com/collabeditor/common/api/ApiExceptionHandler.java
    src/main/java/com/collabeditor/session/api/SessionController.java
  </read_first>
  <files>src/main/java/com/collabeditor/auth/security/SecurityConfig.java, src/main/java/com/collabeditor/common/api/ApiExceptionHandler.java, src/main/java/com/collabeditor/session/api/SessionController.java</files>
  <action>
    Fix the live `401` regression for malformed protected POST requests. Update the security chain so MVC validation failures on protected routes can reach Spring's error handling without being reclassified as unauthorized. Prefer the smallest safe change that preserves stateless bearer auth for real protected business endpoints while allowing `/error` or error dispatches to surface the original validation status. If the error path still relies on the default error controller, ensure the API returns a stable `400` response for malformed invite and invalid language payloads.
  </action>
  <acceptance_criteria>
    - `rg -n "/error|DispatcherType|ERROR" src/main/java/com/collabeditor/auth/security/SecurityConfig.java` shows the explicit error-path allowance.
    - An authenticated `POST /api/sessions/join` with `{"inviteCode":"bad-code"}` returns `400` instead of `401`.
    - An authenticated `POST /api/sessions` with `{"language":"RUBY"}` returns `400` instead of `401`.
  </acceptance_criteria>
  <verify>
    <automated>JAVA_HOME=&lt;supported-jdk&gt; ./gradlew test --tests "*AuthControllerTest" --tests "*SessionControllerTest"</automated>
  </verify>
  <done>Protected validation failures preserve their real HTTP status instead of being masked by the security chain.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add real bearer-filter regression coverage for protected validation failures</name>
  <read_first>
    src/test/java/com/collabeditor/auth/AuthControllerTest.java
    src/test/java/com/collabeditor/session/SessionControllerTest.java
    src/main/java/com/collabeditor/auth/security/JwtAuthenticationFilter.java
  </read_first>
  <files>src/test/java/com/collabeditor/auth/AuthControllerTest.java, src/test/java/com/collabeditor/session/SessionControllerTest.java, src/main/java/com/collabeditor/auth/security/JwtAuthenticationFilter.java</files>
  <action>
    Add regression coverage that exercises invalid protected POST payloads through the real bearer-token filter chain, not only mocked authenticated principals. Reuse the existing `JwtTokenService` test configuration or add the minimal integration wiring needed so tests prove both malformed invite join and invalid session-create language return `400` while neighboring unauthenticated requests still return `401`.
  </action>
  <acceptance_criteria>
    - `rg -n "malformed invite|invalid language|Bearer" src/test/java/com/collabeditor/auth/AuthControllerTest.java src/test/java/com/collabeditor/session/SessionControllerTest.java` shows real bearer-filter regression coverage for protected validation failures.
    - `JAVA_HOME=<supported-jdk> ./gradlew test --tests "*AuthControllerTest" --tests "*SessionControllerTest"` exits with status 0.
  </acceptance_criteria>
  <verify>
    <automated>JAVA_HOME=&lt;supported-jdk&gt; ./gradlew test --tests "*AuthControllerTest" --tests "*SessionControllerTest"</automated>
  </verify>
  <done>The gap is locked down by tests that match the live auth path seen during UAT.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `JAVA_HOME=<supported-jdk> ./gradlew test --tests "*AuthControllerTest" --tests "*SessionControllerTest"`
- [ ] Manual recheck: authenticated malformed invite returns `400`
- [ ] Manual recheck: authenticated invalid language returns `400`
</verification>

<success_criteria>
- Protected session-route validation failures return `400` instead of `401`.
- The real bearer filter path is covered by regression tests, so this bug cannot hide behind mocked authentication.
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-fix-phase-1-auth-session-and-verification-gaps/01.1-04-SUMMARY.md`
</output>
