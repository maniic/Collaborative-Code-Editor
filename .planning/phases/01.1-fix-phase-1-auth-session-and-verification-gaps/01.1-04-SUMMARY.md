---
phase: 01.1-fix-phase-1-auth-session-and-verification-gaps
plan: 04
subsystem: auth
tags: [spring-security, validation, bearer-auth, testing]

requires:
  - phase: 01.1-fix-phase-1-auth-session-and-verification-gaps (plans 01-03)
    provides: protected session APIs, real JWT bearer auth, and the diagnosed UAT gap for protected validation failures
provides:
  - Protected validation failures keep their original 400 status through the Spring Security error dispatch path
  - Real bearer-token regression coverage for malformed invite and invalid language protected POST requests
  - Session controller validation coverage for invalid create-session payloads
affects: [phase-2-ot-collaboration, phase-5-integration-hardening-and-developer-docs]

tech-stack:
  added: []
  patterns: [error-dispatch allowance for protected validation failures, signed-bearer MVC regression coverage]

key-files:
  created: []
  modified:
    - src/main/java/com/collabeditor/auth/security/SecurityConfig.java
    - src/test/java/com/collabeditor/auth/AuthControllerTest.java
    - src/test/java/com/collabeditor/session/SessionControllerTest.java

key-decisions:
  - "SecurityConfig now permits DispatcherType.ERROR and /error so authenticated MVC validation failures preserve their original HTTP status."
  - "Protected-route validation regressions are asserted with JwtTokenService-issued bearer tokens instead of mocked principals alone."

patterns-established:
  - "Protected-route validation bugs should be covered by both controller-slice assertions and real bearer-filter regression tests."

requirements-completed: [SESS-03, QUAL-01]

duration: 4min
completed: 2026-03-27
---

# Phase 01.1 Plan 4 Summary

**Protected session-route validation now returns 400 through the real bearer-auth path, with regression tests for malformed invite and invalid language payloads**

## Performance

- **Duration:** 4 min
- **Started:** 2026-03-27T19:06:32Z
- **Completed:** 2026-03-27T19:11:04Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- Allowed Spring Security error dispatches to reach framework validation handling instead of remapping malformed protected POST requests to 401.
- Added real bearer-token regression coverage for malformed invite and invalid create-session payloads on protected routes.
- Kept the session controller slice explicit about invalid language rejection so protected-route and controller-level validation both stay covered.

## Task Commits

Each task was committed atomically:

1. **Task 1: Preserve validation status on protected MVC error paths** - `830cd12` (fix)
2. **Task 2: Add real bearer-filter regression coverage for protected validation failures** - `5467b74` (test)

## Files Created/Modified
- `src/main/java/com/collabeditor/auth/security/SecurityConfig.java` - Permits error dispatches and `/error` so protected validation failures preserve their original status code.
- `src/test/java/com/collabeditor/auth/AuthControllerTest.java` - Exercises malformed invite and invalid language requests through signed bearer tokens and keeps unauthenticated protected POST coverage explicit.
- `src/test/java/com/collabeditor/session/SessionControllerTest.java` - Covers invalid language rejection in the session controller slice alongside malformed invite validation.

## Decisions Made
- Permitted `DispatcherType.ERROR` and `/error` rather than weakening protection on session endpoints themselves.
- Verified the regression through the production bearer filter path instead of relying only on mocked `Authentication` principals.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Used bearer-path MockMvc regressions instead of an extra localhost curl pass**
- **Found during:** Task 2 (Add real bearer-filter regression coverage for protected validation failures)
- **Issue:** No standalone backend instance was running in this workspace, so the plan's extra live curl recheck could not be performed directly.
- **Fix:** Added and ran signed-bearer MockMvc regressions that exercise the same protected MVC validation path for malformed invite and invalid language payloads.
- **Files modified:** `src/test/java/com/collabeditor/auth/AuthControllerTest.java`
- **Verification:** `JAVA_HOME=/Users/abdullah/Library/Java/JavaVirtualMachines/corretto-24.0.2/Contents/Home ./gradlew test --tests "*AuthControllerTest" --tests "*SessionControllerTest"`
- **Committed in:** `5467b74`

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** The verification intent was preserved through automated bearer-path coverage without expanding this gap-fix plan into local stack bring-up work.

## Issues Encountered
- The targeted Gradle suite passed on the first run after the error-dispatch allowance and bearer-path tests were added. No code-level issues remained after verification.

## User Setup Required

None - no external service configuration was needed for this gap-closure plan.

## Next Phase Readiness
- Phase 01.1 now closes its remaining protected-validation gap, so session API hardening no longer blocks Phase 2 OT work.
- Future security regressions on protected validation paths should surface in tests before UAT because malformed invite and invalid language payloads now run through the real bearer chain in CI.

---
*Phase: 01.1-fix-phase-1-auth-session-and-verification-gaps*
*Completed: 2026-03-27*
