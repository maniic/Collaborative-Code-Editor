---
phase: 01.1
slug: fix-phase-1-auth-session-and-verification-gaps
status: ready
nyquist_compliant: true
wave_0_complete: false
created: 2026-03-27
---

# Phase 01.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | JUnit 5 + Spring Boot Test + Spring Security Test + Testcontainers |
| **Config file** | `build.gradle.kts` |
| **Quick run command** | `JAVA_HOME=<supported-jdk> ./gradlew test --tests "com.collabeditor.auth.*" --tests "com.collabeditor.session.*"` |
| **Full suite command** | `JAVA_HOME=<supported-jdk> DOCKER_HOST=<docker-socket> ./gradlew test` |
| **Estimated runtime** | ~30-90 seconds |

---

## Sampling Rate

- **After every task commit:** Run `JAVA_HOME=<supported-jdk> ./gradlew test --tests "com.collabeditor.auth.*" --tests "com.collabeditor.session.*"`
- **After every plan wave:** Run `JAVA_HOME=<supported-jdk> DOCKER_HOST=<docker-socket> ./gradlew test`
- **Before `$gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 90 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 01.1-01-01 | 01 | 1 | AUTH-02, AUTH-03 | auth service + security regression | `JAVA_HOME=<supported-jdk> ./gradlew test --tests "com.collabeditor.auth.*"` | ❌ 01.1 | ⬜ pending |
| 01.1-02-01 | 02 | 1 | SESS-01, SESS-03, SESS-04 | session service + scheduler regression | `JAVA_HOME=<supported-jdk> ./gradlew test --tests "com.collabeditor.session.*"` | ❌ 01.1 | ⬜ pending |
| 01.1-03-01 | 03 | 2 | QUAL-01 | migration + full-suite verification | `JAVA_HOME=<supported-jdk> DOCKER_HOST=<docker-socket> ./gradlew test --tests "*FlywayMigrationTest"` | ❌ 01.1 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] Supported JDK path or runtime expectation is explicit for Gradle verification
- [ ] `src/test/java/com/collabeditor/auth/` — security-chain regression coverage for bearer-token handling
- [ ] `src/test/java/com/collabeditor/session/` — regression coverage for ownership, join, leave, and cleanup edge cases
- [ ] `src/test/java/com/collabeditor/migration/` — Flyway/JPA parity remains green under Testcontainers

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Live auth flow rejects invalid bearer tokens and rotates refresh cookies safely | AUTH-02, AUTH-03 | Requires running app, database, and real HTTP headers/cookies | Boot the app with a real DB, call register/login/refresh, then verify valid bearer access works and malformed or invalid bearer tokens return `401` |
| Live session lifecycle keeps ownership and cleanup state consistent after transfer and rejoin | SESS-03, SESS-04 | Scheduler timing and DB state transitions are easiest to validate end-to-end | Use two users to create, join, transfer ownership, rejoin, leave, and observe `owner_user_id`, participant roles, and cleanup timestamps |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 90s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-03-27
