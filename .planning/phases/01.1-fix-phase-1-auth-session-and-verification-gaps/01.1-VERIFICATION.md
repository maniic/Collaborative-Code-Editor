---
phase: 01.1-fix-phase-1-auth-session-and-verification-gaps
verified: 2026-03-27T05:35:00Z
status: passed
score: 8/8 must-haves verified
re_verification: true
gaps: []
human_verification: []
---

# Phase 01.1 Verification Report

**Phase Goal:** Phase 1 auth and session behavior is secure, concurrency-safe, and honestly verifiable before real-time collaboration work begins.
**Verified:** 2026-03-27
**Status:** passed
**Re-verification:** Yes — remediation phase after the original Phase 1 review

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | The Flyway baseline and Hibernate validation agree on the `coding_sessions.participant_cap` contract | VERIFIED | `CodingSessionEntity` now maps `participant_cap` as a SMALLINT-compatible field and `FlywayMigrationTest` asserts the PostgreSQL column type |
| 2 | The supported verification path is explicit and reproducible on the supported local runtime | VERIFIED | `build.gradle.kts` defaults tests to the `test` profile and preserves Docker/Testcontainers wiring; `application-test.yml` carries explicit JPA, Flyway, and auth settings |
| 3 | JWT auth fails closed when the secret is missing/short, the issuer is wrong, or the subject is malformed | VERIFIED | `SecurityProperties` validates `jwt-secret` and `jwt-issuer`; `JwtTokenServiceTest` covers wrong issuer, malformed subject, expired token, and insecure secret configuration |
| 4 | The bearer filter treats invalid tokens as authentication failure instead of throwing into the request path | VERIFIED | `JwtAuthenticationFilter` now guards UUID parsing and `AuthControllerTest` exercises invalid and valid bearer access against the production filter chain |
| 5 | Refresh-token rotation is single-use even when two callers race on the same raw token | VERIFIED | `RefreshSessionRepository.findByTokenHashForUpdate(...)` takes a pessimistic lock and `RefreshSessionServiceTest` proves exactly one concurrent rotation succeeds |
| 6 | Session create/join reject malformed invite codes and retry invite collisions deterministically | VERIFIED | `JoinSessionRequest` validates `[A-Z2-9]{8}`, `SessionService` normalizes invite codes to uppercase, and `SessionServiceTest` covers malformed input, uppercase normalization, and collision retry |
| 7 | Session join, leave, owner transfer, and owner rejoin maintain one coherent owner under concurrent mutations | VERIFIED | `SessionService` locks the session row for join/leave, demotes the leaving owner before transfer, and keeps rejoined former owners as participants unless `owner_user_id` still points to them |
| 8 | Cleanup deletes only rooms that are still empty at delete time, and the full Phase 1 suite is green on Docker-backed verification | VERIFIED | `SessionCleanupScheduler` re-locks each expired candidate before delete, `SessionCleanupSchedulerTest` covers the stale cleanup race, and `JAVA_HOME=... DOCKER_HOST=... ./gradlew test` passed |

**Score:** 8/8 truths verified

---

## Verification Commands

The following automated checks were run successfully during execution:

- `JAVA_HOME=/Users/abdullah/Library/Java/JavaVirtualMachines/corretto-24.0.2/Contents/Home DOCKER_HOST=unix:///Users/abdullah/.colima/default/docker.sock ./gradlew test --tests "*FlywayMigrationTest"`
- `JAVA_HOME=/Users/abdullah/Library/Java/JavaVirtualMachines/corretto-24.0.2/Contents/Home ./gradlew test --tests "*JwtTokenServiceTest" --tests "*RefreshSessionServiceTest" --tests "*AuthControllerTest"`
- `JAVA_HOME=/Users/abdullah/Library/Java/JavaVirtualMachines/corretto-24.0.2/Contents/Home ./gradlew test --tests "*SessionServiceTest"`
- `JAVA_HOME=/Users/abdullah/Library/Java/JavaVirtualMachines/corretto-24.0.2/Contents/Home ./gradlew test --tests "*SessionCleanupSchedulerTest" --tests "*SessionControllerTest"`
- `JAVA_HOME=/Users/abdullah/Library/Java/JavaVirtualMachines/corretto-24.0.2/Contents/Home DOCKER_HOST=unix:///Users/abdullah/.colima/default/docker.sock ./gradlew test`

---

## Requirements Coverage

| Requirement | Status | Evidence |
|-------------|--------|----------|
| AUTH-01 | SATISFIED | Auth configuration and controller tests now run against the real security chain without the old `permitAll()` bypass |
| AUTH-02 | SATISFIED | Access JWTs require the app-issued issuer and a valid UUID subject |
| AUTH-03 | SATISFIED | Refresh rotation is protected by a locked lookup and concurrent single-use regression coverage |
| SESS-01 | SATISFIED | Session creation retries invite-code collisions deterministically |
| SESS-03 | SATISFIED | Join validates/normalizes invite codes and enforces participant caps under the session lock |
| SESS-04 | SATISFIED | Leave is idempotent for already-left members, transfers ownership deterministically, and preserves one coherent owner on rejoin |
| QUAL-01 | SATISFIED | Flyway/JPA parity and the full Docker-backed suite are green on the supported runtime |

---

## Gaps Summary

No gaps found. Phase 01.1 closes the reviewed security, concurrency, and verification defects from Phase 1 and unblocks Phase 2.

---

_Verified: 2026-03-27_
_Verifier: Codex (inline execute-phase verification)_
