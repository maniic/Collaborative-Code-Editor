---
phase: 02-real-time-ot-collaboration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/collabeditor/ot/model/TextOperation.java
  - src/main/java/com/collabeditor/ot/model/InsertOperation.java
  - src/main/java/com/collabeditor/ot/model/DeleteOperation.java
  - src/main/java/com/collabeditor/ot/model/AppliedOperation.java
  - src/main/java/com/collabeditor/ot/model/DocumentSnapshot.java
  - src/main/java/com/collabeditor/ot/service/OperationalTransformService.java
  - src/main/java/com/collabeditor/ot/service/CollaborationSessionRuntime.java
  - src/test/java/com/collabeditor/ot/OperationalTransformServiceTest.java
  - src/test/java/com/collabeditor/ot/CollaborationSessionRuntimeTest.java
autonomous: true
requirements:
  - COLL-02
  - COLL-03
  - COLL-04
  - QUAL-02
must_haves:
  truths:
    - "The collaboration engine accepts multi-character insert and delete operations against a canonical document."
    - "Stale client operations are transformed forward through canonical history instead of being dropped by default."
    - "Same-position inserts and overlapping deletes resolve deterministically so every participant converges on the same text."
    - "The OT test suite proves transform edge cases and three-user convergence before WebSocket transport is layered on top."
  artifacts:
    - path: "src/main/java/com/collabeditor/ot/service/OperationalTransformService.java"
      provides: "Pure pairwise OT transform and apply rules for insert/delete operations."
    - path: "src/main/java/com/collabeditor/ot/service/CollaborationSessionRuntime.java"
      provides: "Per-session in-memory canonical document state, revision tracking, and stale-op forward transformation."
    - path: "src/test/java/com/collabeditor/ot/OperationalTransformServiceTest.java"
      provides: "Exhaustive transform edge-case coverage."
    - path: "src/test/java/com/collabeditor/ot/CollaborationSessionRuntimeTest.java"
      provides: "Three-user convergence and stale-revision runtime coverage."
  key_links:
    - from: "src/main/java/com/collabeditor/ot/service/OperationalTransformService.java"
      to: "src/main/java/com/collabeditor/ot/model/TextOperation.java"
      via: "transform dispatch and typed operation modeling"
      pattern: "transform|apply|InsertOperation|DeleteOperation"
    - from: "src/main/java/com/collabeditor/ot/service/CollaborationSessionRuntime.java"
      to: "src/main/java/com/collabeditor/ot/service/OperationalTransformService.java"
      via: "runtime delegates stale-op transformation and document mutation to the pure OT service"
      pattern: "transform|applyClientOperation|snapshot"
---

<objective>
Implement the pure OT core and in-memory canonical document runtime for Phase 2.

Purpose: The project's core value is document convergence. WebSocket plumbing should not be built until the transform rules, revision handling, and three-user convergence behavior are proven in isolation.
Output: OT domain models, deterministic transform/apply logic, per-session canonical document runtime, and an automated OT test suite.
</objective>

<execution_context>
@/Users/abdullah/.codex/get-shit-done/workflows/execute-plan.md
@/Users/abdullah/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-real-time-ot-collaboration/02-CONTEXT.md
@.planning/phases/02-real-time-ot-collaboration/02-RESEARCH.md
@.planning/phases/02-real-time-ot-collaboration/02-VALIDATION.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define typed multi-character OT operations and deterministic transform rules</name>
  <read_first>
    .planning/phases/02-real-time-ot-collaboration/02-CONTEXT.md
    .planning/phases/02-real-time-ot-collaboration/02-RESEARCH.md
    .planning/research/PITFALLS.md
    build.gradle.kts
    src/main/java/com/collabeditor/ot/service/OperationalTransformService.java
  </read_first>
  <files>src/main/java/com/collabeditor/ot/model/TextOperation.java, src/main/java/com/collabeditor/ot/model/InsertOperation.java, src/main/java/com/collabeditor/ot/model/DeleteOperation.java, src/main/java/com/collabeditor/ot/model/AppliedOperation.java, src/main/java/com/collabeditor/ot/service/OperationalTransformService.java, src/test/java/com/collabeditor/ot/OperationalTransformServiceTest.java</files>
  <action>
    Create a sealed OT model under `src/main/java/com/collabeditor/ot/model/`. Define `TextOperation` as the common interface and implement `InsertOperation` and `DeleteOperation` as explicit multi-character records carrying the exact fields `UUID authorUserId`, `long baseRevision`, `String clientOperationId`, and either `int position` + `String text` for inserts or `int position` + `int length` for deletes. Add `AppliedOperation` so canonical history stores the transformed operation together with its assigned revision. Implement `OperationalTransformService` as a pure service with explicit pairwise methods for insert/insert, insert/delete, delete/insert, and delete/delete, plus `apply(String document, TextOperation operation)` for canonical document mutation. Use a deterministic same-position insert tie-break based on lexicographic `authorUserId.toString()` ordering and keep boundary validation exact: insert positions must satisfy `0 <= position <= document.length()`, delete operations must satisfy `0 <= position`, `length > 0`, and `position + length <= document.length()`.
  </action>
  <acceptance_criteria>
    - `rg -n "sealed interface TextOperation|record InsertOperation|record DeleteOperation|record AppliedOperation" src/main/java/com/collabeditor/ot/model/*.java` returns all four type definitions.
    - `rg -n "transformInsertInsert|transformInsertDelete|transformDeleteInsert|transformDeleteDelete|apply\\(" src/main/java/com/collabeditor/ot/service/OperationalTransformService.java` returns all four transform paths and the apply method.
    - `rg -n "authorUserId\\.toString\\(\\)\\.compareTo|clientOperationId|baseRevision" src/main/java/com/collabeditor/ot/service/OperationalTransformService.java src/main/java/com/collabeditor/ot/model/*.java` returns deterministic tie-break and metadata fields.
    - `./gradlew test --tests "*OperationalTransformServiceTest"` exits with status 0.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "*OperationalTransformServiceTest"</automated>
  </verify>
  <done>The OT model and pairwise transform rules are explicit, deterministic, and executable without any transport layer.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the in-memory canonical document runtime for a collaboration session</name>
  <read_first>
    .planning/phases/02-real-time-ot-collaboration/02-CONTEXT.md
    .planning/phases/02-real-time-ot-collaboration/02-RESEARCH.md
    src/main/java/com/collabeditor/ot/model/TextOperation.java
    src/main/java/com/collabeditor/ot/model/AppliedOperation.java
    src/main/java/com/collabeditor/ot/service/OperationalTransformService.java
    src/main/java/com/collabeditor/ot/service/CollaborationSessionRuntime.java
  </read_first>
  <files>src/main/java/com/collabeditor/ot/model/DocumentSnapshot.java, src/main/java/com/collabeditor/ot/service/CollaborationSessionRuntime.java, src/test/java/com/collabeditor/ot/CollaborationSessionRuntimeTest.java</files>
  <action>
    Implement `CollaborationSessionRuntime` as the canonical in-memory state holder for one `sessionId`. Store the exact fields `UUID sessionId`, `StringBuilder document`, `long revision`, `List<AppliedOperation> history`, and a `ReentrantLock` or equivalent per-session guard so `applyClientOperation(...)` is serialized. Add `DocumentSnapshot` to expose the current document text and canonical revision for connect and resync flows. `applyClientOperation(...)` must: validate the client's base revision, transform the incoming operation sequentially against `history` entries newer than `baseRevision`, apply the transformed operation to the canonical document, increment the canonical revision exactly once, append the resulting `AppliedOperation` to history, and return a structured result containing the new revision, the canonical transformed operation, and the updated snapshot. Keep Phase 2 fully in-memory: do not write to PostgreSQL, Redis, or snapshot tables here.
  </action>
  <acceptance_criteria>
    - `rg -n "StringBuilder document|long revision|List<AppliedOperation>|ReentrantLock|applyClientOperation|snapshot" src/main/java/com/collabeditor/ot/service/CollaborationSessionRuntime.java` returns the canonical runtime fields and methods.
    - `rg -n "record DocumentSnapshot|document\\(|revision\\(" src/main/java/com/collabeditor/ot/model/DocumentSnapshot.java` returns the snapshot type used for sync/resync.
    - `rg -n "stale revision|increments revision|history|document snapshot" src/test/java/com/collabeditor/ot/CollaborationSessionRuntimeTest.java` returns runtime test coverage for stale-op handling and canonical snapshots.
    - `./gradlew test --tests "*CollaborationSessionRuntimeTest"` exits with status 0.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "*CollaborationSessionRuntimeTest"</automated>
  </verify>
  <done>The collaboration runtime can accept stale operations, transform them through canonical history, and emit a stable document snapshot without any WebSocket code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add exhaustive OT edge-case and three-user convergence coverage</name>
  <read_first>
    .planning/phases/02-real-time-ot-collaboration/02-VALIDATION.md
    .planning/research/PITFALLS.md
    src/main/java/com/collabeditor/ot/service/OperationalTransformService.java
    src/main/java/com/collabeditor/ot/service/CollaborationSessionRuntime.java
    src/test/java/com/collabeditor/ot/OperationalTransformServiceTest.java
    src/test/java/com/collabeditor/ot/CollaborationSessionRuntimeTest.java
  </read_first>
  <files>src/test/java/com/collabeditor/ot/OperationalTransformServiceTest.java, src/test/java/com/collabeditor/ot/CollaborationSessionRuntimeTest.java</files>
  <action>
    Expand the Phase 2 OT tests so they prove the roadmap’s convergence guarantee instead of only checking one or two happy paths. In `OperationalTransformServiceTest`, add named tests for same-position insert ordering, insert-before-delete boundary handling, delete swallowing a later insert point, partially overlapping deletes, and delete-containing-delete overlap normalization. In `CollaborationSessionRuntimeTest`, add a deterministic three-user convergence suite that applies interleaved operations from three `authorUserId` values against the same runtime, plus a seeded randomized loop that runs hundreds of small operation schedules and asserts all simulated user views converge to the same final document. Keep the tests fast and deterministic by using fixed seeds and clear assertion messages that print the seed on failure.
  </action>
  <acceptance_criteria>
    - `rg -n "same position|overlapping deletes|insert before delete|seed" src/test/java/com/collabeditor/ot/OperationalTransformServiceTest.java src/test/java/com/collabeditor/ot/CollaborationSessionRuntimeTest.java` returns explicit edge-case and seeded-convergence tests.
    - `rg -n "three user|convergence|Random\\(|seed" src/test/java/com/collabeditor/ot/CollaborationSessionRuntimeTest.java` returns named three-user convergence coverage.
    - `./gradlew test --tests "*OperationalTransformServiceTest" --tests "*CollaborationSessionRuntimeTest"` exits with status 0.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "*OperationalTransformServiceTest" --tests "*CollaborationSessionRuntimeTest"</automated>
  </verify>
  <done>The OT core is protected by edge-case and convergence coverage strong enough to justify building transport and presence code on top of it.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `./gradlew test --tests "*OperationalTransformServiceTest"`
- [ ] `./gradlew test --tests "*CollaborationSessionRuntimeTest"`
- [ ] `./gradlew test --tests "*OperationalTransformServiceTest" --tests "*CollaborationSessionRuntimeTest"`
</verification>

<success_criteria>
- Multi-character insert and delete operations transform correctly against concurrent edits.
- A per-session canonical runtime applies stale operations deterministically and increments one canonical revision stream.
- The OT test suite proves transform edge cases and three-user convergence.
</success_criteria>

<output>
After completion, create `.planning/phases/02-real-time-ot-collaboration/02-01-SUMMARY.md`
</output>
