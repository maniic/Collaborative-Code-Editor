---
phase: 02-real-time-ot-collaboration
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - build.gradle.kts
  - src/main/java/com/collabeditor/auth/service/JwtTokenService.java
  - src/main/java/com/collabeditor/session/persistence/SessionParticipantRepository.java
  - src/main/java/com/collabeditor/websocket/config/WebSocketConfig.java
  - src/main/java/com/collabeditor/websocket/security/CollaborationHandshakeInterceptor.java
  - src/main/java/com/collabeditor/websocket/handler/CollaborationWebSocketHandler.java
  - src/main/java/com/collabeditor/websocket/protocol/CollaborationEnvelope.java
  - src/main/java/com/collabeditor/websocket/protocol/ClientMessageType.java
  - src/main/java/com/collabeditor/websocket/protocol/ServerMessageType.java
  - src/main/java/com/collabeditor/websocket/protocol/DocumentSyncPayload.java
  - src/main/java/com/collabeditor/websocket/protocol/SubmitOperationPayload.java
  - src/main/java/com/collabeditor/websocket/protocol/OperationAckPayload.java
  - src/main/java/com/collabeditor/websocket/protocol/OperationAppliedPayload.java
  - src/main/java/com/collabeditor/websocket/protocol/OperationErrorPayload.java
  - src/main/java/com/collabeditor/websocket/protocol/ResyncRequiredPayload.java
  - src/main/java/com/collabeditor/websocket/service/CollaborationSessionRegistry.java
  - src/test/java/com/collabeditor/websocket/CollaborationHandshakeInterceptorTest.java
  - src/test/java/com/collabeditor/websocket/CollaborationWebSocketHandlerTest.java
autonomous: true
requirements:
  - COLL-01
  - COLL-02
  - COLL-03
must_haves:
  truths:
    - "A joined active participant can open a room socket at a session-scoped raw WebSocket endpoint using the existing bearer-token model."
    - "Connecting immediately returns a typed full-document bootstrap with the canonical revision and active participants."
    - "Submitting an operation produces an explicit acknowledgement for the sender plus a canonical operation broadcast to every socket in the room, including the sender."
    - "Invalid operations, unauthorized membership, and unrecoverable desyncs surface as explicit error or resync events instead of silent failures."
  artifacts:
    - path: "src/main/java/com/collabeditor/websocket/config/WebSocketConfig.java"
      provides: "Session-scoped raw WebSocket endpoint registration."
    - path: "src/main/java/com/collabeditor/websocket/security/CollaborationHandshakeInterceptor.java"
      provides: "Handshake-time bearer-token validation and room-membership enforcement."
    - path: "src/main/java/com/collabeditor/websocket/handler/CollaborationWebSocketHandler.java"
      provides: "Typed sync, submit, ack, broadcast, error, and resync message handling."
    - path: "src/test/java/com/collabeditor/websocket/CollaborationWebSocketHandlerTest.java"
      provides: "Automated verification of the raw WebSocket collaboration contract."
  key_links:
    - from: "src/main/java/com/collabeditor/websocket/security/CollaborationHandshakeInterceptor.java"
      to: "src/main/java/com/collabeditor/auth/service/JwtTokenService.java"
      via: "shared token parsing and identity extraction for handshake auth"
      pattern: "parseToken|email|UUID"
    - from: "src/main/java/com/collabeditor/websocket/handler/CollaborationWebSocketHandler.java"
      to: "src/main/java/com/collabeditor/ot/service/CollaborationSessionRuntime.java"
      via: "submit_operation delegates to the canonical OT runtime"
      pattern: "applyClientOperation|snapshot"
    - from: "src/main/java/com/collabeditor/websocket/handler/CollaborationWebSocketHandler.java"
      to: "src/main/java/com/collabeditor/session/persistence/SessionParticipantRepository.java"
      via: "active-membership checks for connect and message authorization"
      pattern: "findBySessionIdAndUserId|ACTIVE"
---

<objective>
Implement the raw WebSocket collaboration endpoint, handshake auth, and typed document-sync/operation protocol for Phase 2.

Purpose: Once the OT core is trustworthy, the next requirement is to expose it through the locked raw JSON wire contract so joined participants can connect, bootstrap, submit operations, and receive canonical acknowledgements and broadcasts.
Output: Spring WebSocket dependency/config, handshake-time bearer auth, typed protocol DTOs, socket registry, handler logic, and WebSocket contract tests.
</objective>

<execution_context>
@/Users/abdullah/.codex/get-shit-done/workflows/execute-plan.md
@/Users/abdullah/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-real-time-ot-collaboration/02-CONTEXT.md
@.planning/phases/02-real-time-ot-collaboration/02-RESEARCH.md
@.planning/phases/02-real-time-ot-collaboration/02-VALIDATION.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add raw WebSocket endpoint registration and handshake-time room authentication</name>
  <read_first>
    .planning/phases/02-real-time-ot-collaboration/02-CONTEXT.md
    .planning/phases/02-real-time-ot-collaboration/02-RESEARCH.md
    build.gradle.kts
    src/main/java/com/collabeditor/auth/service/JwtTokenService.java
    src/main/java/com/collabeditor/auth/security/SecurityConfig.java
    src/main/java/com/collabeditor/session/persistence/SessionParticipantRepository.java
  </read_first>
  <files>build.gradle.kts, src/main/java/com/collabeditor/auth/service/JwtTokenService.java, src/main/java/com/collabeditor/session/persistence/SessionParticipantRepository.java, src/main/java/com/collabeditor/websocket/config/WebSocketConfig.java, src/main/java/com/collabeditor/websocket/security/CollaborationHandshakeInterceptor.java, src/test/java/com/collabeditor/websocket/CollaborationHandshakeInterceptorTest.java</files>
  <action>
    Add `implementation("org.springframework.boot:spring-boot-starter-websocket")` to `build.gradle.kts`. Register a raw endpoint at the exact path pattern `/ws/sessions/{sessionId}` through `WebSocketConfigurer`, not STOMP. Create `CollaborationHandshakeInterceptor` that reads `Authorization: Bearer <token>` from the handshake request, validates it through `JwtTokenService`, resolves `userId` and `email`, parses the canonical `sessionId` path variable, and verifies that `SessionParticipantRepository.findBySessionIdAndUserId(sessionId, userId)` exists with status `ACTIVE`. Store `sessionId`, `userId`, and `email` on the socket attributes for later message handling. Refactor `JwtTokenService` if needed so both the existing HTTP filter and the new handshake interceptor use the same identity extraction semantics rather than duplicating `UUID.fromString(...)` and email-claim parsing in two places.
  </action>
  <acceptance_criteria>
    - `rg -n "spring-boot-starter-websocket" build.gradle.kts` returns the WebSocket dependency.
    - `rg -n "/ws/sessions/\\{sessionId\\}|WebSocketConfigurer|registerWebSocketHandlers" src/main/java/com/collabeditor/websocket/config/WebSocketConfig.java` returns the session-scoped raw endpoint registration.
    - `rg -n "Authorization|Bearer|sessionId|ACTIVE|userId|email" src/main/java/com/collabeditor/websocket/security/CollaborationHandshakeInterceptor.java src/main/java/com/collabeditor/session/persistence/SessionParticipantRepository.java` returns handshake auth and active-membership enforcement.
    - `./gradlew test --tests "*CollaborationHandshakeInterceptorTest"` exits with status 0.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "*CollaborationHandshakeInterceptorTest"</automated>
  </verify>
  <done>Only already-joined authenticated participants can open a room socket, and the raw endpoint is registered on the current Spring MVC application.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the typed sync, submit, acknowledgement, error, and resync protocol</name>
  <read_first>
    .planning/phases/02-real-time-ot-collaboration/02-CONTEXT.md
    .planning/phases/02-real-time-ot-collaboration/02-RESEARCH.md
    src/main/java/com/collabeditor/ot/service/CollaborationSessionRuntime.java
    src/main/java/com/collabeditor/websocket/handler/CollaborationWebSocketHandler.java
    src/test/java/com/collabeditor/websocket/CollaborationWebSocketHandlerTest.java
  </read_first>
  <files>src/main/java/com/collabeditor/websocket/protocol/CollaborationEnvelope.java, src/main/java/com/collabeditor/websocket/protocol/ClientMessageType.java, src/main/java/com/collabeditor/websocket/protocol/ServerMessageType.java, src/main/java/com/collabeditor/websocket/protocol/DocumentSyncPayload.java, src/main/java/com/collabeditor/websocket/protocol/SubmitOperationPayload.java, src/main/java/com/collabeditor/websocket/protocol/OperationAckPayload.java, src/main/java/com/collabeditor/websocket/protocol/OperationAppliedPayload.java, src/main/java/com/collabeditor/websocket/protocol/OperationErrorPayload.java, src/main/java/com/collabeditor/websocket/protocol/ResyncRequiredPayload.java, src/main/java/com/collabeditor/websocket/service/CollaborationSessionRegistry.java, src/main/java/com/collabeditor/websocket/handler/CollaborationWebSocketHandler.java, src/test/java/com/collabeditor/websocket/CollaborationWebSocketHandlerTest.java</files>
  <action>
    Define typed raw-JSON protocol records under `src/main/java/com/collabeditor/websocket/protocol/` using the locked `type` + `payload` shape. Add exact server message types `document_sync`, `operation_ack`, `operation_applied`, `operation_error`, and `resync_required`; add exact client message type `submit_operation`. `DocumentSyncPayload` must carry the full current document, canonical revision, and the active participant snapshot. `SubmitOperationPayload` must carry the exact fields `clientOperationId`, `baseRevision`, `operationType`, `position`, and either `text` or `length`, where `operationType` is `INSERT` or `DELETE`. Implement `CollaborationSessionRegistry` to hold active sockets and a `CollaborationSessionRuntime` per canonical `sessionId`. In `CollaborationWebSocketHandler.afterConnectionEstablished(...)`, send `document_sync` immediately using the runtime snapshot. In `handleTextMessage(...)`, deserialize `submit_operation`, delegate to `CollaborationSessionRuntime.applyClientOperation(...)`, send `operation_ack` to the submitting socket with the assigned canonical revision, and broadcast `operation_applied` to all sockets for that session including the sender. If operation validation fails, send `operation_error` and keep the socket open. If the runtime reports an unrecoverable desync, send `resync_required` with the latest full snapshot. If a later message comes from a socket whose participant record is no longer `ACTIVE`, send an authorization error event and close the socket.
  </action>
  <acceptance_criteria>
    - `rg -n "document_sync|operation_ack|operation_applied|operation_error|resync_required|submit_operation" src/main/java/com/collabeditor/websocket/protocol/*.java src/main/java/com/collabeditor/websocket/handler/CollaborationWebSocketHandler.java` returns every locked message type.
    - `rg -n "afterConnectionEstablished|handleTextMessage|applyClientOperation|broadcast|close\\(" src/main/java/com/collabeditor/websocket/handler/CollaborationWebSocketHandler.java` returns sync, submit, broadcast, and close logic.
    - `rg -n "clientOperationId|baseRevision|operationType|position|text|length" src/main/java/com/collabeditor/websocket/protocol/SubmitOperationPayload.java` returns the exact submit payload fields.
    - `./gradlew test --tests "*CollaborationWebSocketHandlerTest"` exits with status 0.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "*CollaborationWebSocketHandlerTest"</automated>
  </verify>
  <done>The raw room socket exposes the exact sync, submit, ack, error, and resync semantics chosen during discuss-phase.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add WebSocket contract tests for handshake auth, sync bootstrap, and canonical broadcasts</name>
  <read_first>
    .planning/phases/02-real-time-ot-collaboration/02-VALIDATION.md
    src/main/java/com/collabeditor/websocket/config/WebSocketConfig.java
    src/main/java/com/collabeditor/websocket/handler/CollaborationWebSocketHandler.java
    src/test/java/com/collabeditor/websocket/CollaborationHandshakeInterceptorTest.java
    src/test/java/com/collabeditor/websocket/CollaborationWebSocketHandlerTest.java
  </read_first>
  <files>src/test/java/com/collabeditor/websocket/CollaborationHandshakeInterceptorTest.java, src/test/java/com/collabeditor/websocket/CollaborationWebSocketHandlerTest.java</files>
  <action>
    Add WebSocket tests that exercise the exact Phase 2 contract against the Spring configuration. Cover handshake rejection for missing bearer token, invalid JWT, and non-active room membership. Cover successful connect bootstrap by asserting the first server payload is `document_sync` with `document`, `revision`, and participant snapshot fields. Cover a successful `submit_operation` flow by asserting the sender receives both `operation_ack` and `operation_applied`, and a peer socket receives the same `operation_applied` payload with the canonical revision and transformed operation. Add negative tests for malformed payloads yielding `operation_error` and unrecoverable stale-state paths yielding `resync_required`.
  </action>
  <acceptance_criteria>
    - `rg -n "missing bearer|invalid jwt|non-active participant|document_sync|operation_ack|operation_applied|operation_error|resync_required" src/test/java/com/collabeditor/websocket/CollaborationHandshakeInterceptorTest.java src/test/java/com/collabeditor/websocket/CollaborationWebSocketHandlerTest.java` returns the expected handshake and protocol scenarios.
    - `./gradlew test --tests "*CollaborationHandshakeInterceptorTest" --tests "*CollaborationWebSocketHandlerTest"` exits with status 0.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "*CollaborationHandshakeInterceptorTest" --tests "*CollaborationWebSocketHandlerTest"</automated>
  </verify>
  <done>The room socket contract is automated end to end: connect, bootstrap, submit, acknowledge, broadcast, error, and resync.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `./gradlew test --tests "*CollaborationHandshakeInterceptorTest"`
- [ ] `./gradlew test --tests "*CollaborationWebSocketHandlerTest"`
- [ ] `./gradlew test --tests "*CollaborationHandshakeInterceptorTest" --tests "*CollaborationWebSocketHandlerTest"`
</verification>

<success_criteria>
- Joined active participants can connect to a raw room socket using JWT handshake auth.
- Connection bootstrap returns the full document and canonical revision immediately.
- Submitted operations yield explicit acknowledgements and canonical broadcasts, while invalid/desynced paths emit explicit error or resync events.
</success_criteria>

<output>
After completion, create `.planning/phases/02-real-time-ot-collaboration/02-02-SUMMARY.md`
</output>
