---
phase: 02-real-time-ot-collaboration
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - src/main/resources/application.yml
  - src/main/java/com/collabeditor/auth/persistence/UserRepository.java
  - src/main/java/com/collabeditor/session/persistence/SessionParticipantRepository.java
  - src/main/java/com/collabeditor/websocket/model/SelectionRange.java
  - src/main/java/com/collabeditor/websocket/protocol/PresenceUpdatePayload.java
  - src/main/java/com/collabeditor/websocket/protocol/PresenceUpdatedPayload.java
  - src/main/java/com/collabeditor/websocket/protocol/ParticipantJoinedPayload.java
  - src/main/java/com/collabeditor/websocket/protocol/ParticipantLeftPayload.java
  - src/main/java/com/collabeditor/websocket/service/PresenceService.java
  - src/main/java/com/collabeditor/websocket/service/CollaborationSessionRegistry.java
  - src/main/java/com/collabeditor/websocket/handler/CollaborationWebSocketHandler.java
  - src/test/java/com/collabeditor/websocket/PresenceServiceTest.java
  - src/test/java/com/collabeditor/websocket/CollaborationWebSocketHandlerTest.java
autonomous: true
requirements:
  - COLL-05
  - COLL-06
  - QUAL-02
must_haves:
  truths:
    - "Presence payloads expose `userId` and `email` plus a zero-length-or-range selection model."
    - "Room peers receive explicit `participant_joined` and `participant_left` events instead of inferred list diffs."
    - "Selection ranges are updated in response to canonical document operations, and cursor traffic is throttled without losing the latest position."
    - "The final Phase 2 test suite proves OT, WebSocket protocol, presence, and convergence together before execute-phase is allowed to move on."
  artifacts:
    - path: "src/main/java/com/collabeditor/websocket/service/PresenceService.java"
      provides: "Ephemeral per-session presence storage, throttling, and canonical range transformation."
    - path: "src/main/java/com/collabeditor/websocket/handler/CollaborationWebSocketHandler.java"
      provides: "Participant join/leave and presence update event broadcasting."
    - path: "src/test/java/com/collabeditor/websocket/PresenceServiceTest.java"
      provides: "Automated presence transformation and throttling coverage."
    - path: "src/test/java/com/collabeditor/websocket/CollaborationWebSocketHandlerTest.java"
      provides: "End-to-end socket coverage for presence and final collaboration flows."
  key_links:
    - from: "src/main/java/com/collabeditor/websocket/service/PresenceService.java"
      to: "src/main/java/com/collabeditor/ot/service/OperationalTransformService.java"
      via: "selection ranges must transform through the same canonical operations that mutate the document"
      pattern: "transform|SelectionRange|InsertOperation|DeleteOperation"
    - from: "src/main/java/com/collabeditor/websocket/handler/CollaborationWebSocketHandler.java"
      to: "src/main/java/com/collabeditor/websocket/service/PresenceService.java"
      via: "presence updates and join/leave event broadcasting"
      pattern: "participant_joined|participant_left|presence_updated"
    - from: "src/main/java/com/collabeditor/session/persistence/SessionParticipantRepository.java"
      to: "src/main/java/com/collabeditor/auth/persistence/UserRepository.java"
      via: "participant identity resolution for `userId + email` presence payloads"
      pattern: "email|ACTIVE|userId"
---

<objective>
Implement explicit join/leave presence, selection-range updates, and final Phase 2 verification.

Purpose: The OT and protocol layers are not enough on their own; the phase must also deliver real-time participant awareness and cursor presence while keeping the final verification bar centered on convergence and honest socket behavior.
Output: Presence models and services, explicit join/leave and presence events, cursor transformation/throttling, and the final Phase 2 regression suite.
</objective>

<execution_context>
@/Users/abdullah/.codex/get-shit-done/workflows/execute-plan.md
@/Users/abdullah/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-real-time-ot-collaboration/02-CONTEXT.md
@.planning/phases/02-real-time-ot-collaboration/02-RESEARCH.md
@.planning/phases/02-real-time-ot-collaboration/02-VALIDATION.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement explicit participant join/leave events and selection-range presence storage</name>
  <read_first>
    .planning/phases/02-real-time-ot-collaboration/02-CONTEXT.md
    .planning/phases/02-real-time-ot-collaboration/02-RESEARCH.md
    src/main/java/com/collabeditor/auth/persistence/UserRepository.java
    src/main/java/com/collabeditor/session/persistence/SessionParticipantRepository.java
    src/main/java/com/collabeditor/websocket/handler/CollaborationWebSocketHandler.java
  </read_first>
  <files>src/main/resources/application.yml, src/main/java/com/collabeditor/auth/persistence/UserRepository.java, src/main/java/com/collabeditor/session/persistence/SessionParticipantRepository.java, src/main/java/com/collabeditor/websocket/model/SelectionRange.java, src/main/java/com/collabeditor/websocket/protocol/ParticipantJoinedPayload.java, src/main/java/com/collabeditor/websocket/protocol/ParticipantLeftPayload.java, src/main/java/com/collabeditor/websocket/protocol/PresenceUpdatePayload.java, src/main/java/com/collabeditor/websocket/protocol/PresenceUpdatedPayload.java, src/main/java/com/collabeditor/websocket/service/PresenceService.java, src/main/java/com/collabeditor/websocket/handler/CollaborationWebSocketHandler.java</files>
  <action>
    Add presence support that matches the locked Phase 2 decisions. Create `SelectionRange` with exact integer fields `start` and `end`, where a caret is represented by `start == end`. Create typed payloads for `participant_joined`, `participant_left`, `update_presence`, and `presence_updated`. Implement `PresenceService` as an in-memory per-session store keyed by `sessionId` and `userId`; store the latest `SelectionRange`, last-broadcast timestamp, and participant identity. Add `app.collaboration.cursor-throttle-ms: 75` to `application.yml` and use that value as the minimum broadcast interval for one user’s presence updates. Update `CollaborationWebSocketHandler` so successful connects broadcast `participant_joined` and socket closes broadcast `participant_left`, both carrying the exact fields `userId` and `email`. Resolve `email` from the existing user store and active session membership rather than inventing a second participant directory.
  </action>
  <acceptance_criteria>
    - `rg -n "cursor-throttle-ms: 75" src/main/resources/application.yml` returns the exact throttling property.
    - `rg -n "record SelectionRange|record ParticipantJoinedPayload|record ParticipantLeftPayload|record PresenceUpdatedPayload" src/main/java/com/collabeditor/websocket/model/SelectionRange.java src/main/java/com/collabeditor/websocket/protocol/*.java` returns the range and presence payload types.
    - `rg -n "participant_joined|participant_left|userId|email|cursor-throttle-ms" src/main/java/com/collabeditor/websocket/service/PresenceService.java src/main/java/com/collabeditor/websocket/handler/CollaborationWebSocketHandler.java` returns explicit join/leave presence behavior and throttling use.
    - `./gradlew test --tests "*PresenceServiceTest"` exits with status 0.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "*PresenceServiceTest"</automated>
  </verify>
  <done>Room peers now receive explicit join/leave events and a stored, typed selection-range presence model backed by user identity from the existing auth/session data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Transform selection ranges through canonical operations and broadcast throttled presence updates</name>
  <read_first>
    .planning/research/PITFALLS.md
    src/main/java/com/collabeditor/ot/service/OperationalTransformService.java
    src/main/java/com/collabeditor/websocket/model/SelectionRange.java
    src/main/java/com/collabeditor/websocket/service/PresenceService.java
    src/test/java/com/collabeditor/websocket/PresenceServiceTest.java
  </read_first>
  <files>src/main/java/com/collabeditor/websocket/service/PresenceService.java, src/main/java/com/collabeditor/websocket/service/CollaborationSessionRegistry.java, src/main/java/com/collabeditor/websocket/handler/CollaborationWebSocketHandler.java, src/test/java/com/collabeditor/websocket/PresenceServiceTest.java, src/test/java/com/collabeditor/websocket/CollaborationWebSocketHandlerTest.java</files>
  <action>
    Extend `PresenceService` so every canonical insert or delete updates stored selection ranges before the latest presence is broadcast. For inserts before or inside a range, shift the affected edges by the inserted text length. For deletes that overlap a range, clamp the range to the surviving text so `start` and `end` never fall outside the canonical document bounds. Accept `update_presence` messages only from active room sockets, persist the latest `SelectionRange`, and broadcast `presence_updated` only when the user has crossed the configured throttle window or when a document operation transformed the stored range. Keep the latest in-memory range even when throttling suppresses an immediate outbound frame.
  </action>
  <acceptance_criteria>
    - `rg -n "update_presence|presence_updated|SelectionRange|inserted text length|clamp" src/main/java/com/collabeditor/websocket/service/PresenceService.java src/main/java/com/collabeditor/websocket/handler/CollaborationWebSocketHandler.java` returns presence-update handling and canonical range transformation logic.
    - `rg -n "throttle|75|overlap|clamp|shift" src/test/java/com/collabeditor/websocket/PresenceServiceTest.java src/test/java/com/collabeditor/websocket/CollaborationWebSocketHandlerTest.java` returns presence throttling and transform regression tests.
    - `./gradlew test --tests "*PresenceServiceTest" --tests "*CollaborationWebSocketHandlerTest"` exits with status 0.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "*PresenceServiceTest" --tests "*CollaborationWebSocketHandlerTest"</automated>
  </verify>
  <done>Presence updates remain real-time and bounded, and selection ranges stay aligned with the canonical document after edits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Finish the Phase 2 regression suite and full verification pass</name>
  <read_first>
    .planning/phases/02-real-time-ot-collaboration/02-VALIDATION.md
    src/test/java/com/collabeditor/ot/OperationalTransformServiceTest.java
    src/test/java/com/collabeditor/ot/CollaborationSessionRuntimeTest.java
    src/test/java/com/collabeditor/websocket/CollaborationWebSocketHandlerTest.java
    src/test/java/com/collabeditor/websocket/PresenceServiceTest.java
  </read_first>
  <files>src/test/java/com/collabeditor/websocket/CollaborationWebSocketHandlerTest.java, src/test/java/com/collabeditor/websocket/PresenceServiceTest.java</files>
  <action>
    Expand the final Phase 2 tests so the full phase goal is honestly verifiable. In `CollaborationWebSocketHandlerTest`, add scenarios where two or three sockets connect to the same session, exchange operations and presence updates, and all finish on the same canonical document text and revision. Add named tests that assert `participant_joined` and `participant_left` are explicit events, that the sender still receives the canonical `operation_applied` broadcast after its own `operation_ack`, and that selection ranges survive inserts and deletes. End this plan by running `./gradlew test` as the full suite gate for Phase 2.
  </action>
  <acceptance_criteria>
    - `rg -n "participant_joined|participant_left|operation_applied|operation_ack|three socket|canonical document|same revision" src/test/java/com/collabeditor/websocket/CollaborationWebSocketHandlerTest.java` returns final collaboration and presence scenarios.
    - `./gradlew test --tests "*OperationalTransformServiceTest" --tests "*CollaborationSessionRuntimeTest" --tests "*CollaborationWebSocketHandlerTest" --tests "*PresenceServiceTest"` exits with status 0.
    - `./gradlew test` exits with status 0.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "*OperationalTransformServiceTest" --tests "*CollaborationSessionRuntimeTest" --tests "*CollaborationWebSocketHandlerTest" --tests "*PresenceServiceTest"</automated>
    <automated>./gradlew test</automated>
  </verify>
  <done>Phase 2 finishes with explicit presence semantics and a full regression suite that proves convergence and socket behavior together.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `./gradlew test --tests "*PresenceServiceTest"`
- [ ] `./gradlew test --tests "*CollaborationWebSocketHandlerTest"`
- [ ] `./gradlew test --tests "*OperationalTransformServiceTest" --tests "*CollaborationSessionRuntimeTest" --tests "*CollaborationWebSocketHandlerTest" --tests "*PresenceServiceTest"`
- [ ] `./gradlew test`
</verification>

<success_criteria>
- Participants see explicit join/leave events and selection-range presence with `userId + email`.
- Presence updates stay aligned with canonical document edits and are throttled without losing the latest state.
- The full Phase 2 OT + WebSocket + presence suite is green.
</success_criteria>

<output>
After completion, create `.planning/phases/02-real-time-ot-collaboration/02-03-SUMMARY.md`
</output>
