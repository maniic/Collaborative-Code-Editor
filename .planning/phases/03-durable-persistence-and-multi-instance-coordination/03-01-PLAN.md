---
phase: 03-durable-persistence-and-multi-instance-coordination
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/resources/db/migration/V2__phase3_collaboration_persistence.sql
  - src/main/java/com/collabeditor/ot/persistence/entity/SessionOperationEntity.java
  - src/main/java/com/collabeditor/ot/persistence/SessionOperationRepository.java
  - src/main/java/com/collabeditor/snapshot/persistence/entity/DocumentStateSnapshotEntity.java
  - src/main/java/com/collabeditor/snapshot/persistence/DocumentStateSnapshotRepository.java
  - src/main/java/com/collabeditor/execution/persistence/entity/ExecutionHistoryEntity.java
  - src/main/java/com/collabeditor/execution/persistence/ExecutionHistoryRepository.java
  - src/test/java/com/collabeditor/migration/FlywayMigrationTest.java
  - src/test/java/com/collabeditor/ot/CollaborationPersistenceRepositoryTest.java
autonomous: true
requirements:
  - DATA-01
  - DATA-02
must_haves:
  truths:
    - "Phase 3 adds durable PostgreSQL tables for canonical operation history, document snapshots, and execution-history records without replacing the Phase 1 baseline schema."
    - "The canonical operation log stays append-only and uniquely ordered by `session_id + revision`."
    - "Snapshot rows store full document text at canonical revisions so later plans can rebuild from snapshot plus replay."
    - "Execution-history persistence exists as schema and repository groundwork before Phase 4 starts writing execution rows."
  artifacts:
    - path: "src/main/resources/db/migration/V2__phase3_collaboration_persistence.sql"
      provides: "Flyway schema extension for session operations, document snapshots, and execution history."
    - path: "src/main/java/com/collabeditor/ot/persistence/SessionOperationRepository.java"
      provides: "Ordered canonical-operation replay queries by session and revision."
    - path: "src/main/java/com/collabeditor/snapshot/persistence/DocumentStateSnapshotRepository.java"
      provides: "Latest-snapshot lookup queries for lazy runtime rebuild."
    - path: "src/test/java/com/collabeditor/ot/CollaborationPersistenceRepositoryTest.java"
      provides: "Repository-level proof that revision ordering and latest-snapshot queries work against PostgreSQL."
  key_links:
    - from: "src/main/resources/db/migration/V2__phase3_collaboration_persistence.sql"
      to: "src/main/resources/db/migration/V1__phase1_baseline.sql"
      via: "Phase 3 extends the existing Flyway baseline instead of replacing it"
      pattern: "CREATE TABLE|REFERENCES coding_sessions|REFERENCES users"
    - from: "src/main/java/com/collabeditor/ot/persistence/SessionOperationRepository.java"
      to: "src/main/java/com/collabeditor/ot/persistence/entity/SessionOperationEntity.java"
      via: "Ordered replay and latest-revision queries depend on the canonical revision column"
      pattern: "findBySessionIdAndRevisionGreaterThanOrderByRevisionAsc|findTopBySessionIdOrderByRevisionDesc"
    - from: "src/main/java/com/collabeditor/snapshot/persistence/DocumentStateSnapshotRepository.java"
      to: "src/main/java/com/collabeditor/snapshot/persistence/entity/DocumentStateSnapshotEntity.java"
      via: "Latest-snapshot lookup drives snapshot-plus-replay recovery"
      pattern: "findTopBySessionIdOrderByRevisionDesc|findTopBySessionIdAndRevisionLessThanEqualOrderByRevisionDesc"
---

<objective>
Establish the durable PostgreSQL schema and repository foundation for Phase 3.

Purpose: Before adding recovery or Redis coordination, the project needs an append-only canonical operation log, periodic snapshot storage, and execution-history schema that extend the existing Flyway baseline and support ordered replay.
Output: Flyway V2 migration, JPA entities and repositories for session operations, document snapshots, and execution history, plus PostgreSQL-backed schema and repository tests.
</objective>

<execution_context>
@/Users/abdullah/.codex/get-shit-done/workflows/execute-plan.md
@/Users/abdullah/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-durable-persistence-and-multi-instance-coordination/03-CONTEXT.md
@.planning/phases/03-durable-persistence-and-multi-instance-coordination/03-RESEARCH.md
@.planning/phases/03-durable-persistence-and-multi-instance-coordination/03-VALIDATION.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the Phase 3 Flyway schema for operation logs, snapshots, and execution history</name>
  <read_first>
    .planning/phases/03-durable-persistence-and-multi-instance-coordination/03-CONTEXT.md
    .planning/phases/03-durable-persistence-and-multi-instance-coordination/03-RESEARCH.md
    src/main/resources/db/migration/V1__phase1_baseline.sql
    src/test/java/com/collabeditor/migration/FlywayMigrationTest.java
    src/main/java/com/collabeditor/session/persistence/entity/CodingSessionEntity.java
  </read_first>
  <files>src/main/resources/db/migration/V2__phase3_collaboration_persistence.sql, src/main/java/com/collabeditor/ot/persistence/entity/SessionOperationEntity.java, src/main/java/com/collabeditor/snapshot/persistence/entity/DocumentStateSnapshotEntity.java, src/main/java/com/collabeditor/execution/persistence/entity/ExecutionHistoryEntity.java</files>
  <action>
    Create `src/main/resources/db/migration/V2__phase3_collaboration_persistence.sql` that adds three exact tables: `session_operations`, `document_snapshots`, and `execution_history`. `session_operations` must contain the exact columns `id UUID PRIMARY KEY`, `session_id UUID NOT NULL REFERENCES coding_sessions(id) ON DELETE CASCADE`, `revision BIGINT NOT NULL`, `author_user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE`, `client_operation_id VARCHAR(128) NOT NULL`, `operation_type VARCHAR(16) NOT NULL`, `position INTEGER NOT NULL`, `text TEXT`, `length INTEGER`, and `created_at TIMESTAMPTZ NOT NULL DEFAULT now()`. Add exact constraints so `operation_type` is only `INSERT` or `DELETE`, `position >= 0`, insert rows require non-null `text` and null `length`, and delete rows require null `text` and `length > 0`. Add exact uniqueness on `(session_id, revision)` and `(session_id, author_user_id, client_operation_id)`, plus indexes on `(session_id, revision)` and `(session_id, created_at)`. `document_snapshots` must contain `id UUID PRIMARY KEY`, `session_id UUID NOT NULL REFERENCES coding_sessions(id) ON DELETE CASCADE`, `revision BIGINT NOT NULL`, `document TEXT NOT NULL`, `created_at TIMESTAMPTZ NOT NULL DEFAULT now()`, unique `(session_id, revision)`, and an index on `(session_id, revision DESC)`. `execution_history` must contain `id UUID PRIMARY KEY`, `session_id UUID NOT NULL REFERENCES coding_sessions(id) ON DELETE CASCADE`, `requested_by_user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE`, `language VARCHAR(16) NOT NULL`, `source_revision BIGINT NOT NULL`, `status VARCHAR(16) NOT NULL`, `stdout TEXT`, `stderr TEXT`, `exit_code INTEGER`, `created_at TIMESTAMPTZ NOT NULL DEFAULT now()`, `started_at TIMESTAMPTZ`, and `finished_at TIMESTAMPTZ`. Mirror those exact table and column names in new JPA entities `SessionOperationEntity`, `DocumentStateSnapshotEntity`, and `ExecutionHistoryEntity`.
  </action>
  <acceptance_criteria>
    - `rg -n "CREATE TABLE session_operations|CREATE TABLE document_snapshots|CREATE TABLE execution_history" src/main/resources/db/migration/V2__phase3_collaboration_persistence.sql` returns all three tables.
    - `rg -n "uq_session_operations_session_revision|uq_session_operations_client_op|uq_document_snapshots_session_revision" src/main/resources/db/migration/V2__phase3_collaboration_persistence.sql` returns the exact unique constraints.
    - `rg -n "@Table\\(name = \"session_operations\"\\)|@Table\\(name = \"document_snapshots\"\\)|@Table\\(name = \"execution_history\"\\)" src/main/java/com/collabeditor/ot/persistence/entity/SessionOperationEntity.java src/main/java/com/collabeditor/snapshot/persistence/entity/DocumentStateSnapshotEntity.java src/main/java/com/collabeditor/execution/persistence/entity/ExecutionHistoryEntity.java` returns all three entity mappings.
    - `./gradlew test --tests "*FlywayMigrationTest"` exits with status 0.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "*FlywayMigrationTest"</automated>
  </verify>
  <done>The Phase 3 durable schema exists in Flyway and the repo has typed JPA entities for the new persistence tables.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add ordered replay, latest snapshot, and execution-history repository interfaces</name>
  <read_first>
    .planning/phases/03-durable-persistence-and-multi-instance-coordination/03-CONTEXT.md
    .planning/phases/03-durable-persistence-and-multi-instance-coordination/03-RESEARCH.md
    src/main/java/com/collabeditor/ot/persistence/entity/SessionOperationEntity.java
    src/main/java/com/collabeditor/snapshot/persistence/entity/DocumentStateSnapshotEntity.java
    src/main/java/com/collabeditor/execution/persistence/entity/ExecutionHistoryEntity.java
    src/main/java/com/collabeditor/session/persistence/CodingSessionRepository.java
  </read_first>
  <files>src/main/java/com/collabeditor/ot/persistence/SessionOperationRepository.java, src/main/java/com/collabeditor/snapshot/persistence/DocumentStateSnapshotRepository.java, src/main/java/com/collabeditor/execution/persistence/ExecutionHistoryRepository.java, src/test/java/com/collabeditor/ot/CollaborationPersistenceRepositoryTest.java</files>
  <action>
    Create `SessionOperationRepository` with the exact methods `List<SessionOperationEntity> findBySessionIdAndRevisionGreaterThanOrderByRevisionAsc(UUID sessionId, long revision)`, `Optional<SessionOperationEntity> findTopBySessionIdOrderByRevisionDesc(UUID sessionId)`, and `Optional<SessionOperationEntity> findBySessionIdAndRevision(UUID sessionId, long revision)`. Create `DocumentStateSnapshotRepository` with the exact methods `Optional<DocumentStateSnapshotEntity> findTopBySessionIdOrderByRevisionDesc(UUID sessionId)` and `Optional<DocumentStateSnapshotEntity> findTopBySessionIdAndRevisionLessThanEqualOrderByRevisionDesc(UUID sessionId, long revision)`. Create `ExecutionHistoryRepository` with the exact method `List<ExecutionHistoryEntity> findBySessionIdOrderByCreatedAtDesc(UUID sessionId)`. Add `CollaborationPersistenceRepositoryTest` as a PostgreSQL-backed integration test that inserts operation rows for one `session_id`, asserts `findBySessionIdAndRevisionGreaterThanOrderByRevisionAsc(...)` returns revisions in ascending numeric order, asserts the snapshot repository returns the latest snapshot and the latest snapshot below a requested revision, and asserts the execution-history repository returns newest-first ordering by `created_at`.
  </action>
  <acceptance_criteria>
    - `rg -n "findBySessionIdAndRevisionGreaterThanOrderByRevisionAsc|findTopBySessionIdOrderByRevisionDesc|findBySessionIdAndRevision\\(" src/main/java/com/collabeditor/ot/persistence/SessionOperationRepository.java` returns the exact replay queries.
    - `rg -n "findTopBySessionIdOrderByRevisionDesc|findTopBySessionIdAndRevisionLessThanEqualOrderByRevisionDesc" src/main/java/com/collabeditor/snapshot/persistence/DocumentStateSnapshotRepository.java` returns the exact snapshot lookup queries.
    - `rg -n "findBySessionIdOrderByCreatedAtDesc" src/main/java/com/collabeditor/execution/persistence/ExecutionHistoryRepository.java` returns the execution-history query.
    - `rg -n "ascending|latest snapshot|below a revision|createdAt" src/test/java/com/collabeditor/ot/CollaborationPersistenceRepositoryTest.java` returns repository coverage for replay ordering, snapshot lookup, and execution-history ordering.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "*CollaborationPersistenceRepositoryTest"</automated>
  </verify>
  <done>The project has exact repository APIs for ordered replay and latest-snapshot lookup, and those queries are verified against PostgreSQL.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend migration verification to cover Phase 3 durability constraints</name>
  <read_first>
    .planning/phases/03-durable-persistence-and-multi-instance-coordination/03-VALIDATION.md
    src/main/resources/db/migration/V2__phase3_collaboration_persistence.sql
    src/test/java/com/collabeditor/migration/FlywayMigrationTest.java
    src/test/java/com/collabeditor/ot/CollaborationPersistenceRepositoryTest.java
  </read_first>
  <files>src/test/java/com/collabeditor/migration/FlywayMigrationTest.java, src/test/java/com/collabeditor/ot/CollaborationPersistenceRepositoryTest.java</files>
  <action>
    Extend `FlywayMigrationTest` so it asserts the new Phase 3 schema contract, not just table existence. Add explicit checks that `session_operations` contains the columns `revision`, `author_user_id`, `client_operation_id`, `operation_type`, `position`, `text`, and `length`; that `document_snapshots` contains `revision` and `document`; and that `execution_history` contains `requested_by_user_id`, `source_revision`, `status`, `stdout`, `stderr`, and `exit_code`. Add a uniqueness test proving `session_operations` rejects two rows with the same `(session_id, revision)`. Add a constraint test proving an `INSERT` operation row cannot set `length` and a `DELETE` operation row cannot omit `length`. Keep `CollaborationPersistenceRepositoryTest` focused on query ordering and latest-snapshot selection so the plan ends with both migration-level and repository-level proof.
  </action>
  <acceptance_criteria>
    - `rg -n "session_operations|document_snapshots|execution_history|same \\(session_id, revision\\)|INSERT operation row|DELETE operation row" src/test/java/com/collabeditor/migration/FlywayMigrationTest.java` returns the new migration assertions.
    - `./gradlew test --tests "*FlywayMigrationTest" --tests "*CollaborationPersistenceRepositoryTest"` exits with status 0.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "*FlywayMigrationTest" --tests "*CollaborationPersistenceRepositoryTest"</automated>
  </verify>
  <done>Phase 3 ends wave 1 with durable collaboration tables, exact replay queries, and PostgreSQL-backed proof that the schema enforces the intended invariants.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `./gradlew test --tests "*FlywayMigrationTest"`
- [ ] `./gradlew test --tests "*CollaborationPersistenceRepositoryTest"`
- [ ] `./gradlew test --tests "*FlywayMigrationTest" --tests "*CollaborationPersistenceRepositoryTest"`
</verification>

<success_criteria>
- PostgreSQL has durable Phase 3 tables for canonical operations, document snapshots, and execution history.
- Repository queries can replay canonical operations in revision order and fetch the latest snapshot for a session.
- The Flyway and repository tests prove the new durable schema behaves correctly against PostgreSQL.
</success_criteria>

<output>
After completion, create `.planning/phases/03-durable-persistence-and-multi-instance-coordination/03-01-SUMMARY.md`
</output>
