---
phase: 03-durable-persistence-and-multi-instance-coordination
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/main/java/com/collabeditor/ot/service/CollaborationSessionRuntime.java
  - src/main/java/com/collabeditor/ot/service/CollaborationPersistenceService.java
  - src/main/java/com/collabeditor/snapshot/service/SnapshotRecoveryService.java
  - src/test/java/com/collabeditor/ot/CollaborationSessionRuntimeTest.java
  - src/test/java/com/collabeditor/ot/CollaborationPersistenceServiceTest.java
autonomous: true
requirements:
  - DATA-01
  - DATA-02
must_haves:
  truths:
    - "Accepted canonical operations are durable before later plans wire them into ack or broadcast flows."
    - "Snapshots are written on exact canonical revisions `50`, `100`, `150`, and so on, not on approximate batch counts."
    - "A collaboration runtime can be rebuilt lazily from the latest stored snapshot plus later canonical operations."
    - "The Phase 2 OT runtime remains the single document-mutation engine; Phase 3 adds persistence and hydration around it rather than replacing it."
  artifacts:
    - path: "src/main/java/com/collabeditor/ot/service/CollaborationPersistenceService.java"
      provides: "Durable append and snapshot write service for accepted canonical operations."
    - path: "src/main/java/com/collabeditor/snapshot/service/SnapshotRecoveryService.java"
      provides: "Lazy runtime rebuild from latest snapshot plus replay."
    - path: "src/test/java/com/collabeditor/ot/CollaborationPersistenceServiceTest.java"
      provides: "PostgreSQL-backed proof of snapshot cadence and durable replay recovery."
  key_links:
    - from: "src/main/java/com/collabeditor/ot/service/CollaborationPersistenceService.java"
      to: "src/main/java/com/collabeditor/ot/persistence/SessionOperationRepository.java"
      via: "Accepted canonical operations append to the durable operation log"
      pattern: "save|sessionId|revision|clientOperationId"
    - from: "src/main/java/com/collabeditor/snapshot/service/SnapshotRecoveryService.java"
      to: "src/main/java/com/collabeditor/snapshot/persistence/DocumentStateSnapshotRepository.java"
      via: "Latest snapshot lookup anchors lazy rebuild"
      pattern: "findTopBySessionIdOrderByRevisionDesc|findTopBySessionIdAndRevisionLessThanEqualOrderByRevisionDesc"
    - from: "src/main/java/com/collabeditor/ot/service/CollaborationSessionRuntime.java"
      to: "src/main/java/com/collabeditor/ot/model/AppliedOperation.java"
      via: "Hydrated runtime must restore canonical history needed for stale-op transforms"
      pattern: "restore|history|ApplyResult"
---

<objective>
Implement the durable append, snapshot cadence, and lazy rebuild services for Phase 3.

Purpose: Once the durable schema exists, the server needs a write path that persists canonical operations and snapshots plus a read path that can rebuild room state from the latest snapshot plus replay without preloading every session at startup.
Output: Hydration-capable runtime support, persistence and recovery services, and PostgreSQL-backed tests for snapshot cadence and rebuild correctness.
</objective>

<execution_context>
@/Users/abdullah/.codex/get-shit-done/workflows/execute-plan.md
@/Users/abdullah/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-durable-persistence-and-multi-instance-coordination/03-CONTEXT.md
@.planning/phases/03-durable-persistence-and-multi-instance-coordination/03-RESEARCH.md
@.planning/phases/03-durable-persistence-and-multi-instance-coordination/03-VALIDATION.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make the canonical runtime restorable from persisted document state and history</name>
  <read_first>
    .planning/phases/03-durable-persistence-and-multi-instance-coordination/03-CONTEXT.md
    .planning/phases/03-durable-persistence-and-multi-instance-coordination/03-RESEARCH.md
    src/main/java/com/collabeditor/ot/service/CollaborationSessionRuntime.java
    src/main/java/com/collabeditor/ot/model/AppliedOperation.java
    src/main/java/com/collabeditor/ot/model/DocumentSnapshot.java
    src/test/java/com/collabeditor/ot/CollaborationSessionRuntimeTest.java
  </read_first>
  <files>src/main/java/com/collabeditor/ot/service/CollaborationSessionRuntime.java, src/test/java/com/collabeditor/ot/CollaborationSessionRuntimeTest.java</files>
  <action>
    Refactor `CollaborationSessionRuntime` so it can be restored from durable state instead of only starting empty. Add the exact static factory `restore(UUID sessionId, String document, long revision, List<AppliedOperation> history, OperationalTransformService otService)` that initializes the runtime with a preexisting document string, canonical revision, and in-memory history list. Preserve the existing empty-room constructor for Phase 2 behavior, but make `restore(...)` the explicit entry point for lazy rebuilds. Keep the exact internal fields `StringBuilder document`, `long revision`, `List<AppliedOperation> history`, and `ReentrantLock`. Update `CollaborationSessionRuntimeTest` with named coverage that a restored runtime reports the provided document and revision immediately, still transforms stale operations against restored history, and keeps deterministic convergence after a restore-then-apply sequence.
  </action>
  <acceptance_criteria>
    - `rg -n "static CollaborationSessionRuntime restore|String document, long revision, List<AppliedOperation> history" src/main/java/com/collabeditor/ot/service/CollaborationSessionRuntime.java` returns the exact restore factory.
    - `rg -n "restore|restored runtime|stale operations against restored history" src/test/java/com/collabeditor/ot/CollaborationSessionRuntimeTest.java` returns restore-focused runtime coverage.
    - `./gradlew test --tests "*CollaborationSessionRuntimeTest"` exits with status 0.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "*CollaborationSessionRuntimeTest"</automated>
  </verify>
  <done>The OT runtime can now be instantiated from durable state without losing its stale-op transform guarantees.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement durable append and snapshot-plus-replay recovery services</name>
  <read_first>
    .planning/phases/03-durable-persistence-and-multi-instance-coordination/03-CONTEXT.md
    .planning/phases/03-durable-persistence-and-multi-instance-coordination/03-RESEARCH.md
    src/main/java/com/collabeditor/ot/persistence/SessionOperationRepository.java
    src/main/java/com/collabeditor/snapshot/persistence/DocumentStateSnapshotRepository.java
    src/main/java/com/collabeditor/ot/service/CollaborationSessionRuntime.java
  </read_first>
  <files>src/main/java/com/collabeditor/ot/service/CollaborationPersistenceService.java, src/main/java/com/collabeditor/snapshot/service/SnapshotRecoveryService.java, src/test/java/com/collabeditor/ot/CollaborationPersistenceServiceTest.java</files>
  <action>
    Create `CollaborationPersistenceService` with the exact write methods `appendAcceptedOperation(UUID sessionId, TextOperation canonicalOperation, long revision, DocumentSnapshot snapshot)` and `recordSnapshot(UUID sessionId, DocumentSnapshot snapshot)`. `appendAcceptedOperation(...)` must persist one `session_operations` row for the canonical operation and call `recordSnapshot(...)` when the accepted canonical `revision % 50 == 0`. `recordSnapshot(...)` must write one `document_snapshots` row containing the full canonical `snapshot.document()` and `snapshot.revision()`. Create `SnapshotRecoveryService` with the exact read method `loadRuntime(UUID sessionId)` that finds the latest snapshot for the session, loads later operations using `findBySessionIdAndRevisionGreaterThanOrderByRevisionAsc(...)`, converts those rows into `AppliedOperation` values, and returns `CollaborationSessionRuntime.restore(...)`. If no snapshot exists, `loadRuntime(...)` must rebuild from an empty document, revision `0`, and the full ordered operation list. Do not preload all sessions at startup; the only rebuild API in this plan is the explicit on-demand `loadRuntime(UUID sessionId)` call.
  </action>
  <acceptance_criteria>
    - `rg -n "appendAcceptedOperation|recordSnapshot|revision % 50 == 0|session_operations|document_snapshots" src/main/java/com/collabeditor/ot/service/CollaborationPersistenceService.java` returns the exact append and snapshot cadence logic.
    - `rg -n "loadRuntime\\(UUID sessionId\\)|findTopBySessionIdOrderByRevisionDesc|findBySessionIdAndRevisionGreaterThanOrderByRevisionAsc|CollaborationSessionRuntime\\.restore" src/main/java/com/collabeditor/snapshot/service/SnapshotRecoveryService.java` returns the lazy rebuild flow.
    - `./gradlew test --tests "*CollaborationPersistenceServiceTest"` exits with status 0.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "*CollaborationPersistenceServiceTest"</automated>
  </verify>
  <done>The server has explicit durable append and lazy runtime rebuild services centered on snapshot-plus-replay.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add PostgreSQL-backed tests for snapshot cadence and rebuild correctness</name>
  <read_first>
    .planning/phases/03-durable-persistence-and-multi-instance-coordination/03-VALIDATION.md
    src/test/java/com/collabeditor/migration/FlywayMigrationTest.java
    src/test/java/com/collabeditor/ot/CollaborationPersistenceRepositoryTest.java
    src/main/java/com/collabeditor/ot/service/CollaborationPersistenceService.java
    src/main/java/com/collabeditor/snapshot/service/SnapshotRecoveryService.java
  </read_first>
  <files>src/test/java/com/collabeditor/ot/CollaborationPersistenceServiceTest.java</files>
  <action>
    Implement `CollaborationPersistenceServiceTest` as a PostgreSQLContainer-backed integration suite. Add one named test that appends canonical operations until revision `50` and asserts exactly one snapshot row exists at revision `50`, then repeats through revision `100` and asserts a second snapshot row exists at `100`. Add one named test that seeds a snapshot at revision `50`, appends later operations through revision `53`, calls `SnapshotRecoveryService.loadRuntime(sessionId)`, and asserts the rebuilt runtime returns the expected full document text and canonical revision `53`. Add one named test that rebuilds a room with no snapshot rows and proves replay starts from revision `0`. Keep the assertions on exact revision numbers and expected final document strings so the tests catch off-by-one or missing-replay regressions.
  </action>
  <acceptance_criteria>
    - `rg -n "revision 50|revision 100|loadRuntime|no snapshot rows|canonical revision 53" src/test/java/com/collabeditor/ot/CollaborationPersistenceServiceTest.java` returns the exact snapshot-cadence and rebuild scenarios.
    - `./gradlew test --tests "*CollaborationPersistenceServiceTest" --tests "*CollaborationSessionRuntimeTest"` exits with status 0.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "*CollaborationPersistenceServiceTest" --tests "*CollaborationSessionRuntimeTest"</automated>
  </verify>
  <done>Wave 2 finishes with durable append and lazy rebuild behavior proven against PostgreSQL and the real OT runtime.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `./gradlew test --tests "*CollaborationSessionRuntimeTest"`
- [ ] `./gradlew test --tests "*CollaborationPersistenceServiceTest"`
- [ ] `./gradlew test --tests "*CollaborationPersistenceServiceTest" --tests "*CollaborationSessionRuntimeTest"`
</verification>

<success_criteria>
- Canonical operations are persisted in PostgreSQL before later phases wire them into ack and relay flows.
- Snapshot rows are created exactly on every 50th canonical revision.
- A session runtime can be rebuilt lazily from latest snapshot plus replay or from full replay when no snapshot exists.
</success_criteria>

<output>
After completion, create `.planning/phases/03-durable-persistence-and-multi-instance-coordination/03-02-SUMMARY.md`
</output>
