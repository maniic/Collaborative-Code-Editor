---
phase: 03-durable-persistence-and-multi-instance-coordination
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - build.gradle.kts
  - src/main/resources/application.yml
  - src/test/resources/application-test.yml
  - src/main/java/com/collabeditor/redis/config/RedisCollaborationProperties.java
  - src/main/java/com/collabeditor/redis/config/RedisConfig.java
  - src/main/java/com/collabeditor/redis/model/CanonicalEventType.java
  - src/main/java/com/collabeditor/redis/model/CanonicalCollaborationEvent.java
  - src/main/java/com/collabeditor/redis/service/SessionCoordinationService.java
  - src/main/java/com/collabeditor/redis/service/CollaborationRelayService.java
  - src/test/java/com/collabeditor/redis/SessionCoordinationServiceTest.java
  - src/test/java/com/collabeditor/redis/CollaborationRelayServiceTest.java
autonomous: true
requirements:
  - DATA-03
  - DATA-04
must_haves:
  truths:
    - "Redis coordinates active-session serialization and revision mirroring, but PostgreSQL remains the durable source of truth."
    - "Per-session Redis keys and channels are deterministic and shared across all instances."
    - "Canonical collaboration events relay through a single Redis pub/sub path that all instances can consume."
    - "Redis inconsistency is recoverable because later plans can rebuild from PostgreSQL snapshot plus replay."
  artifacts:
    - path: "src/main/java/com/collabeditor/redis/service/SessionCoordinationService.java"
      provides: "Per-session locking, revision mirror management, and active-session bookkeeping."
    - path: "src/main/java/com/collabeditor/redis/service/CollaborationRelayService.java"
      provides: "Canonical Redis pub/sub publish and subscribe behavior for collaboration events."
    - path: "src/test/java/com/collabeditor/redis/SessionCoordinationServiceTest.java"
      provides: "Redis-backed proof of lock, TTL, and revision coordination behavior."
    - path: "src/test/java/com/collabeditor/redis/CollaborationRelayServiceTest.java"
      provides: "Redis-backed proof that canonical collaboration events publish and subscribe correctly."
  key_links:
    - from: "src/main/java/com/collabeditor/redis/service/SessionCoordinationService.java"
      to: "src/main/java/com/collabeditor/ot/service/CollaborationPersistenceService.java"
      via: "Redis revision mirrors initialize from durable PostgreSQL state rather than inventing new canonical revisions"
      pattern: "initializeRevisionIfAbsent|setRevision|getRevision"
    - from: "src/main/java/com/collabeditor/redis/service/CollaborationRelayService.java"
      to: "src/main/java/com/collabeditor/redis/model/CanonicalCollaborationEvent.java"
      via: "Publish and subscribe operate on one typed canonical relay payload"
      pattern: "publish|subscribe|payloadJson|eventType"
    - from: "build.gradle.kts"
      to: "src/main/java/com/collabeditor/redis/config/RedisConfig.java"
      via: "Redis coordination depends on the Boot-managed Lettuce stack"
      pattern: "spring-boot-starter-data-redis"
---

<objective>
Introduce Redis coordination and canonical collaboration relay services for Phase 3.

Purpose: After durable persistence and lazy rebuild exist, the server needs a shared per-session coordination layer and a canonical relay path so multiple backend instances cannot assign competing revisions or broadcast divergent collaboration events.
Output: Redis starter/configuration, deterministic key and channel conventions, coordination and relay services, and Redis-backed regression tests.
</objective>

<execution_context>
@/Users/abdullah/.codex/get-shit-done/workflows/execute-plan.md
@/Users/abdullah/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-durable-persistence-and-multi-instance-coordination/03-CONTEXT.md
@.planning/phases/03-durable-persistence-and-multi-instance-coordination/03-RESEARCH.md
@.planning/phases/03-durable-persistence-and-multi-instance-coordination/03-VALIDATION.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the Redis stack and exact collaboration coordination properties</name>
  <read_first>
    .planning/phases/03-durable-persistence-and-multi-instance-coordination/03-CONTEXT.md
    .planning/phases/03-durable-persistence-and-multi-instance-coordination/03-RESEARCH.md
    build.gradle.kts
    src/main/resources/application.yml
    src/test/resources/application-test.yml
  </read_first>
  <files>build.gradle.kts, src/main/resources/application.yml, src/test/resources/application-test.yml, src/main/java/com/collabeditor/redis/config/RedisCollaborationProperties.java, src/main/java/com/collabeditor/redis/config/RedisConfig.java</files>
  <action>
    Add `implementation("org.springframework.boot:spring-boot-starter-data-redis")` to `build.gradle.kts`. Introduce `RedisCollaborationProperties` under the exact prefix `app.collaboration.redis` with the exact fields `String keyPrefix`, `long lockTtlMs`, and `long sessionTtlSeconds`. Set the production defaults in `application.yml` to `app.collaboration.redis.key-prefix: collab`, `app.collaboration.redis.lock-ttl-ms: 5000`, and `app.collaboration.redis.session-ttl-seconds: 1800`. Add `spring.data.redis.host` and `spring.data.redis.port` placeholders in `application.yml` and `application-test.yml` so Redis-backed tests can override them dynamically. Create `RedisConfig` that exposes a `StringRedisTemplate`, a Jackson-backed serializer for relay payloads, and a `RedisMessageListenerContainer` ready to subscribe to collaboration channels.
  </action>
  <acceptance_criteria>
    - `rg -n "spring-boot-starter-data-redis" build.gradle.kts` returns the Redis starter dependency.
    - `rg -n "key-prefix: collab|lock-ttl-ms: 5000|session-ttl-seconds: 1800" src/main/resources/application.yml` returns the exact Redis collaboration properties.
    - `rg -n "redis:" src/main/resources/application.yml src/test/resources/application-test.yml` returns Redis configuration blocks in both files, and `rg -n "host:|port:" src/main/resources/application.yml src/test/resources/application-test.yml` returns Redis host/port keys.
    - `rg -n "@ConfigurationProperties\\(prefix = \"app\\.collaboration\\.redis\"\\)|StringRedisTemplate|RedisMessageListenerContainer" src/main/java/com/collabeditor/redis/config/RedisCollaborationProperties.java src/main/java/com/collabeditor/redis/config/RedisConfig.java` returns the exact properties and config beans.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "*SessionCoordinationServiceTest"</automated>
  </verify>
  <done>The repo can now construct Redis-backed coordination services with explicit, versioned collaboration configuration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic per-session locking and revision coordination in Redis</name>
  <read_first>
    .planning/phases/03-durable-persistence-and-multi-instance-coordination/03-CONTEXT.md
    .planning/phases/03-durable-persistence-and-multi-instance-coordination/03-RESEARCH.md
    src/main/java/com/collabeditor/redis/config/RedisCollaborationProperties.java
    src/main/java/com/collabeditor/ot/service/CollaborationPersistenceService.java
    src/test/resources/application-test.yml
  </read_first>
  <files>src/main/java/com/collabeditor/redis/service/SessionCoordinationService.java, src/test/java/com/collabeditor/redis/SessionCoordinationServiceTest.java</files>
  <action>
    Implement `SessionCoordinationService` with deterministic keys derived from the exact patterns `collab:session:{sessionId}:lock`, `collab:session:{sessionId}:revision`, and `collab:session:{sessionId}:active`. Add the exact public methods `withSessionLock(UUID sessionId, Supplier<T> criticalSection)`, `initializeRevisionIfAbsent(UUID sessionId, long durableRevision)`, `setRevision(UUID sessionId, long revision)`, `OptionalLong getRevision(UUID sessionId)`, `markSessionActive(UUID sessionId)`, and `markSessionInactive(UUID sessionId)`. `withSessionLock(...)` must use a random token plus `SET NX PX` semantics with the configured `lockTtlMs`, and release the lock only when the stored token matches the current owner. `markSessionActive(...)` and `markSessionInactive(...)` must use the configured `sessionTtlSeconds`. `SessionCoordinationServiceTest` must use a real Redis container to assert lock exclusivity, revision initialization from a durable value, revision overwrite to a newer value, and TTL-backed active-session bookkeeping.
  </action>
  <acceptance_criteria>
    - `rg -n "collab:session:\\{sessionId\\}:lock|collab:session:\\{sessionId\\}:revision|collab:session:\\{sessionId\\}:active" src/main/java/com/collabeditor/redis/service/SessionCoordinationService.java` returns the exact key patterns.
    - `rg -n "withSessionLock|initializeRevisionIfAbsent|setRevision|getRevision|markSessionActive|markSessionInactive" src/main/java/com/collabeditor/redis/service/SessionCoordinationService.java` returns the exact public API.
    - `rg -n "lock exclusivity|revision initialization|TTL|active session" src/test/java/com/collabeditor/redis/SessionCoordinationServiceTest.java` returns the Redis-backed coordination scenarios.
    - `./gradlew test --tests "*SessionCoordinationServiceTest"` exits with status 0.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "*SessionCoordinationServiceTest"</automated>
  </verify>
  <done>The project now has a deterministic Redis coordination primitive for per-session apply serialization and revision mirroring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement the canonical Redis pub/sub relay for collaboration events</name>
  <read_first>
    .planning/phases/03-durable-persistence-and-multi-instance-coordination/03-CONTEXT.md
    .planning/phases/03-durable-persistence-and-multi-instance-coordination/03-RESEARCH.md
    src/main/java/com/collabeditor/redis/config/RedisConfig.java
    src/main/java/com/collabeditor/websocket/protocol/ServerMessageType.java
    src/test/java/com/collabeditor/websocket/CollaborationWebSocketHandlerTest.java
  </read_first>
  <files>src/main/java/com/collabeditor/redis/model/CanonicalEventType.java, src/main/java/com/collabeditor/redis/model/CanonicalCollaborationEvent.java, src/main/java/com/collabeditor/redis/service/CollaborationRelayService.java, src/test/java/com/collabeditor/redis/CollaborationRelayServiceTest.java</files>
  <action>
    Create `CanonicalEventType` with the exact enum members `OPERATION_APPLIED`, `PARTICIPANT_JOINED`, `PARTICIPANT_LEFT`, and `PRESENCE_UPDATED`. Create `CanonicalCollaborationEvent` with the exact fields `UUID sessionId`, `CanonicalEventType eventType`, `long revision`, `UUID userId`, `String payloadJson`, and `Instant emittedAt`. Implement `CollaborationRelayService` so it publishes every canonical event to the exact Redis channel `collab:session:{sessionId}:events` and lets callers register a callback-based subscriber for that same channel pattern. Serialize and deserialize relay payloads with Jackson rather than ad hoc string splitting. `CollaborationRelayServiceTest` must use a real Redis container and prove that a published `OPERATION_APPLIED` event is observed by a subscriber, that participant and presence event types round-trip without losing `payloadJson`, and that the session-specific channel naming prevents one session's event from being delivered to another session's subscriber.
  </action>
  <acceptance_criteria>
    - `rg -n "OPERATION_APPLIED|PARTICIPANT_JOINED|PARTICIPANT_LEFT|PRESENCE_UPDATED" src/main/java/com/collabeditor/redis/model/CanonicalEventType.java` returns all four canonical relay event types.
    - `rg -n "UUID sessionId|CanonicalEventType eventType|long revision|String payloadJson|Instant emittedAt" src/main/java/com/collabeditor/redis/model/CanonicalCollaborationEvent.java` returns the exact relay payload fields.
    - `rg -n "collab:session:|events|publish|subscribe" src/main/java/com/collabeditor/redis/service/CollaborationRelayService.java` returns the exact channel naming and relay API.
    - `./gradlew test --tests "*CollaborationRelayServiceTest"` exits with status 0.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "*CollaborationRelayServiceTest"</automated>
  </verify>
  <done>Wave 3 ends with Redis-backed locking, revision coordination, and one canonical relay path for collaboration events.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `./gradlew test --tests "*SessionCoordinationServiceTest"`
- [ ] `./gradlew test --tests "*CollaborationRelayServiceTest"`
- [ ] `./gradlew test --tests "*SessionCoordinationServiceTest" --tests "*CollaborationRelayServiceTest"`
</verification>

<success_criteria>
- Redis coordinates per-session apply sequencing and revision tracking without becoming the durable source of truth.
- Canonical collaboration events relay through deterministic session channels.
- The Redis coordination and relay services are verified against a real Redis container.
</success_criteria>

<output>
After completion, create `.planning/phases/03-durable-persistence-and-multi-instance-coordination/03-03-SUMMARY.md`
</output>
