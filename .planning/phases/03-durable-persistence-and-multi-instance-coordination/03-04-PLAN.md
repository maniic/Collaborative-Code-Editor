---
phase: 03-durable-persistence-and-multi-instance-coordination
plan: 04
type: execute
wave: 4
depends_on:
  - 03-02
  - 03-03
files_modified:
  - src/main/java/com/collabeditor/websocket/service/CollaborationSessionRegistry.java
  - src/main/java/com/collabeditor/websocket/service/DistributedCollaborationGateway.java
  - src/main/java/com/collabeditor/websocket/service/PresenceService.java
  - src/main/java/com/collabeditor/websocket/handler/CollaborationWebSocketHandler.java
  - src/test/java/com/collabeditor/websocket/CollaborationWebSocketHandlerTest.java
  - src/test/java/com/collabeditor/websocket/DistributedCollaborationWebSocketHandlerTest.java
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
must_haves:
  truths:
    - "A reconnect after restart bootstraps from persisted document state rather than starting from an empty room."
    - "The accepted-operation path persists first, then synchronizes the Redis revision mirror, then publishes the canonical event, then acknowledges the sender."
    - "All local sockets consume the same canonical relay path for operations and presence events; the origin instance does not bypass Redis."
    - "Revision gaps or relay inconsistencies trigger runtime eviction plus rebuild or `resync_required` rather than silent divergence."
  artifacts:
    - path: "src/main/java/com/collabeditor/websocket/service/DistributedCollaborationGateway.java"
      provides: "Bridge from the existing raw WebSocket contract to durable persistence and Redis coordination."
    - path: "src/main/java/com/collabeditor/websocket/handler/CollaborationWebSocketHandler.java"
      provides: "Stable Phase 2 websocket contract backed by durable and distributed collaboration internals."
    - path: "src/test/java/com/collabeditor/websocket/DistributedCollaborationWebSocketHandlerTest.java"
      provides: "Distributed handler proof for relay, rebuild, and resync behavior."
  key_links:
    - from: "src/main/java/com/collabeditor/websocket/service/DistributedCollaborationGateway.java"
      to: "src/main/java/com/collabeditor/ot/service/CollaborationPersistenceService.java"
      via: "Acks and broadcasts must wait for durable append and snapshot handling"
      pattern: "appendAcceptedOperation|recordSnapshot|loadRuntime"
    - from: "src/main/java/com/collabeditor/websocket/service/DistributedCollaborationGateway.java"
      to: "src/main/java/com/collabeditor/redis/service/SessionCoordinationService.java"
      via: "Per-session coordination wraps the canonical apply path"
      pattern: "withSessionLock|setRevision|getRevision"
    - from: "src/main/java/com/collabeditor/websocket/handler/CollaborationWebSocketHandler.java"
      to: "src/main/java/com/collabeditor/redis/service/CollaborationRelayService.java"
      via: "Local socket fan-out must follow the canonical Redis relay path"
      pattern: "publish|subscribe|operation_applied|participant_joined|presence_updated"
---

<objective>
Rewire the WebSocket collaboration flow to use durable recovery and Redis-backed canonical relay behavior.

Purpose: Phase 3 is only complete when the existing raw WebSocket contract is backed by PostgreSQL durability and Redis coordination, including restart recovery, relay-driven local fan-out, and safe rebuild or resync on revision gaps.
Output: Local-socket registry refactor, distributed collaboration gateway, durable and coordinated handler flow, and final distributed WebSocket regression coverage.
</objective>

<execution_context>
@/Users/abdullah/.codex/get-shit-done/workflows/execute-plan.md
@/Users/abdullah/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-durable-persistence-and-multi-instance-coordination/03-CONTEXT.md
@.planning/phases/03-durable-persistence-and-multi-instance-coordination/03-RESEARCH.md
@.planning/phases/03-durable-persistence-and-multi-instance-coordination/03-VALIDATION.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor local runtime and socket tracking around lazy durable rebuilds</name>
  <read_first>
    .planning/phases/03-durable-persistence-and-multi-instance-coordination/03-CONTEXT.md
    .planning/phases/03-durable-persistence-and-multi-instance-coordination/03-RESEARCH.md
    src/main/java/com/collabeditor/websocket/service/CollaborationSessionRegistry.java
    src/main/java/com/collabeditor/websocket/handler/CollaborationWebSocketHandler.java
    src/main/java/com/collabeditor/snapshot/service/SnapshotRecoveryService.java
    src/main/java/com/collabeditor/redis/service/SessionCoordinationService.java
  </read_first>
  <files>src/main/java/com/collabeditor/websocket/service/CollaborationSessionRegistry.java, src/main/java/com/collabeditor/websocket/service/DistributedCollaborationGateway.java</files>
  <action>
    Refactor `CollaborationSessionRegistry` so it no longer unconditionally creates a brand-new `CollaborationSessionRuntime` for every `sessionId`. Replace the exact method `getOrCreateRuntime(UUID sessionId)` with the exact methods `Optional<CollaborationSessionRuntime> getRuntimeIfPresent(UUID sessionId)`, `void cacheRuntime(UUID sessionId, CollaborationSessionRuntime runtime)`, and `void evictRuntime(UUID sessionId)`, while keeping `addSocket`, `removeSocket`, and `getSockets` for local socket tracking. Create `DistributedCollaborationGateway` as the new orchestration service with the exact public methods `DocumentSnapshot connectSnapshot(UUID sessionId)`, `SubmissionResult submitOperation(UUID sessionId, UUID userId, SubmitOperationPayload payload)`, `void publishParticipantJoined(UUID sessionId, UUID userId, String email)`, `void publishParticipantLeft(UUID sessionId, UUID userId, String email)`, `void publishPresenceUpdated(UUID sessionId, UUID userId, String email, SelectionRange selection)`, and `void handleRelayEvent(CanonicalCollaborationEvent event)`. `connectSnapshot(...)` must lazily call `SnapshotRecoveryService.loadRuntime(sessionId)` only when the runtime is not already cached on that instance.
  </action>
  <acceptance_criteria>
    - `rg -n "getRuntimeIfPresent|cacheRuntime|evictRuntime" src/main/java/com/collabeditor/websocket/service/CollaborationSessionRegistry.java` returns the exact local runtime-cache API.
    - `rg -n "connectSnapshot|submitOperation|publishParticipantJoined|publishParticipantLeft|publishPresenceUpdated|handleRelayEvent" src/main/java/com/collabeditor/websocket/service/DistributedCollaborationGateway.java` returns the exact gateway API.
    - `rg -n "SnapshotRecoveryService\\.loadRuntime|Optional<CollaborationSessionRuntime>" src/main/java/com/collabeditor/websocket/service/DistributedCollaborationGateway.java src/main/java/com/collabeditor/websocket/service/CollaborationSessionRegistry.java` returns lazy rebuild usage.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "*CollaborationWebSocketHandlerTest"</automated>
  </verify>
  <done>The websocket layer now has an explicit distributed gateway and a registry that distinguishes local socket tracking from lazily rebuilt canonical runtimes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewire the handler so ack, relay, and bootstrap follow the durable coordination path</name>
  <read_first>
    .planning/phases/03-durable-persistence-and-multi-instance-coordination/03-CONTEXT.md
    .planning/phases/03-durable-persistence-and-multi-instance-coordination/03-RESEARCH.md
    src/main/java/com/collabeditor/websocket/handler/CollaborationWebSocketHandler.java
    src/main/java/com/collabeditor/websocket/service/PresenceService.java
    src/main/java/com/collabeditor/websocket/service/DistributedCollaborationGateway.java
    src/main/java/com/collabeditor/redis/service/CollaborationRelayService.java
  </read_first>
  <files>src/main/java/com/collabeditor/websocket/service/PresenceService.java, src/main/java/com/collabeditor/websocket/handler/CollaborationWebSocketHandler.java, src/test/java/com/collabeditor/websocket/CollaborationWebSocketHandlerTest.java</files>
  <action>
    Update `CollaborationWebSocketHandler.afterConnectionEstablished(...)` so it calls `DistributedCollaborationGateway.connectSnapshot(sessionId)` and sends `document_sync` from the durable snapshot returned by that gateway, not from an always-empty `getOrCreateRuntime(...)` path. Update `handleSubmitOperation(...)` so it delegates the full accept flow to `DistributedCollaborationGateway.submitOperation(...)`. Inside that gateway, wrap the canonical apply path in `SessionCoordinationService.withSessionLock(sessionId, ...)`, hydrate the runtime if missing, apply the client operation, call `CollaborationPersistenceService.appendAcceptedOperation(...)`, call `SessionCoordinationService.setRevision(sessionId, acceptedRevision)`, then publish the canonical `OPERATION_APPLIED` relay event through `CollaborationRelayService.publish(...)`, and only after those steps send the sender's `operation_ack`. Do not directly call the old local `broadcast(...)` method for accepted operations. Rewire join, leave, and throttled presence updates so the handler publishes `PARTICIPANT_JOINED`, `PARTICIPANT_LEFT`, and `PRESENCE_UPDATED` through the relay path too. Keep the external Phase 2 websocket payload types and event names unchanged.
  </action>
  <acceptance_criteria>
    - `rg -n "connectSnapshot\\(|submitOperation\\(|operation_ack|appendAcceptedOperation|setRevision|publish\\(" src/main/java/com/collabeditor/websocket/handler/CollaborationWebSocketHandler.java src/main/java/com/collabeditor/websocket/service/DistributedCollaborationGateway.java` returns bootstrap, durable append, revision sync, relay publish, and ack sequencing.
    - `rg -n "PARTICIPANT_JOINED|PARTICIPANT_LEFT|PRESENCE_UPDATED|publishParticipantJoined|publishParticipantLeft|publishPresenceUpdated" src/main/java/com/collabeditor/websocket/handler/CollaborationWebSocketHandler.java src/main/java/com/collabeditor/websocket/service/DistributedCollaborationGateway.java` returns relay-backed participant and presence publishing.
    - `rg -n "document_sync|operation_ack|operation_applied|participant_joined|participant_left|presence_updated" src/test/java/com/collabeditor/websocket/CollaborationWebSocketHandlerTest.java` returns stable external-contract coverage.
    - `./gradlew test --tests "*CollaborationWebSocketHandlerTest"` exits with status 0.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "*CollaborationWebSocketHandlerTest"</automated>
  </verify>
  <done>The handler now bootstraps from durable state and uses the coordinated relay path for canonical collaboration events without changing the client-facing contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add distributed websocket tests for relay delivery, restart recovery, and gap-triggered resync</name>
  <read_first>
    .planning/phases/03-durable-persistence-and-multi-instance-coordination/03-VALIDATION.md
    src/main/java/com/collabeditor/websocket/service/DistributedCollaborationGateway.java
    src/main/java/com/collabeditor/websocket/handler/CollaborationWebSocketHandler.java
    src/test/java/com/collabeditor/websocket/CollaborationWebSocketHandlerTest.java
    src/test/java/com/collabeditor/redis/CollaborationRelayServiceTest.java
  </read_first>
  <files>src/test/java/com/collabeditor/websocket/CollaborationWebSocketHandlerTest.java, src/test/java/com/collabeditor/websocket/DistributedCollaborationWebSocketHandlerTest.java</files>
  <action>
    Create `DistributedCollaborationWebSocketHandlerTest` as the final Phase 3 distributed regression suite. Use shared PostgreSQL and Redis test containers with two isolated `CollaborationSessionRegistry` plus `DistributedCollaborationGateway` instances to represent two backend nodes. Add one named test where node A accepts an insert, node B receives the resulting relay event, and both local runtimes finish on the same canonical document and revision. Add one named test where node B starts with no cached runtime after node A already persisted edits and `connectSnapshot(sessionId)` returns the persisted document and revision from PostgreSQL. Add one named test where node B receives a relay event with a revision gap, evicts its cached runtime, and causes the local websocket path to emit `resync_required` instead of continuing on stale state. Update `CollaborationWebSocketHandlerTest` so its bootstrap assertions cover persisted-document startup, not just empty-room startup.
  </action>
  <acceptance_criteria>
    - `rg -n "node A|node B|same canonical document|persisted document and revision|revision gap|resync_required" src/test/java/com/collabeditor/websocket/DistributedCollaborationWebSocketHandlerTest.java` returns the distributed relay, restart recovery, and gap-handling scenarios.
    - `rg -n "persisted document|document_sync" src/test/java/com/collabeditor/websocket/CollaborationWebSocketHandlerTest.java` returns persisted bootstrap coverage.
    - `./gradlew test --tests "*CollaborationWebSocketHandlerTest" --tests "*DistributedCollaborationWebSocketHandlerTest"` exits with status 0.
    - `./gradlew test` exits with status 0.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "*CollaborationWebSocketHandlerTest" --tests "*DistributedCollaborationWebSocketHandlerTest"</automated>
    <automated>./gradlew test</automated>
  </verify>
  <done>Phase 3 finishes with automated proof that durable bootstrap, Redis relay, and gap-triggered rebuild or resync preserve canonical collaboration behavior across instances.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `./gradlew test --tests "*CollaborationWebSocketHandlerTest"`
- [ ] `./gradlew test --tests "*DistributedCollaborationWebSocketHandlerTest"`
- [ ] `./gradlew test --tests "*CollaborationWebSocketHandlerTest" --tests "*DistributedCollaborationWebSocketHandlerTest"`
- [ ] `./gradlew test`
</verification>

<success_criteria>
- The websocket collaboration contract bootstraps from durable room state after restarts.
- Accepted operations, join/leave events, and presence updates flow through the canonical Redis relay path.
- Relay gaps trigger runtime eviction and rebuild or `resync_required` instead of document divergence.
</success_criteria>

<output>
After completion, create `.planning/phases/03-durable-persistence-and-multi-instance-coordination/03-04-SUMMARY.md`
</output>
