---
phase: 04-sandboxed-code-execution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/resources/application.yml
  - src/test/resources/application-test.yml
  - src/main/java/com/collabeditor/execution/config/ExecutionProperties.java
  - src/main/java/com/collabeditor/execution/model/ExecutionStatus.java
  - src/main/java/com/collabeditor/execution/model/ExecutionSourceSnapshot.java
  - src/main/java/com/collabeditor/execution/service/ExecutionSourceService.java
  - src/main/java/com/collabeditor/execution/service/ExecutionPersistenceService.java
  - src/main/java/com/collabeditor/execution/persistence/ExecutionHistoryRepository.java
  - src/test/java/com/collabeditor/execution/ExecutionServiceTest.java
autonomous: true
requirements:
  - EXEC-01
  - EXEC-04
must_haves:
  truths:
    - "Execution admission snapshots the canonical persisted room document, immutable room language, and canonical revision before background work begins."
    - "Execution lifecycle state is durable in `execution_history`, including queued, running, rejected, and terminal outcomes."
    - "Phase 4 runtime configuration is explicit and versioned in application config: cooldown `5s`, timeout `10s`, worker-count `2`, queue-capacity `8`, memory `256MB`, CPU `0.5`, and fixed Python/Java image names."
  artifacts:
    - path: "src/main/java/com/collabeditor/execution/config/ExecutionProperties.java"
      provides: "Central typed configuration for queue, timeout, sandbox, and runtime-image settings."
    - path: "src/main/java/com/collabeditor/execution/service/ExecutionSourceService.java"
      provides: "Canonical room-source capture that validates membership and reads the persisted room document/revision."
    - path: "src/main/java/com/collabeditor/execution/service/ExecutionPersistenceService.java"
      provides: "Durable execution-history state transitions for queued, running, rejected, and terminal results."
    - path: "src/test/java/com/collabeditor/execution/ExecutionServiceTest.java"
      provides: "Proof that Phase 4 admission captures canonical source and persists execution lifecycle rows correctly."
  key_links:
    - from: "src/main/java/com/collabeditor/execution/service/ExecutionSourceService.java"
      to: "src/main/java/com/collabeditor/snapshot/service/SnapshotRecoveryService.java"
      via: "Execution source capture must read the current canonical persisted document and revision."
      pattern: "loadRuntime|snapshot"
    - from: "src/main/java/com/collabeditor/execution/service/ExecutionSourceService.java"
      to: "src/main/java/com/collabeditor/session/persistence/SessionParticipantRepository.java"
      via: "Only ACTIVE room participants may enqueue executions."
      pattern: "findBySessionIdAndUserId|ACTIVE"
    - from: "src/main/java/com/collabeditor/execution/service/ExecutionPersistenceService.java"
      to: "src/main/java/com/collabeditor/execution/persistence/entity/ExecutionHistoryEntity.java"
      via: "Lifecycle transitions must preserve source revision, status, timestamps, and final output fields."
      pattern: "setStatus|setStartedAt|setFinishedAt|setStdout|setStderr|setExitCode"
---

<objective>
Establish the execution admission and durable state foundation for Phase 4.

Purpose: Before Docker containers or queue workers run, the backend needs explicit execution configuration, canonical room-source capture, and durable execution-history lifecycle helpers built on the Phase 3 persistence model.
Output: Typed execution properties and status model, source-capture and persistence services, and tests proving queued/running/terminal execution rows derive from canonical room state.
</objective>

<execution_context>
@/Users/abdullah/.codex/get-shit-done/workflows/execute-plan.md
@/Users/abdullah/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-sandboxed-code-execution/04-CONTEXT.md
@.planning/phases/04-sandboxed-code-execution/04-RESEARCH.md
@.planning/phases/04-sandboxed-code-execution/04-VALIDATION.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add explicit execution properties and lifecycle status types</name>
  <read_first>
    .planning/phases/04-sandboxed-code-execution/04-CONTEXT.md
    .planning/phases/04-sandboxed-code-execution/04-RESEARCH.md
    src/main/resources/application.yml
    src/test/resources/application-test.yml
    src/main/java/com/collabeditor/execution/persistence/entity/ExecutionHistoryEntity.java
  </read_first>
  <files>src/main/resources/application.yml, src/test/resources/application-test.yml, src/main/java/com/collabeditor/execution/config/ExecutionProperties.java, src/main/java/com/collabeditor/execution/model/ExecutionStatus.java</files>
  <action>
    Introduce an `app.execution` configuration block in `src/main/resources/application.yml` with these exact keys and values: `cooldown-seconds: 5`, `timeout-seconds: 10`, `worker-count: 2`, `queue-capacity: 8`, `python-image: python:3.12-slim`, `java-image: eclipse-temurin:17-jdk-jammy`, `max-memory-bytes: 268435456`, `nano-cpus: 500000000`, `workspace-tmpfs-bytes: 67108864`, `tmp-tmpfs-bytes: 33554432`, and `non-root-user: 65534:65534`. Mirror the same keys in `src/test/resources/application-test.yml` with test-safe overrides or identical defaults. Create `ExecutionProperties` as a `@ConfigurationProperties(prefix = "app.execution")` type exposing those exact fields. Create `ExecutionStatus` with the exact enum members `QUEUED`, `RUNNING`, `COMPLETED`, `FAILED`, `TIMED_OUT`, and `REJECTED`.
  </action>
  <acceptance_criteria>
    - `rg -n "app:|execution:|cooldown-seconds: 5|timeout-seconds: 10|worker-count: 2|queue-capacity: 8|python-image: python:3.12-slim|java-image: eclipse-temurin:17-jdk-jammy|max-memory-bytes: 268435456|nano-cpus: 500000000|workspace-tmpfs-bytes: 67108864|tmp-tmpfs-bytes: 33554432|non-root-user: 65534:65534" src/main/resources/application.yml src/test/resources/application-test.yml` returns the exact execution settings in both config files.
    - `rg -n "@ConfigurationProperties\\(prefix = \"app\\.execution\"\\)|cooldownSeconds|timeoutSeconds|workerCount|queueCapacity|pythonImage|javaImage|maxMemoryBytes|nanoCpus|workspaceTmpfsBytes|tmpTmpfsBytes|nonRootUser" src/main/java/com/collabeditor/execution/config/ExecutionProperties.java` returns the typed execution properties.
    - `rg -n "QUEUED|RUNNING|COMPLETED|FAILED|TIMED_OUT|REJECTED" src/main/java/com/collabeditor/execution/model/ExecutionStatus.java` returns the exact lifecycle statuses.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "*ExecutionServiceTest"</automated>
  </verify>
  <done>The repo now has a typed and explicit execution configuration and lifecycle vocabulary that later plans can wire into queueing and sandbox orchestration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement canonical execution-source capture and durable lifecycle persistence helpers</name>
  <read_first>
    .planning/phases/04-sandboxed-code-execution/04-CONTEXT.md
    .planning/phases/04-sandboxed-code-execution/04-RESEARCH.md
    src/main/java/com/collabeditor/session/persistence/CodingSessionRepository.java
    src/main/java/com/collabeditor/session/persistence/SessionParticipantRepository.java
    src/main/java/com/collabeditor/snapshot/service/SnapshotRecoveryService.java
    src/main/java/com/collabeditor/execution/persistence/entity/ExecutionHistoryEntity.java
  </read_first>
  <files>src/main/java/com/collabeditor/execution/model/ExecutionSourceSnapshot.java, src/main/java/com/collabeditor/execution/service/ExecutionSourceService.java, src/main/java/com/collabeditor/execution/service/ExecutionPersistenceService.java, src/main/java/com/collabeditor/execution/persistence/ExecutionHistoryRepository.java</files>
  <action>
    Create `ExecutionSourceSnapshot` with the exact fields `UUID sessionId`, `UUID requestedByUserId`, `String requestedByEmail`, `String language`, `long sourceRevision`, and `String sourceCode`. Implement `ExecutionSourceService` with the exact method `ExecutionSourceSnapshot capture(UUID sessionId, UUID requestedByUserId)` that loads the `coding_sessions` row, verifies `SessionParticipantRepository.findBySessionIdAndUserId(sessionId, requestedByUserId)` exists with status `ACTIVE`, resolves the requester email from `UserRepository`, and reads the canonical document/revision from `SnapshotRecoveryService.loadRuntime(sessionId).snapshot()`. Implement `ExecutionPersistenceService` with the exact methods `ExecutionHistoryEntity recordQueuedExecution(ExecutionSourceSnapshot snapshot)`, `ExecutionHistoryEntity recordRejectedExecution(ExecutionSourceSnapshot snapshot, String stderr)`, `ExecutionHistoryEntity markRunning(UUID executionId)`, and `ExecutionHistoryEntity finishExecution(UUID executionId, ExecutionStatus status, String stdout, String stderr, Integer exitCode)`. These methods must persist `language`, `sourceRevision`, timestamps, and final output fields on the existing `execution_history` table. Extend `ExecutionHistoryRepository` with the exact method `Optional<ExecutionHistoryEntity> findByIdAndSessionId(UUID id, UUID sessionId)`.
  </action>
  <acceptance_criteria>
    - `rg -n "UUID sessionId|UUID requestedByUserId|String requestedByEmail|String language|long sourceRevision|String sourceCode" src/main/java/com/collabeditor/execution/model/ExecutionSourceSnapshot.java` returns the exact snapshot fields.
    - `rg -n "ExecutionSourceSnapshot capture\\(UUID sessionId, UUID requestedByUserId\\)|findBySessionIdAndUserId|loadRuntime\\(sessionId\\)\\.snapshot" src/main/java/com/collabeditor/execution/service/ExecutionSourceService.java` returns membership validation and canonical snapshot capture.
    - `rg -n "recordQueuedExecution|recordRejectedExecution|markRunning|finishExecution|findByIdAndSessionId" src/main/java/com/collabeditor/execution/service/ExecutionPersistenceService.java src/main/java/com/collabeditor/execution/persistence/ExecutionHistoryRepository.java` returns the exact persistence helpers and repository lookup.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "*ExecutionServiceTest"</automated>
  </verify>
  <done>Execution admission can now derive a runnable snapshot from canonical room state and persist durable lifecycle rows before the queue or sandbox exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add focused service tests for canonical source capture and execution-history transitions</name>
  <read_first>
    .planning/phases/04-sandboxed-code-execution/04-VALIDATION.md
    src/main/java/com/collabeditor/execution/service/ExecutionSourceService.java
    src/main/java/com/collabeditor/execution/service/ExecutionPersistenceService.java
    src/test/java/com/collabeditor/ot/CollaborationPersistenceIntegrationTest.java
    src/test/java/com/collabeditor/ot/CollaborationPersistenceRepositoryTest.java
  </read_first>
  <files>src/test/java/com/collabeditor/execution/ExecutionServiceTest.java</files>
  <action>
    Create `ExecutionServiceTest` as a focused integration-style regression covering three exact cases: `capture(...)` returns the persisted canonical document, canonical revision, immutable room language, and requester email for an ACTIVE participant; `capture(...)` rejects a non-active or non-participant requester; and `ExecutionPersistenceService` writes durable `QUEUED`, `RUNNING`, `REJECTED`, and terminal (`COMPLETED` or `FAILED`) transitions with the correct `source_revision`, `started_at`, `finished_at`, `stdout`, `stderr`, and `exit_code` values. Follow the existing repository-integration testing style already used under `src/test/java/com/collabeditor/ot/`.
  </action>
  <acceptance_criteria>
    - `rg -n "canonical document|canonical revision|room language|requester email|non-active|QUEUED|RUNNING|REJECTED|COMPLETED|FAILED" src/test/java/com/collabeditor/execution/ExecutionServiceTest.java` returns the exact service coverage.
    - `./gradlew test --tests "*ExecutionServiceTest"` exits with status 0.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "*ExecutionServiceTest"</automated>
  </verify>
  <done>Wave 1 foundation work is proven: execution admission reads canonical room state and durable lifecycle rows behave correctly before any Docker orchestration is introduced.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `./gradlew test --tests "*ExecutionServiceTest"`
</verification>

<success_criteria>
- Phase 4 has explicit execution configuration and lifecycle states.
- Execution admission can derive a canonical room snapshot from persisted collaboration state.
- Durable execution-history helpers exist for queued, running, rejected, and terminal results.
</success_criteria>

<output>
After completion, create `.planning/phases/04-sandboxed-code-execution/04-01-SUMMARY.md`
</output>
