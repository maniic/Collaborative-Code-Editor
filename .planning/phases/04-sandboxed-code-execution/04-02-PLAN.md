---
phase: 04-sandboxed-code-execution
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - build.gradle.kts
  - src/main/java/com/collabeditor/execution/config/DockerExecutionConfig.java
  - src/main/java/com/collabeditor/execution/model/SandboxExecutionResult.java
  - src/main/java/com/collabeditor/execution/service/ExecutionLanguageSpecResolver.java
  - src/main/java/com/collabeditor/execution/service/DockerSandboxRunner.java
  - src/test/java/com/collabeditor/execution/DockerSandboxRunnerTest.java
autonomous: true
requirements:
  - EXEC-01
  - EXEC-02
  - EXEC-03
must_haves:
  truths:
    - "Phase 4 uses docker-java rather than shelling out to `docker run`."
    - "Python executes as a plain script from `main.py`, while Java executes only as a package-less `Main.java` with `public static void main(String[] args)`."
    - "Every sandbox run applies the exact baseline limits: memory `256MB`, CPU `0.5`, timeout `10s`, `networkMode=none`, read-only root filesystem, and non-root user `65534:65534`."
    - "Writable runtime artifacts live only in tmpfs mounts, not in the container rootfs or a broad host bind mount."
  artifacts:
    - path: "src/main/java/com/collabeditor/execution/config/DockerExecutionConfig.java"
      provides: "docker-java client beans wired to the local Docker host and transport."
    - path: "src/main/java/com/collabeditor/execution/service/ExecutionLanguageSpecResolver.java"
      provides: "Exact Python and Java runtime-image, source-file, command, and contract rules."
    - path: "src/main/java/com/collabeditor/execution/service/DockerSandboxRunner.java"
      provides: "Concrete container create/start/wait/log/kill/remove implementation with strict host config."
    - path: "src/test/java/com/collabeditor/execution/DockerSandboxRunnerTest.java"
      provides: "Proof that the runner builds the expected language commands and Docker HostConfig limits."
  key_links:
    - from: "src/main/java/com/collabeditor/execution/config/DockerExecutionConfig.java"
      to: "build.gradle.kts"
      via: "docker-java dependencies and transport configuration must agree with the bean wiring."
      pattern: "docker-java|transport-httpclient5|DockerClientImpl|ApacheDockerHttpClient"
    - from: "src/main/java/com/collabeditor/execution/service/ExecutionLanguageSpecResolver.java"
      to: "src/main/java/com/collabeditor/execution/model/ExecutionSourceSnapshot.java"
      via: "Language runtime resolution depends on the immutable room language and captured source code."
      pattern: "PYTHON|JAVA|Main.java|main.py"
    - from: "src/main/java/com/collabeditor/execution/service/DockerSandboxRunner.java"
      to: "src/main/java/com/collabeditor/execution/config/ExecutionProperties.java"
      via: "The runner must apply the exact timeout, memory, CPU, tmpfs, image, and user settings from typed config."
      pattern: "getTimeoutSeconds|getMaxMemoryBytes|getNanoCpus|getWorkspaceTmpfsBytes|getTmpTmpfsBytes|getNonRootUser"
---

<objective>
Implement the concrete Docker sandbox runner and language runtime contracts for Phase 4.

Purpose: Once execution admission can capture canonical room source, the backend needs a real Docker runner that knows exactly how to execute Python and Java under the required isolation limits.
Output: docker-java dependencies and config, language contract resolution, Docker sandbox runner implementation, and contract tests for commands and HostConfig limits.
</objective>

<execution_context>
@/Users/abdullah/.codex/get-shit-done/workflows/execute-plan.md
@/Users/abdullah/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-sandboxed-code-execution/04-CONTEXT.md
@.planning/phases/04-sandboxed-code-execution/04-RESEARCH.md
@.planning/phases/04-sandboxed-code-execution/04-VALIDATION.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add docker-java and create the typed Docker client configuration</name>
  <read_first>
    .planning/phases/04-sandboxed-code-execution/04-CONTEXT.md
    .planning/phases/04-sandboxed-code-execution/04-RESEARCH.md
    build.gradle.kts
    src/main/resources/application.yml
    src/main/java/com/collabeditor/redis/config/RedisConfig.java
  </read_first>
  <files>build.gradle.kts, src/main/java/com/collabeditor/execution/config/DockerExecutionConfig.java</files>
  <action>
    Add the exact dependencies `implementation("com.github.docker-java:docker-java:3.7.1")` and `implementation("com.github.docker-java:docker-java-transport-httpclient5:3.7.1")` to `build.gradle.kts`. Create `DockerExecutionConfig` that builds a `DockerClient` bean from `DefaultDockerClientConfig.createDefaultConfigBuilder().build()`, an `ApacheDockerHttpClient.Builder` wired with `dockerHost` and `sslConfig`, and `DockerClientImpl.getInstance(...)`. Keep the bean reusable by the execution services instead of hiding docker-java setup inside the runner itself.
  </action>
  <acceptance_criteria>
    - `rg -n "com.github.docker-java:docker-java:3.7.1|com.github.docker-java:docker-java-transport-httpclient5:3.7.1" build.gradle.kts` returns both docker-java dependencies.
    - `rg -n "DefaultDockerClientConfig|ApacheDockerHttpClient|DockerClientImpl|getInstance" src/main/java/com/collabeditor/execution/config/DockerExecutionConfig.java` returns the exact Docker client wiring.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "*DockerSandboxRunnerTest"</automated>
  </verify>
  <done>The repo now has an explicit docker-java dependency set and a reusable Docker client bean for execution work.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Encode the exact Python and Java runtime contracts</name>
  <read_first>
    .planning/phases/04-sandboxed-code-execution/04-CONTEXT.md
    .planning/phases/04-sandboxed-code-execution/04-RESEARCH.md
    src/main/java/com/collabeditor/execution/model/ExecutionSourceSnapshot.java
    src/main/java/com/collabeditor/execution/config/ExecutionProperties.java
  </read_first>
  <files>src/main/java/com/collabeditor/execution/model/SandboxExecutionResult.java, src/main/java/com/collabeditor/execution/service/ExecutionLanguageSpecResolver.java</files>
  <action>
    Create `SandboxExecutionResult` with the exact fields `ExecutionStatus status`, `String stdout`, `String stderr`, and `Integer exitCode`. Implement `ExecutionLanguageSpecResolver` so `PYTHON` maps to the exact source filename `main.py`, image `python:3.12-slim`, environment `PYTHONDONTWRITEBYTECODE=1`, and container command `["python", "/input/main.py"]`. Map `JAVA` to the exact source filename `Main.java`, image `eclipse-temurin:17-jdk-jammy`, and container command `["sh", "-lc", "cp /input/Main.java /workspace/Main.java && javac -d /workspace/out /workspace/Main.java && java -cp /workspace/out Main"]`. Reject Java source that does not contain a package-less `public class Main` and `public static void main(String[] args)` with a clear validation message before attempting container startup.
  </action>
  <acceptance_criteria>
    - `rg -n "main.py|python:3.12-slim|PYTHONDONTWRITEBYTECODE=1|/input/main.py" src/main/java/com/collabeditor/execution/service/ExecutionLanguageSpecResolver.java` returns the exact Python runtime contract.
    - `rg -n "Main.java|eclipse-temurin:17-jdk-jammy|javac -d /workspace/out /workspace/Main.java|java -cp /workspace/out Main|public class Main|public static void main\\(String\\[\\] args\\)" src/main/java/com/collabeditor/execution/service/ExecutionLanguageSpecResolver.java` returns the exact Java runtime contract and validation checks.
    - `rg -n "ExecutionStatus status|String stdout|String stderr|Integer exitCode" src/main/java/com/collabeditor/execution/model/SandboxExecutionResult.java` returns the exact sandbox result fields.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "*DockerSandboxRunnerTest"</automated>
  </verify>
  <done>Phase 4 now has a single explicit runtime contract for Python and Java, with no snippet wrapping or inferred entrypoints.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement the Docker sandbox runner with exact host config limits and cleanup behavior</name>
  <read_first>
    .planning/phases/04-sandboxed-code-execution/04-VALIDATION.md
    src/main/java/com/collabeditor/execution/config/DockerExecutionConfig.java
    src/main/java/com/collabeditor/execution/service/ExecutionLanguageSpecResolver.java
    src/main/java/com/collabeditor/execution/config/ExecutionProperties.java
  </read_first>
  <files>src/main/java/com/collabeditor/execution/service/DockerSandboxRunner.java, src/test/java/com/collabeditor/execution/DockerSandboxRunnerTest.java</files>
  <action>
    Create `DockerSandboxRunner` with the exact method `SandboxExecutionResult run(ExecutionSourceSnapshot snapshot)`. `run(...)` must write the resolved source file into a temp directory, bind that directory read-only to `/input`, mount tmpfs at `/workspace` sized from `workspaceTmpfsBytes`, mount tmpfs at `/tmp` sized from `tmpTmpfsBytes`, and create containers with `withReadonlyRootfs(true)`, `withNetworkMode("none")`, `withMemory(268435456L)`, `withMemorySwap(268435456L)`, `withNanoCPUs(500_000_000L)`, `withUser("65534:65534")`, and `withWorkingDir("/workspace")`. Use docker-java create/start/log/wait APIs, enforce the exact ten-second timeout, kill timed-out containers, collect stdout/stderr, and remove containers and temp directories in `finally`. Create `DockerSandboxRunnerTest` that asserts the exact Python and Java command lines, exact image names, exact `HostConfig` resource-limit values, and timeout cleanup behavior.
  </action>
  <acceptance_criteria>
    - `rg -n "withReadonlyRootfs\\(true\\)|withNetworkMode\\(\"none\"\\)|withMemory\\(268435456L\\)|withMemorySwap\\(268435456L\\)|withNanoCPUs\\(500_000_000L\\)|withUser\\(\"65534:65534\"\\)|withWorkingDir\\(\"/workspace\"\\)" src/main/java/com/collabeditor/execution/service/DockerSandboxRunner.java` returns the exact sandbox limits.
    - `rg -n "python /input/main.py|cp /input/Main.java /workspace/Main.java && javac -d /workspace/out /workspace/Main.java && java -cp /workspace/out Main|python:3.12-slim|eclipse-temurin:17-jdk-jammy|timeout" src/test/java/com/collabeditor/execution/DockerSandboxRunnerTest.java` returns the exact runtime-contract and cleanup assertions.
    - `./gradlew test --tests "*DockerSandboxRunnerTest"` exits with status 0.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "*DockerSandboxRunnerTest"</automated>
  </verify>
  <done>Wave 1 sandbox work is complete: the backend has a real Docker runner that encodes the exact runtime contract and isolation limits for both supported languages.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `./gradlew test --tests "*DockerSandboxRunnerTest"`
</verification>

<success_criteria>
- docker-java is installed and configured through explicit Spring beans.
- Python and Java map to fixed, documented runtime contracts.
- The Docker runner applies the exact resource, filesystem, network, and user constraints required by Phase 4.
</success_criteria>

<output>
After completion, create `.planning/phases/04-sandboxed-code-execution/04-02-SUMMARY.md`
</output>
