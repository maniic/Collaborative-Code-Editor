---
phase: 04-sandboxed-code-execution
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified:
  - src/main/java/com/collabeditor/execution/config/ExecutionTaskConfig.java
  - src/main/java/com/collabeditor/execution/service/ExecutionRateLimitService.java
  - src/main/java/com/collabeditor/execution/service/ExecutionEventRelayService.java
  - src/main/java/com/collabeditor/execution/service/ExecutionBroadcastGateway.java
  - src/main/java/com/collabeditor/execution/service/ExecutionCoordinatorService.java
  - src/main/java/com/collabeditor/execution/api/ExecutionController.java
  - src/main/java/com/collabeditor/execution/api/dto/EnqueueExecutionResponse.java
  - src/main/java/com/collabeditor/websocket/protocol/ExecutionUpdatedPayload.java
  - src/main/java/com/collabeditor/websocket/protocol/ServerMessageType.java
  - src/main/java/com/collabeditor/websocket/handler/CollaborationWebSocketHandler.java
  - src/main/java/com/collabeditor/common/api/ApiExceptionHandler.java
  - src/test/java/com/collabeditor/execution/ExecutionControllerTest.java
  - src/test/java/com/collabeditor/execution/ExecutionEventRelayServiceTest.java
  - src/test/java/com/collabeditor/execution/ExecutionServiceTest.java
autonomous: true
requirements:
  - EXEC-01
  - EXEC-04
must_haves:
  truths:
    - "Accepted execution requests return `202 Accepted` with an execution identifier, room language, source revision, and initial `QUEUED` status."
    - "The one-execution-per-user-per-five-seconds rule is enforced across backend instances through Redis, not local memory."
    - "The worker queue is bounded and fail-fast: cooldown violations return `429`, full queues return `503`, and both outcomes are explicit."
    - "Room participants connected to any backend instance receive the same execution lifecycle updates over the existing session WebSocket."
  artifacts:
    - path: "src/main/java/com/collabeditor/execution/service/ExecutionCoordinatorService.java"
      provides: "Distributed cooldown, bounded queue admission, worker execution, and lifecycle persistence orchestration."
    - path: "src/main/java/com/collabeditor/execution/api/ExecutionController.java"
      provides: "Authenticated REST enqueue contract for session execution requests."
    - path: "src/main/java/com/collabeditor/execution/service/ExecutionEventRelayService.java"
      provides: "Redis-backed room relay for cross-instance execution lifecycle delivery."
    - path: "src/main/java/com/collabeditor/websocket/protocol/ExecutionUpdatedPayload.java"
      provides: "Additive execution lifecycle payload on the existing session socket."
    - path: "src/test/java/com/collabeditor/execution/ExecutionControllerTest.java"
      provides: "Proof of accepted enqueue, cooldown rejection, and queue-full rejection semantics."
  key_links:
    - from: "src/main/java/com/collabeditor/execution/service/ExecutionCoordinatorService.java"
      to: "src/main/java/com/collabeditor/execution/service/ExecutionRateLimitService.java"
      via: "Queue admission must honor the distributed five-second cooldown before enqueuing work."
      pattern: "tryAcquire|cooldown|Duration.ofSeconds\\(5\\)"
    - from: "src/main/java/com/collabeditor/execution/service/ExecutionCoordinatorService.java"
      to: "src/main/java/com/collabeditor/execution/service/DockerSandboxRunner.java"
      via: "Background workers must transition queued executions to running and terminal results through the real sandbox runner."
      pattern: "markRunning|run\\(|finishExecution"
    - from: "src/main/java/com/collabeditor/execution/service/ExecutionBroadcastGateway.java"
      to: "src/main/java/com/collabeditor/websocket/handler/CollaborationWebSocketHandler.java"
      via: "Room websocket connections must subscribe to execution lifecycle broadcasts on connect."
      pattern: "ensureSessionSubscription|execution_updated|afterConnectionEstablished"
---

<objective>
Implement asynchronous execution orchestration, distributed cooldown, and room-visible lifecycle events for Phase 4.

Purpose: After the canonical source capture and Docker runner exist, the backend still needs a bounded worker queue, cross-instance cooldown enforcement, an authenticated enqueue API, and execution lifecycle broadcasts on the existing room socket.
Output: Execution coordinator, Redis-backed cooldown and relay services, REST enqueue controller, additive websocket payloads, and controller/relay tests proving distributed execution admission semantics.
</objective>

<execution_context>
@/Users/abdullah/.codex/get-shit-done/workflows/execute-plan.md
@/Users/abdullah/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-sandboxed-code-execution/04-CONTEXT.md
@.planning/phases/04-sandboxed-code-execution/04-RESEARCH.md
@.planning/phases/04-sandboxed-code-execution/04-VALIDATION.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the distributed cooldown and bounded worker-queue coordinator</name>
  <read_first>
    .planning/phases/04-sandboxed-code-execution/04-CONTEXT.md
    .planning/phases/04-sandboxed-code-execution/04-RESEARCH.md
    src/main/java/com/collabeditor/execution/service/ExecutionSourceService.java
    src/main/java/com/collabeditor/execution/service/ExecutionPersistenceService.java
    src/main/java/com/collabeditor/execution/service/DockerSandboxRunner.java
    src/main/java/com/collabeditor/redis/service/SessionCoordinationService.java
  </read_first>
  <files>src/main/java/com/collabeditor/execution/config/ExecutionTaskConfig.java, src/main/java/com/collabeditor/execution/service/ExecutionRateLimitService.java, src/main/java/com/collabeditor/execution/service/ExecutionCoordinatorService.java, src/test/java/com/collabeditor/execution/ExecutionServiceTest.java</files>
  <action>
    Create `ExecutionTaskConfig` with a `ThreadPoolTaskExecutor` bean configured to the exact values `corePoolSize=2`, `maxPoolSize=2`, `queueCapacity=8`, and a rejection policy that surfaces `TaskRejectedException`. Create `ExecutionRateLimitService` using `StringRedisTemplate` and the exact Redis key pattern `collab:execution:user:{userId}:cooldown`; `tryAcquire(UUID userId)` must use Redis `setIfAbsent` with a five-second TTL and return `false` when the cooldown key already exists. Create `ExecutionCoordinatorService` with the exact method `EnqueueExecutionResponse enqueue(UUID sessionId, UUID requestedByUserId)` that captures canonical source, acquires cooldown, records a `QUEUED` row, submits background work to the task executor, marks rows `RUNNING` before calling `DockerSandboxRunner.run(...)`, and writes terminal results through `ExecutionPersistenceService.finishExecution(...)`. If cooldown acquisition fails, persist a `REJECTED` row and throw a dedicated rate-limit exception mapped to HTTP `429`. If executor submission throws `TaskRejectedException`, persist a `REJECTED` row and throw a dedicated queue-full exception mapped to HTTP `503`.
  </action>
  <acceptance_criteria>
    - `rg -n "corePoolSize=2|maxPoolSize=2|queueCapacity=8|ThreadPoolTaskExecutor|TaskRejectedException" src/main/java/com/collabeditor/execution/config/ExecutionTaskConfig.java src/main/java/com/collabeditor/execution/service/ExecutionCoordinatorService.java` returns the exact bounded executor settings and rejection handling.
    - `rg -n "collab:execution:user:|setIfAbsent|Duration\\.ofSeconds\\(5\\)|tryAcquire" src/main/java/com/collabeditor/execution/service/ExecutionRateLimitService.java` returns the exact distributed cooldown key and TTL enforcement.
    - `rg -n "recordQueuedExecution|recordRejectedExecution|markRunning|finishExecution|DockerSandboxRunner\\.run|TaskRejectedException" src/main/java/com/collabeditor/execution/service/ExecutionCoordinatorService.java` returns the exact lifecycle orchestration steps.
    - `./gradlew test --tests "*ExecutionServiceTest"` exits with status 0.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "*ExecutionServiceTest"</automated>
  </verify>
  <done>The backend can now admit execution requests through a bounded queue while enforcing a distributed five-second cooldown and durable lifecycle transitions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Relay execution lifecycle updates across instances and broadcast them on the room socket</name>
  <read_first>
    .planning/phases/04-sandboxed-code-execution/04-CONTEXT.md
    .planning/phases/04-sandboxed-code-execution/04-RESEARCH.md
    src/main/java/com/collabeditor/redis/service/CollaborationRelayService.java
    src/main/java/com/collabeditor/websocket/service/CollaborationSessionRegistry.java
    src/main/java/com/collabeditor/websocket/handler/CollaborationWebSocketHandler.java
    src/main/java/com/collabeditor/websocket/protocol/ServerMessageType.java
  </read_first>
  <files>src/main/java/com/collabeditor/execution/service/ExecutionEventRelayService.java, src/main/java/com/collabeditor/execution/service/ExecutionBroadcastGateway.java, src/main/java/com/collabeditor/websocket/protocol/ExecutionUpdatedPayload.java, src/main/java/com/collabeditor/websocket/protocol/ServerMessageType.java, src/main/java/com/collabeditor/websocket/handler/CollaborationWebSocketHandler.java, src/test/java/com/collabeditor/execution/ExecutionEventRelayServiceTest.java</files>
  <action>
    Create `ExecutionUpdatedPayload` with the exact fields `UUID executionId`, `UUID requestedByUserId`, `String requestedByEmail`, `String language`, `long sourceRevision`, `String status`, `String stdout`, `String stderr`, `Integer exitCode`, `Instant createdAt`, `Instant startedAt`, `Instant finishedAt`, and `String message`. Add the exact server event type `execution_updated` to `ServerMessageType`. Implement `ExecutionEventRelayService` so it publishes room-scoped execution updates to the exact Redis channel `collab:session:{sessionId}:execution-events` and lets callers subscribe with a callback for one `sessionId`. Implement `ExecutionBroadcastGateway` so it converts those relay messages into `execution_updated` websocket broadcasts for local sockets in `CollaborationSessionRegistry`. Update `CollaborationWebSocketHandler.afterConnectionEstablished(...)` to call `ExecutionBroadcastGateway.ensureSessionSubscription(sessionId)` after room bootstrap so any active room socket begins receiving execution updates without opening a second websocket connection.
  </action>
  <acceptance_criteria>
    - `rg -n "execution_updated" src/main/java/com/collabeditor/websocket/protocol/ServerMessageType.java src/main/java/com/collabeditor/websocket/handler/CollaborationWebSocketHandler.java` returns the additive execution websocket event and its subscription hook.
    - `rg -n "UUID executionId|UUID requestedByUserId|String requestedByEmail|String language|long sourceRevision|String status|String stdout|String stderr|Integer exitCode|Instant createdAt|Instant startedAt|Instant finishedAt|String message" src/main/java/com/collabeditor/websocket/protocol/ExecutionUpdatedPayload.java` returns the exact lifecycle payload fields.
    - `rg -n "collab:session:|execution-events|publish|subscribe|ensureSessionSubscription" src/main/java/com/collabeditor/execution/service/ExecutionEventRelayService.java src/main/java/com/collabeditor/execution/service/ExecutionBroadcastGateway.java` returns the exact Redis channel and broadcast API.
    - `./gradlew test --tests "*ExecutionEventRelayServiceTest"` exits with status 0.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "*ExecutionEventRelayServiceTest"</automated>
  </verify>
  <done>Execution lifecycle updates are now room-visible across backend instances through the existing session websocket and a Redis-backed relay path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add the authenticated REST enqueue controller and explicit rejection semantics</name>
  <read_first>
    .planning/phases/04-sandboxed-code-execution/04-VALIDATION.md
    src/main/java/com/collabeditor/session/api/SessionController.java
    src/main/java/com/collabeditor/common/api/ApiExceptionHandler.java
    src/main/java/com/collabeditor/auth/security/SecurityConfig.java
    src/main/java/com/collabeditor/execution/service/ExecutionCoordinatorService.java
  </read_first>
  <files>src/main/java/com/collabeditor/execution/api/ExecutionController.java, src/main/java/com/collabeditor/execution/api/dto/EnqueueExecutionResponse.java, src/main/java/com/collabeditor/common/api/ApiExceptionHandler.java, src/test/java/com/collabeditor/execution/ExecutionControllerTest.java</files>
  <action>
    Create `ExecutionController` under `@RequestMapping("/api/sessions")` with the exact endpoint `POST /{sessionId}/executions` that extracts the authenticated user UUID from `Authentication`, calls `ExecutionCoordinatorService.enqueue(sessionId, userId)`, and returns `202 Accepted`. Create `EnqueueExecutionResponse` with the exact fields `UUID executionId`, `UUID sessionId`, `String language`, `long sourceRevision`, and `String status`. Extend `ApiExceptionHandler` with dedicated handlers that map the new execution rate-limit exception to HTTP `429 Too Many Requests` and the new queue-full exception to HTTP `503 Service Unavailable`, both using the repo’s existing `ErrorResponse` shape. Add `ExecutionControllerTest` covering accepted enqueue (`202` with `QUEUED`), cooldown rejection (`429`), queue-full rejection (`503`), and unauthorized access (`401`) through the production security filter chain.
  </action>
  <acceptance_criteria>
    - `rg -n "@PostMapping\\(\"/\\{sessionId\\}/executions\"\\)|ResponseEntity\\.accepted\\(|ExecutionCoordinatorService\\.enqueue" src/main/java/com/collabeditor/execution/api/ExecutionController.java` returns the exact authenticated enqueue endpoint.
    - `rg -n "UUID executionId|UUID sessionId|String language|long sourceRevision|String status" src/main/java/com/collabeditor/execution/api/dto/EnqueueExecutionResponse.java` returns the exact response contract.
    - `rg -n "Too Many Requests|Service Unavailable|429|503" src/main/java/com/collabeditor/common/api/ApiExceptionHandler.java src/test/java/com/collabeditor/execution/ExecutionControllerTest.java` returns explicit cooldown and queue-full mapping coverage.
    - `./gradlew test --tests "*ExecutionControllerTest" --tests "*ExecutionEventRelayServiceTest"` exits with status 0.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "*ExecutionControllerTest" --tests "*ExecutionEventRelayServiceTest"</automated>
  </verify>
  <done>Phase 4 now has a room-scoped, authenticated execution enqueue API with explicit distributed rejection semantics and websocket lifecycle delivery.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `./gradlew test --tests "*ExecutionServiceTest"`
- [ ] `./gradlew test --tests "*ExecutionEventRelayServiceTest"`
- [ ] `./gradlew test --tests "*ExecutionControllerTest" --tests "*ExecutionEventRelayServiceTest"`
</verification>

<success_criteria>
- Accepted executions are enqueued asynchronously through an authenticated session REST endpoint.
- Cooldown and full-queue behavior are explicit, durable, and correct across backend instances.
- Active room participants receive execution lifecycle updates on the existing session WebSocket regardless of which node accepted the request.
</success_criteria>

<output>
After completion, create `.planning/phases/04-sandboxed-code-execution/04-03-SUMMARY.md`
</output>
