---
phase: 04-sandboxed-code-execution
plan: 04
type: execute
wave: 3
depends_on:
  - 04-03
files_modified:
  - src/test/java/com/collabeditor/execution/ExecutionIntegrationTest.java
  - src/test/java/com/collabeditor/execution/ExecutionControllerTest.java
  - src/test/java/com/collabeditor/execution/ExecutionEventRelayServiceTest.java
  - src/main/java/com/collabeditor/execution/service/DockerSandboxRunner.java
  - src/main/java/com/collabeditor/execution/service/ExecutionCoordinatorService.java
  - src/main/java/com/collabeditor/execution/service/ExecutionBroadcastGateway.java
autonomous: true
requirements:
  - EXEC-01
  - EXEC-02
  - EXEC-03
  - EXEC-04
must_haves:
  truths:
    - "Python and Java both execute successfully in real Docker containers and return structured output."
    - "Phase 4 enforces timeout, no-network, read-only filesystem, and non-root behavior in real execution flows, not only mocked HostConfig tests."
    - "Cross-instance room participants converge on the same execution lifecycle and final result payload."
    - "Timed-out or failed executions clean up their Docker containers and publish one final terminal update instead of leaking resources or hanging in `RUNNING`."
  artifacts:
    - path: "src/test/java/com/collabeditor/execution/ExecutionIntegrationTest.java"
      provides: "Real Docker-backed execution proof for Python, Java, timeout, and isolation behavior."
    - path: "src/test/java/com/collabeditor/execution/ExecutionEventRelayServiceTest.java"
      provides: "Cross-instance execution relay proof for room-visible lifecycle delivery."
    - path: "src/main/java/com/collabeditor/execution/service/DockerSandboxRunner.java"
      provides: "Hardened cleanup and timeout behavior validated by live Docker tests."
  key_links:
    - from: "src/test/java/com/collabeditor/execution/ExecutionIntegrationTest.java"
      to: "src/main/java/com/collabeditor/execution/service/DockerSandboxRunner.java"
      via: "The final phase proof must exercise the real sandbox implementation, not a mock path."
      pattern: "run\\(|TIMED_OUT|COMPLETED|FAILED"
    - from: "src/test/java/com/collabeditor/execution/ExecutionEventRelayServiceTest.java"
      to: "src/main/java/com/collabeditor/execution/service/ExecutionBroadcastGateway.java"
      via: "Two-node room participants must see the same execution lifecycle updates."
      pattern: "execution_updated|subscribe|broadcast"
    - from: "src/main/java/com/collabeditor/execution/service/ExecutionCoordinatorService.java"
      to: "src/main/java/com/collabeditor/execution/service/ExecutionPersistenceService.java"
      via: "Terminal cleanup paths must end with one durable final state and timestamps."
      pattern: "finishExecution|TIMED_OUT|FAILED|COMPLETED|REJECTED"
---

<objective>
Finish Phase 4 with real Docker-backed verification and execution-path hardening.

Purpose: Phase 4 is only complete when the planned queueing, relay, and sandbox guarantees are proven against real containers and the edge cases uncovered by those tests are closed.
Output: Docker-backed integration suite for both languages and isolation guarantees, hardened cleanup/timeout behavior, and final cross-instance execution relay proof.
</objective>

<execution_context>
@/Users/abdullah/.codex/get-shit-done/workflows/execute-plan.md
@/Users/abdullah/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-sandboxed-code-execution/04-CONTEXT.md
@.planning/phases/04-sandboxed-code-execution/04-RESEARCH.md
@.planning/phases/04-sandboxed-code-execution/04-VALIDATION.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add real Docker-backed execution integration tests for Python and Java</name>
  <read_first>
    .planning/phases/04-sandboxed-code-execution/04-CONTEXT.md
    .planning/phases/04-sandboxed-code-execution/04-RESEARCH.md
    src/main/java/com/collabeditor/execution/service/DockerSandboxRunner.java
    src/main/java/com/collabeditor/execution/service/ExecutionLanguageSpecResolver.java
    src/test/resources/application-test.yml
  </read_first>
  <files>src/test/java/com/collabeditor/execution/ExecutionIntegrationTest.java</files>
  <action>
    Create `ExecutionIntegrationTest` as a live Docker-backed suite that exercises the real `DockerSandboxRunner` with the configured images. Add one named test where Python source prints to stdout and exits successfully, returning `COMPLETED`, stdout content, empty stderr, and exit code `0`. Add one named test where valid Java `Main` source prints to stdout and returns `COMPLETED`. Add one named test where invalid Java source missing the required `Main` contract returns a controlled failure with a clear stderr/message instead of an internal exception. Add one named test where an infinite loop hits the exact ten-second timeout and returns `TIMED_OUT`. Keep the tests explicit about the structured result fields rather than only asserting container exit.
  </action>
  <acceptance_criteria>
    - `rg -n "Python source prints|Java Main source prints|missing the required Main contract|ten-second timeout|TIMED_OUT|COMPLETED" src/test/java/com/collabeditor/execution/ExecutionIntegrationTest.java` returns the exact live execution scenarios.
    - `./gradlew test --tests "*ExecutionIntegrationTest"` exits with status 0.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "*ExecutionIntegrationTest"</automated>
  </verify>
  <done>The phase now has live Docker proof that both supported languages execute and return structured lifecycle results.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove isolation guarantees and harden timeout or cleanup edge cases</name>
  <read_first>
    .planning/phases/04-sandboxed-code-execution/04-VALIDATION.md
    src/main/java/com/collabeditor/execution/service/DockerSandboxRunner.java
    src/main/java/com/collabeditor/execution/service/ExecutionCoordinatorService.java
    src/test/java/com/collabeditor/execution/ExecutionIntegrationTest.java
  </read_first>
  <files>src/main/java/com/collabeditor/execution/service/DockerSandboxRunner.java, src/main/java/com/collabeditor/execution/service/ExecutionCoordinatorService.java, src/test/java/com/collabeditor/execution/ExecutionIntegrationTest.java</files>
  <action>
    Extend the live integration coverage to assert isolation behavior, then harden the implementation wherever those tests expose gaps. Add one test where Python attempts outbound network access and the run fails under `networkMode=none`. Add one test where user code attempts to write outside the tmpfs workspace and receives a controlled filesystem failure consistent with `readonlyRootfs=true` and non-root user `65534:65534`. Update `DockerSandboxRunner` and `ExecutionCoordinatorService` so every timeout, failure, or interruption path kills any live container, removes the container in `finally`, preserves collected stdout/stderr, and ends with one durable final state update instead of leaving the execution stuck in `RUNNING`.
  </action>
  <acceptance_criteria>
    - `rg -n "outbound network access|networkMode=none|write outside the tmpfs workspace|readonlyRootfs|65534:65534|stuck in RUNNING" src/test/java/com/collabeditor/execution/ExecutionIntegrationTest.java src/main/java/com/collabeditor/execution/service/DockerSandboxRunner.java src/main/java/com/collabeditor/execution/service/ExecutionCoordinatorService.java` returns the isolation and cleanup-hardening coverage.
    - `./gradlew test --tests "*ExecutionIntegrationTest"` exits with status 0.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "*ExecutionIntegrationTest"</automated>
  </verify>
  <done>Timeout and isolation guarantees are now validated against real containers, and the implementation cleans up correctly on every terminal path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add final cross-instance execution visibility tests and run the full suite</name>
  <read_first>
    .planning/phases/04-sandboxed-code-execution/04-VALIDATION.md
    src/main/java/com/collabeditor/execution/service/ExecutionEventRelayService.java
    src/main/java/com/collabeditor/execution/service/ExecutionBroadcastGateway.java
    src/test/java/com/collabeditor/execution/ExecutionEventRelayServiceTest.java
    src/test/java/com/collabeditor/websocket/DistributedCollaborationWebSocketHandlerTest.java
  </read_first>
  <files>src/test/java/com/collabeditor/execution/ExecutionEventRelayServiceTest.java, src/test/java/com/collabeditor/execution/ExecutionControllerTest.java, src/test/java/com/collabeditor/execution/ExecutionIntegrationTest.java</files>
  <action>
    Extend `ExecutionEventRelayServiceTest` into the final distributed execution visibility suite. Add one test that simulates two backend nodes subscribed to the same session execution channel and proves both nodes broadcast the same `execution_updated` payload when one execution transitions from `QUEUED` to `RUNNING` to `COMPLETED`. Add one controller-level test showing a successfully accepted execution returns `202` immediately while the terminal output arrives asynchronously on the websocket-relay path instead of the HTTP response body. Finish the phase by running the exact focused command `./gradlew test --tests "*ExecutionControllerTest" --tests "*ExecutionEventRelayServiceTest" --tests "*ExecutionIntegrationTest"` and then `./gradlew test`.
  </action>
  <acceptance_criteria>
    - `rg -n "two backend nodes|QUEUED|RUNNING|COMPLETED|execution_updated|202 immediately|asynchronously on the websocket-relay path" src/test/java/com/collabeditor/execution/ExecutionEventRelayServiceTest.java src/test/java/com/collabeditor/execution/ExecutionControllerTest.java` returns the final cross-instance visibility and async-response assertions.
    - `./gradlew test --tests "*ExecutionControllerTest" --tests "*ExecutionEventRelayServiceTest" --tests "*ExecutionIntegrationTest"` exits with status 0.
    - `./gradlew test` exits with status 0.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "*ExecutionControllerTest" --tests "*ExecutionEventRelayServiceTest" --tests "*ExecutionIntegrationTest"</automated>
    <automated>./gradlew test</automated>
  </verify>
  <done>Phase 4 ends with real-container language proof, isolation proof, and cross-instance room visibility proof, backed by focused and full-suite verification.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `./gradlew test --tests "*ExecutionIntegrationTest"`
- [ ] `./gradlew test --tests "*ExecutionControllerTest" --tests "*ExecutionEventRelayServiceTest" --tests "*ExecutionIntegrationTest"`
- [ ] `./gradlew test`
</verification>

<success_criteria>
- Python and Java both execute successfully in real Docker containers and return structured results.
- Timeout, no-network, read-only filesystem, and non-root guarantees are proven by live tests.
- Execution lifecycle remains room-visible across backend instances and the phase finishes with a green full test suite.
</success_criteria>

<output>
After completion, create `.planning/phases/04-sandboxed-code-execution/04-04-SUMMARY.md`
</output>
