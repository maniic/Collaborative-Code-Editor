---
phase: 05-integration-hardening-and-developer-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - build.gradle.kts
  - src/test/java/com/collabeditor/migration/FlywayMigrationTest.java
  - src/test/java/com/collabeditor/ot/CollaborationPersistenceIntegrationTest.java
  - src/test/java/com/collabeditor/websocket/DistributedCollaborationWebSocketHandlerTest.java
  - src/test/java/com/collabeditor/execution/ExecutionServiceTest.java
  - src/test/java/com/collabeditor/execution/ExecutionEventRelayServiceTest.java
  - src/test/java/com/collabeditor/execution/ExecutionIntegrationTest.java
autonomous: true
requirements:
  - QUAL-03
must_haves:
  truths:
    - "`./gradlew integrationTest` is the one obvious automated proof command for Phase 5."
    - "The canonical integration task runs a curated set of focused Testcontainers and live-Docker suites instead of relying on one mega-test class."
    - "The integration task inherits the same Docker and Testcontainers environment forwarding as the default Gradle test task."
    - "Persistence, Redis coordination, and execution flows are all represented in the curated integration suite."
  artifacts:
    - path: "build.gradle.kts"
      provides: "Dedicated Gradle `integrationTest` task and shared test-task environment wiring."
    - path: "src/test/java/com/collabeditor/migration/FlywayMigrationTest.java"
      provides: "Tagged PostgreSQL and Flyway bootstrapping proof."
    - path: "src/test/java/com/collabeditor/websocket/DistributedCollaborationWebSocketHandlerTest.java"
      provides: "Tagged PostgreSQL plus Redis distributed collaboration proof."
    - path: "src/test/java/com/collabeditor/execution/ExecutionServiceTest.java"
      provides: "Tagged execution persistence and admission proof."
    - path: "src/test/java/com/collabeditor/execution/ExecutionEventRelayServiceTest.java"
      provides: "Tagged Redis-backed execution relay proof."
    - path: "src/test/java/com/collabeditor/execution/ExecutionIntegrationTest.java"
      provides: "Tagged live Docker execution proof."
  key_links:
    - from: "build.gradle.kts"
      to: "src/test/java/com/collabeditor/migration/FlywayMigrationTest.java"
      via: "The new integration task should explicitly include tagged persistence bootstrap coverage."
      pattern: "integrationTest|Tag\\(\"integration\"\\)"
    - from: "build.gradle.kts"
      to: "src/test/java/com/collabeditor/websocket/DistributedCollaborationWebSocketHandlerTest.java"
      via: "The canonical verification run must include Redis coordination and relay behavior."
      pattern: "integrationTest|Tag\\(\"integration\"\\)"
    - from: "build.gradle.kts"
      to: "src/test/java/com/collabeditor/execution/ExecutionIntegrationTest.java"
      via: "The canonical verification run must include real Docker-backed execution proof."
      pattern: "integrationTest|Tag\\(\"integration\"\\)"
---

<objective>
Create the canonical Testcontainers-backed verification entrypoint for Phase 5.

Purpose: Phase 5 cannot claim "one automated run" until the existing focused integration suites are curated behind one obvious Gradle command.
Output: A dedicated `integrationTest` task plus explicit integration tagging across the persistence, Redis, and execution proof suites.
</objective>

<execution_context>
@/Users/abdullah/.codex/get-shit-done/workflows/execute-plan.md
@/Users/abdullah/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-integration-hardening-and-developer-docs/05-CONTEXT.md
@.planning/phases/05-integration-hardening-and-developer-docs/05-RESEARCH.md
@.planning/phases/05-integration-hardening-and-developer-docs/05-VALIDATION.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the dedicated Gradle integrationTest task and share the existing Docker/Testcontainers wiring</name>
  <read_first>
    .planning/phases/05-integration-hardening-and-developer-docs/05-CONTEXT.md
    .planning/phases/05-integration-hardening-and-developer-docs/05-RESEARCH.md
    .planning/phases/05-integration-hardening-and-developer-docs/05-VALIDATION.md
    build.gradle.kts
    src/test/resources/application-test.yml
  </read_first>
  <files>build.gradle.kts</files>
  <action>
    Update `build.gradle.kts` to register a dedicated Gradle `Test` task named exactly `integrationTest` with `group = "verification"` and a description explaining that it runs the canonical Phase 5 integration proof suites. Configure `integrationTest` to call `useJUnitPlatform { includeTags("integration") }`, inherit the same active Spring profile logic already used by the default `test` task, and forward the same Docker/Testcontainers environment values already present today: `DOCKER_HOST`, `TESTCONTAINERS_DOCKER_SOCKET_OVERRIDE`, `DOCKER_API_VERSION=1.44`, `user.home`, and `TESTCONTAINERS_RYUK_DISABLED`. Keep `./gradlew test` unchanged as the full-suite command; do not move tests into a new source set.
  </action>
  <acceptance_criteria>
    - `rg -n "integrationTest|includeTags\\(\"integration\"\\)|group = \"verification\"" build.gradle.kts` returns the dedicated task definition.
    - `rg -n "DOCKER_HOST|TESTCONTAINERS_DOCKER_SOCKET_OVERRIDE|DOCKER_API_VERSION|TESTCONTAINERS_RYUK_DISABLED" build.gradle.kts` returns the shared Docker and Testcontainers environment wiring.
    - `./gradlew integrationTest` exits with status 0.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew integrationTest</automated>
  </verify>
  <done>The repo has one canonical Gradle command for Phase 5 integration proof without disturbing the full default test suite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tag the persistence and Redis coordination proof suites as canonical integration coverage</name>
  <read_first>
    .planning/phases/05-integration-hardening-and-developer-docs/05-RESEARCH.md
    src/test/java/com/collabeditor/migration/FlywayMigrationTest.java
    src/test/java/com/collabeditor/ot/CollaborationPersistenceIntegrationTest.java
    src/test/java/com/collabeditor/websocket/DistributedCollaborationWebSocketHandlerTest.java
  </read_first>
  <files>src/test/java/com/collabeditor/migration/FlywayMigrationTest.java, src/test/java/com/collabeditor/ot/CollaborationPersistenceIntegrationTest.java, src/test/java/com/collabeditor/websocket/DistributedCollaborationWebSocketHandlerTest.java</files>
  <action>
    Annotate `FlywayMigrationTest`, `CollaborationPersistenceIntegrationTest`, and `DistributedCollaborationWebSocketHandlerTest` with the exact JUnit marker `@Tag("integration")`. Keep their existing `@SpringBootTest`, `@Testcontainers`, and `@ActiveProfiles("test")` behavior intact. Do not rename the classes or collapse them into one aggregate test; the goal is to keep each proof suite focused while making it part of the canonical Gradle integration run.
  </action>
  <acceptance_criteria>
    - `rg -n "@Tag\\(\"integration\"\\)" src/test/java/com/collabeditor/migration/FlywayMigrationTest.java src/test/java/com/collabeditor/ot/CollaborationPersistenceIntegrationTest.java src/test/java/com/collabeditor/websocket/DistributedCollaborationWebSocketHandlerTest.java` returns all three tagged suites.
    - `./gradlew integrationTest` exits with status 0.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew integrationTest</automated>
  </verify>
  <done>The canonical integration run now includes focused PostgreSQL and Redis coordination proof suites.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tag the execution persistence, relay, and live Docker proof suites as canonical integration coverage</name>
  <read_first>
    .planning/phases/05-integration-hardening-and-developer-docs/05-RESEARCH.md
    src/test/java/com/collabeditor/execution/ExecutionServiceTest.java
    src/test/java/com/collabeditor/execution/ExecutionEventRelayServiceTest.java
    src/test/java/com/collabeditor/execution/ExecutionIntegrationTest.java
  </read_first>
  <files>src/test/java/com/collabeditor/execution/ExecutionServiceTest.java, src/test/java/com/collabeditor/execution/ExecutionEventRelayServiceTest.java, src/test/java/com/collabeditor/execution/ExecutionIntegrationTest.java</files>
  <action>
    Annotate `ExecutionServiceTest`, `ExecutionEventRelayServiceTest`, and `ExecutionIntegrationTest` with the exact JUnit marker `@Tag("integration")`. Keep the existing container setup and test names intact so the canonical integration run proves execution admission persistence, Redis-backed execution relay behavior, and live Docker execution behavior in one pass without inventing a new mega-suite.
  </action>
  <acceptance_criteria>
    - `rg -n "@Tag\\(\"integration\"\\)" src/test/java/com/collabeditor/execution/ExecutionServiceTest.java src/test/java/com/collabeditor/execution/ExecutionEventRelayServiceTest.java src/test/java/com/collabeditor/execution/ExecutionIntegrationTest.java` returns all three tagged execution suites.
    - `./gradlew integrationTest` exits with status 0.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew integrationTest</automated>
  </verify>
  <done>The canonical integration run now covers the shipped execution flow from persistence through relay to live Docker sandbox behavior.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `./gradlew integrationTest`
</verification>

<success_criteria>
- `./gradlew integrationTest` exists and runs one curated automated proof path.
- The curated proof path includes persistence bootstrap, Redis coordination, and execution flow coverage.
- The repo keeps focused suites rather than replacing them with one giant integration test.
</success_criteria>

<output>
After completion, create `.planning/phases/05-integration-hardening-and-developer-docs/05-01-SUMMARY.md`
</output>
