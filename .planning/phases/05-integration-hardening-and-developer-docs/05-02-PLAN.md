---
phase: 05-integration-hardening-and-developer-docs
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - build.gradle.kts
  - src/main/resources/application.yml
  - src/test/java/com/collabeditor/common/HealthEndpointTest.java
  - Dockerfile
  - .dockerignore
autonomous: true
requirements:
  - DOCS-01
must_haves:
  truths:
    - "The app exposes a real unauthenticated `/actuator/health` endpoint that matches the already-permitted security route."
    - "The repository can build a runnable app container image from a checked-out workspace."
    - "The packaged app image is ready to participate in Compose healthchecks and still reach the Docker daemon through env-configured socket access."
  artifacts:
    - path: "build.gradle.kts"
      provides: "Actuator dependency needed for the Compose health surface."
    - path: "src/main/resources/application.yml"
      provides: "Runtime health endpoint exposure configuration."
    - path: "src/test/java/com/collabeditor/common/HealthEndpointTest.java"
      provides: "Automated proof that `/actuator/health` is available without auth."
    - path: "Dockerfile"
      provides: "Multi-stage container packaging for the Spring Boot app."
    - path: ".dockerignore"
      provides: "Lean Docker build context that excludes generated and planning artifacts."
  key_links:
    - from: "build.gradle.kts"
      to: "src/test/java/com/collabeditor/common/HealthEndpointTest.java"
      via: "Actuator must exist before the health endpoint test can pass."
      pattern: "spring-boot-starter-actuator|HealthEndpointTest"
    - from: "src/main/resources/application.yml"
      to: "Dockerfile"
      via: "Compose healthchecks and runtime env need a real health endpoint in the packaged app."
      pattern: "management|actuator|8080"
    - from: "Dockerfile"
      to: "src/main/java/com/collabeditor/execution/config/DockerExecutionConfig.java"
      via: "The containerized app still needs Docker client access for Phase 4 execution."
      pattern: "DOCKER_HOST|dockerClientConfig"
---

<objective>
Add the app packaging and health surface that the Compose stack depends on.

Purpose: Phase 5's documented local stack cannot be credible until the app has a real health endpoint and a reproducible container image.
Output: Actuator-backed health exposure, a focused health endpoint test, and a multi-stage Dockerfile with a lean build context.
</objective>

<execution_context>
@/Users/abdullah/.codex/get-shit-done/workflows/execute-plan.md
@/Users/abdullah/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-integration-hardening-and-developer-docs/05-CONTEXT.md
@.planning/phases/05-integration-hardening-and-developer-docs/05-RESEARCH.md
@.planning/phases/05-integration-hardening-and-developer-docs/05-VALIDATION.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the real health endpoint contract for Compose and verify it without authentication</name>
  <read_first>
    .planning/phases/05-integration-hardening-and-developer-docs/05-RESEARCH.md
    build.gradle.kts
    src/main/resources/application.yml
    src/main/java/com/collabeditor/auth/security/SecurityConfig.java
    src/test/java/com/collabeditor/common/HealthEndpointTest.java
  </read_first>
  <files>build.gradle.kts, src/main/resources/application.yml, src/test/java/com/collabeditor/common/HealthEndpointTest.java</files>
  <action>
    Update `build.gradle.kts` to add the exact dependency `implementation("org.springframework.boot:spring-boot-starter-actuator")`. Update `src/main/resources/application.yml` to expose only the health endpoint over HTTP using exact keys under `management.endpoints.web.exposure.include: health`. Create `src/test/java/com/collabeditor/common/HealthEndpointTest.java` as a focused Spring Boot test that verifies `GET /actuator/health` returns HTTP `200` without a bearer token and includes the string `"UP"` in the response body. Keep `/actuator/health` public in `SecurityConfig`; do not add new auth rules that would block Compose healthchecks.
  </action>
  <acceptance_criteria>
    - `rg -n "spring-boot-starter-actuator" build.gradle.kts` returns the exact Actuator dependency.
    - `rg -n "management:|exposure:|include: health" src/main/resources/application.yml` returns the health exposure configuration.
    - `rg -n "/actuator/health|status|UP|200" src/test/java/com/collabeditor/common/HealthEndpointTest.java` returns the unauthenticated health endpoint assertions.
    - `./gradlew test --tests "*HealthEndpointTest"` exits with status 0.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "*HealthEndpointTest"</automated>
  </verify>
  <done>The repo now has a real health endpoint that matches security configuration and is safe to use from Compose.</done>
</task>

<task type="auto">
  <name>Task 2: Create a multi-stage app Dockerfile and lean Docker build context</name>
  <read_first>
    .planning/phases/05-integration-hardening-and-developer-docs/05-CONTEXT.md
    .planning/phases/05-integration-hardening-and-developer-docs/05-RESEARCH.md
    Dockerfile
    .dockerignore
    build.gradle.kts
    gradlew
    gradle/wrapper/gradle-wrapper.properties
    src/main/resources/application.yml
    src/main/java/com/collabeditor/execution/config/DockerExecutionConfig.java
  </read_first>
  <files>Dockerfile, .dockerignore</files>
  <action>
    Create a multi-stage `Dockerfile` at the repo root. In the build stage, use a Java 17 build image, copy `gradlew`, `gradlew.bat`, `gradle/`, `build.gradle.kts`, `settings.gradle.kts`, and `src/`, then run the exact command `./gradlew --no-daemon bootJar`. In the runtime stage, use a Java 17 runtime image, install `curl` so Compose can run `curl -f http://localhost:8080/actuator/health`, copy the built Boot jar to `/app/app.jar`, expose port `8080`, set `DOCKER_HOST=unix:///var/run/docker.sock`, and use `ENTRYPOINT ["java","-jar","/app/app.jar"]`. Create `.dockerignore` with exact exclusions for `.git`, `.planning`, `build`, `.gradle`, `.idea`, and `*.iml` so Docker builds stay small and deterministic.
  </action>
  <acceptance_criteria>
    - `rg -n "bootJar|curl|EXPOSE 8080|DOCKER_HOST=unix:///var/run/docker.sock|ENTRYPOINT" Dockerfile` returns the exact build, healthcheck, and runtime markers.
    - `rg -n "^\\.planning$|^build$|^\\.gradle$|^\\.git$" .dockerignore` returns the required ignored paths.
    - `docker build -t collaborative-code-editor:test .` exits with status 0.
  </acceptance_criteria>
  <verify>
    <automated>docker build -t collaborative-code-editor:test .</automated>
  </verify>
  <done>The backend can be packaged into a reusable app image that is ready for Compose and health checks.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `./gradlew test --tests "*HealthEndpointTest"`
- [ ] `docker build -t collaborative-code-editor:test .`
</verification>

<success_criteria>
- `/actuator/health` exists and is usable by Compose without authentication.
- The repo builds a runnable app container image from source.
- The packaged app image is ready to participate in the local Compose stack.
</success_criteria>

<output>
After completion, create `.planning/phases/05-integration-hardening-and-developer-docs/05-02-SUMMARY.md`
</output>
