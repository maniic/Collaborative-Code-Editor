---
phase: 05-integration-hardening-and-developer-docs
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - .env.example
  - docker-compose.yml
autonomous: true
requirements:
  - DOCS-01
must_haves:
  truths:
    - "One Compose file starts `app`, `postgres`, and `redis` together with the correct environment contract."
    - "The app service mounts a configurable Docker socket path so Phase 4 execution still works when the app runs in a container."
    - "The Compose stack uses healthchecks and service dependencies that reflect the actual app, PostgreSQL, and Redis contracts."
  artifacts:
    - path: ".env.example"
      provides: "Documented local environment contract for database, Redis, JWT, and Docker socket settings."
    - path: "docker-compose.yml"
      provides: "Canonical local stack definition for app, PostgreSQL, and Redis."
  key_links:
    - from: ".env.example"
      to: "docker-compose.yml"
      via: "Compose should source the same JWT, database, Redis, and Docker socket variables it documents."
      pattern: "APP_DB_|APP_REDIS_|APP_JWT_SECRET|DOCKER_SOCKET_PATH"
    - from: "docker-compose.yml"
      to: "Dockerfile"
      via: "The app service should build from the packaged app image created in Plan 02."
      pattern: "build:|Dockerfile"
    - from: "docker-compose.yml"
      to: "src/main/resources/application.yml"
      via: "Compose env vars must align with the runtime property keys already used by Spring Boot."
      pattern: "APP_DB_URL|APP_REDIS_HOST|APP_JWT_SECRET"
---

<objective>
Create the full-stack Compose workflow and explicit local environment contract.

Purpose: Phase 5 cannot satisfy DOCS-01 until the repo has a concrete, reproducible Compose stack and a clearly documented env file contract.
Output: `.env.example` and `docker-compose.yml` that start the app, PostgreSQL, and Redis with the Docker socket path required for execution.
</objective>

<execution_context>
@/Users/abdullah/.codex/get-shit-done/workflows/execute-plan.md
@/Users/abdullah/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-integration-hardening-and-developer-docs/05-CONTEXT.md
@.planning/phases/05-integration-hardening-and-developer-docs/05-RESEARCH.md
@.planning/phases/05-integration-hardening-and-developer-docs/05-VALIDATION.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the local environment contract in .env.example</name>
  <read_first>
    .planning/phases/05-integration-hardening-and-developer-docs/05-CONTEXT.md
    .env.example
    src/main/resources/application.yml
    src/test/resources/application-test.yml
    src/main/java/com/collabeditor/execution/config/DockerExecutionConfig.java
  </read_first>
  <files>.env.example</files>
  <action>
    Create `.env.example` at the repo root with the exact variables `APP_DB_URL=jdbc:postgresql://postgres:5432/collabeditor`, `APP_DB_USERNAME=collabeditor`, `APP_DB_PASSWORD=collabeditor`, `APP_REDIS_HOST=redis`, `APP_REDIS_PORT=6379`, `APP_JWT_SECRET=change-this-to-at-least-32-characters`, and `DOCKER_SOCKET_PATH=/var/run/docker.sock`. Add short inline comments that explain `APP_JWT_SECRET` must be non-empty and at least 32 characters, and that `DOCKER_SOCKET_PATH` should be overridden for non-default local runtimes such as Colima.
  </action>
  <acceptance_criteria>
    - `rg -n "APP_DB_URL=jdbc:postgresql://postgres:5432/collabeditor|APP_DB_USERNAME=collabeditor|APP_DB_PASSWORD=collabeditor|APP_REDIS_HOST=redis|APP_REDIS_PORT=6379|APP_JWT_SECRET=|DOCKER_SOCKET_PATH=/var/run/docker.sock" .env.example` returns the exact environment contract.
    - `.env.example` contains the phrase `at least 32 characters`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "APP_DB_URL=jdbc:postgresql://postgres:5432/collabeditor|APP_DB_USERNAME=collabeditor|APP_DB_PASSWORD=collabeditor|APP_REDIS_HOST=redis|APP_REDIS_PORT=6379|APP_JWT_SECRET=|DOCKER_SOCKET_PATH=/var/run/docker.sock" .env.example</automated>
  </verify>
  <done>The repo has an explicit env-file contract for the canonical local stack.</done>
</task>

<task type="auto">
  <name>Task 2: Create the canonical docker-compose stack for app, PostgreSQL, and Redis</name>
  <read_first>
    .planning/phases/05-integration-hardening-and-developer-docs/05-RESEARCH.md
    docker-compose.yml
    Dockerfile
    .env.example
    src/main/resources/application.yml
  </read_first>
  <files>docker-compose.yml</files>
  <action>
    Create `docker-compose.yml` with exactly three services named `postgres`, `redis`, and `app`. Use `postgres:16-alpine` with database name `collabeditor`, user `collabeditor`, password `collabeditor`, a named volume for PostgreSQL data, and healthcheck `pg_isready -U collabeditor -d collabeditor`. Use `redis:7-alpine` with healthcheck `redis-cli ping`. Configure `app` to `build: .`, publish `8080:8080`, load variables from the same keys documented in `.env.example`, set `DOCKER_HOST=unix:///var/run/docker.sock`, mount `${DOCKER_SOCKET_PATH:-/var/run/docker.sock}:/var/run/docker.sock`, depend on healthy `postgres` and `redis`, and run a healthcheck using `curl -f http://localhost:8080/actuator/health`. Keep the app service environment aligned with Spring property names already in `application.yml`: `APP_DB_URL`, `APP_DB_USERNAME`, `APP_DB_PASSWORD`, `APP_REDIS_HOST`, `APP_REDIS_PORT`, and `APP_JWT_SECRET`.
  </action>
  <acceptance_criteria>
    - `rg -n "^services:|postgres:16-alpine|redis:7-alpine|build: \\.|8080:8080|DOCKER_HOST=unix:///var/run/docker.sock|DOCKER_SOCKET_PATH|/actuator/health" docker-compose.yml` returns the canonical stack markers.
    - `rg -n "depends_on:|healthcheck:|pg_isready|redis-cli ping|curl -f http://localhost:8080/actuator/health" docker-compose.yml` returns the service dependency and healthcheck wiring.
    - `docker compose --env-file .env.example config` exits with status 0.
  </acceptance_criteria>
  <verify>
    <automated>docker compose --env-file .env.example config</automated>
  </verify>
  <done>The repo has one Compose file that reproduces the full backend stack and preserves execution's Docker daemon access.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `docker compose --env-file .env.example config`
</verification>

<success_criteria>
- One Compose file starts the backend, PostgreSQL, and Redis together.
- The Compose app service keeps the execution Docker socket contract explicit.
- The repo documents all required env vars in `.env.example`.
</success_criteria>

<output>
After completion, create `.planning/phases/05-integration-hardening-and-developer-docs/05-03-SUMMARY.md`
</output>
