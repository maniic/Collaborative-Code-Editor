---
phase: 05-integration-hardening-and-developer-docs
plan: 04
type: execute
wave: 4
depends_on:
  - 05-01
  - 05-03
files_modified:
  - README.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
must_haves:
  truths:
    - "README.md enables a developer to start the backend from a clean checkout using the documented env and Compose workflow."
    - "README.md names the canonical verification commands `./gradlew integrationTest` and `./gradlew test`."
    - "README.md documents the live REST and WebSocket contract implemented in the repo, not imagined future endpoints."
    - "README.md explains the existing architecture and key design decisions with one high-signal diagram."
  artifacts:
    - path: "README.md"
      provides: "Executable onboarding guide, representative API/WebSocket contract, architecture overview, and design-decision rationale."
  key_links:
    - from: "README.md"
      to: "docker-compose.yml"
      via: "Quickstart instructions must reference the real Compose stack and env file."
      pattern: "docker compose|.env.example"
    - from: "README.md"
      to: "build.gradle.kts"
      via: "Verification instructions must use the canonical integration and full-suite Gradle commands."
      pattern: "integrationTest|./gradlew test"
    - from: "README.md"
      to: "src/main/java/com/collabeditor/auth/api/AuthController.java"
      via: "Auth docs must mirror the live controller routes."
      pattern: "/api/auth/register|/api/auth/login|/api/auth/refresh"
    - from: "README.md"
      to: "src/main/java/com/collabeditor/websocket/protocol/ServerMessageType.java"
      via: "WebSocket docs must mirror the live server event vocabulary."
      pattern: "document_sync|operation_ack|operation_applied|execution_updated"
---

<objective>
Replace the placeholder README with executable onboarding, representative contract docs, and architecture rationale.

Purpose: Phase 5 is not complete until a new developer can both run the stack and understand what the backend actually does by reading one accurate top-level document.
Output: A practical README with prerequisites, quickstart, verification, REST and WebSocket contract sections, an architecture diagram, and explicit design decisions.
</objective>

<execution_context>
@/Users/abdullah/.codex/get-shit-done/workflows/execute-plan.md
@/Users/abdullah/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-integration-hardening-and-developer-docs/05-CONTEXT.md
@.planning/phases/05-integration-hardening-and-developer-docs/05-RESEARCH.md
@.planning/phases/05-integration-hardening-and-developer-docs/05-VALIDATION.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the prerequisites, quickstart, and verification sections for a clean local setup</name>
  <read_first>
    .planning/phases/05-integration-hardening-and-developer-docs/05-CONTEXT.md
    .planning/phases/05-integration-hardening-and-developer-docs/05-RESEARCH.md
    README.md
    .env.example
    docker-compose.yml
    build.gradle.kts
  </read_first>
  <files>README.md</files>
  <action>
    Replace the placeholder contents of `README.md` with a real onboarding document that starts with `## Prerequisites`, `## Quickstart`, and `## Verification`. In `## Prerequisites`, list Java 17+, Docker, Docker Compose v2, and a Docker daemon/socket accessible to the app container. In `## Quickstart`, document the exact workflow: prepare env values from `.env.example`, run `docker compose --env-file .env.example up --build`, and confirm the app responds on `http://localhost:8080/actuator/health`. Also document the optional faster inner-loop path where a developer starts only `postgres` and `redis` with Compose and runs the app locally with Gradle. In `## Verification`, document the exact commands `./gradlew integrationTest` and `./gradlew test`, and describe what each command proves.
  </action>
  <acceptance_criteria>
    - `rg -n "## Prerequisites|## Quickstart|## Verification|docker compose --env-file \\.env.example up --build|http://localhost:8080/actuator/health|\\./gradlew integrationTest|\\./gradlew test" README.md` returns the exact onboarding and verification markers.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "## Prerequisites|## Quickstart|## Verification|docker compose --env-file \\.env.example up --build|http://localhost:8080/actuator/health|\\./gradlew integrationTest|\\./gradlew test" README.md</automated>
  </verify>
  <done>The README now provides an executable local-start and verification path instead of a placeholder slogan.</done>
</task>

<task type="auto">
  <name>Task 2: Document the representative REST and WebSocket contract from the live backend code</name>
  <read_first>
    README.md
    src/main/java/com/collabeditor/auth/api/AuthController.java
    src/main/java/com/collabeditor/session/api/SessionController.java
    src/main/java/com/collabeditor/execution/api/ExecutionController.java
    src/main/java/com/collabeditor/websocket/config/WebSocketConfig.java
    src/main/java/com/collabeditor/websocket/protocol/ClientMessageType.java
    src/main/java/com/collabeditor/websocket/protocol/ServerMessageType.java
  </read_first>
  <files>README.md</files>
  <action>
    Add `## REST API` and `## WebSocket Protocol` sections to `README.md`. In `## REST API`, document the representative routes `/api/auth/register`, `/api/auth/login`, `/api/auth/refresh`, `/api/sessions`, `/api/sessions/join`, `/api/sessions/{sessionId}/leave`, and `/api/sessions/{sessionId}/executions`, along with one concise example request/response flow for login, session creation or join, and execution enqueue. In `## WebSocket Protocol`, document the exact endpoint `/ws/sessions/{sessionId}`, the client message types `submit_operation` and `update_presence`, and the server message types `document_sync`, `operation_ack`, `operation_applied`, `operation_error`, `resync_required`, `participant_joined`, `participant_left`, `presence_updated`, and `execution_updated`. Explicitly note that session language is immutable, invite codes are used for joining, and Java execution requires a single-file package-less `Main` entrypoint.
  </action>
  <acceptance_criteria>
    - `rg -n "## REST API|/api/auth/register|/api/auth/login|/api/auth/refresh|/api/sessions/join|/api/sessions/\\{sessionId\\}/executions" README.md` returns the representative REST contract.
    - `rg -n "## WebSocket Protocol|/ws/sessions/\\{sessionId\\}|submit_operation|update_presence|document_sync|operation_ack|operation_applied|execution_updated" README.md` returns the live WebSocket contract and event vocabulary.
    - `README.md` contains the phrase `single-file package-less Main`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "## REST API|/api/auth/register|/api/auth/login|/api/auth/refresh|/api/sessions/join|/api/sessions/\\{sessionId\\}/executions|## WebSocket Protocol|/ws/sessions/\\{sessionId\\}|submit_operation|update_presence|document_sync|operation_ack|operation_applied|execution_updated" README.md</automated>
  </verify>
  <done>The README now mirrors the real backend contract that future frontend work and reviewers need to understand.</done>
</task>

<task type="auto">
  <name>Task 3: Add the architecture diagram, runtime constraints, and explicit design decisions</name>
  <read_first>
    README.md
    .planning/PROJECT.md
    .planning/phases/03-durable-persistence-and-multi-instance-coordination/03-CONTEXT.md
    .planning/phases/04-sandboxed-code-execution/04-CONTEXT.md
    src/main/resources/application.yml
  </read_first>
  <files>README.md</files>
  <action>
    Add `## Architecture` and `## Design Decisions` sections to `README.md`. In `## Architecture`, include exactly one Mermaid diagram showing clients connecting to the Spring Boot app, the app interacting with PostgreSQL for durable state, Redis for coordination and relay, and Docker for sandboxed execution. Under the diagram, explain the major subsystems `auth`, `session`, `websocket`, `ot`, `snapshot`, `redis`, and `execution`. In `## Design Decisions`, add concise rationale bullets for server-authoritative OT, snapshot-plus-replay recovery, Redis for 2-3 instance coordination, Docker-only sandboxing, and the fixed execution contract (`PYTHON`, single-file `JAVA Main`, no network, read-only filesystem, non-root). Also state the Docker socket requirement for the Compose-hosted app so the docs are honest about how execution works locally.
  </action>
  <acceptance_criteria>
    - `rg -n "## Architecture|```mermaid|PostgreSQL|Redis|Docker|auth|session|websocket|ot|execution" README.md` returns the architecture section and diagram markers.
    - `rg -n "## Design Decisions|server-authoritative OT|snapshot-plus-replay|Redis|Docker-only sandboxing|read-only filesystem|non-root|Docker socket" README.md` returns the design-decision rationale and runtime constraints.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "## Architecture|```mermaid|PostgreSQL|Redis|Docker|auth|session|websocket|ot|execution|## Design Decisions|server-authoritative OT|snapshot-plus-replay|Redis|Docker-only sandboxing|read-only filesystem|non-root|Docker socket" README.md</automated>
  </verify>
  <done>The README now explains both how to run the backend and why the major architectural choices were made.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `rg -n "## Prerequisites|## Quickstart|## Verification|docker compose --env-file \\.env.example up --build|http://localhost:8080/actuator/health|\\./gradlew integrationTest|\\./gradlew test" README.md`
- [ ] `rg -n "## REST API|/api/auth/register|/api/auth/login|/api/auth/refresh|/api/sessions/join|/api/sessions/\\{sessionId\\}/executions|## WebSocket Protocol|/ws/sessions/\\{sessionId\\}|submit_operation|update_presence|document_sync|operation_ack|operation_applied|execution_updated" README.md`
- [ ] `rg -n "## Architecture|```mermaid|PostgreSQL|Redis|Docker|auth|session|websocket|ot|execution|## Design Decisions|server-authoritative OT|snapshot-plus-replay|Redis|Docker-only sandboxing|read-only filesystem|non-root|Docker socket" README.md`
</verification>

<success_criteria>
- README.md gets a developer from clean checkout to a running local stack.
- README.md documents the actual REST and WebSocket contract already implemented in code.
- README.md captures the architecture and key design decisions of the backend.
</success_criteria>

<output>
After completion, create `.planning/phases/05-integration-hardening-and-developer-docs/05-04-SUMMARY.md`
</output>
